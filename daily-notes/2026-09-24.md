# 2026-09-24

## Checagem das pendências do Google Ads: o corte da Barra foi atingido (~BRT)

Checagem proativa das pendências de LAPE-38, 3 dias depois.

### 🔴 A Barra atingiu o corte — deixa de ser observação e vira achado

Em 21/09 ficou definido: *"se a Barra passar de ~700 cliques ainda com zero evento, há algo
que estes testes não alcançam"*. Está em **609** e a evidência já é conclusiva.

| Campanha | Cliques desde 17/09 | Eventos | % capturado | Gasto |
|---|---|---|---|---|
| CG P.MAX | 1.206 | 14 | 1,161% | R$ 171,31 |
| RECREIO P.MAX | 704 | 8 | 1,136% | R$ 202,19 |
| **BARRA P.MAX** | **609** | **0** | **0,000%** | **R$ 233,80** |

**A taxa das outras duas é estável e quase idêntica (1,16% × 1,14%)** — não é ruído, é uma
constante da operação. Com p = 1,152%, o esperado na Barra seria **7 eventos**; observado 0.

`P(0 em 609 | p=1,152%) ≈ 0,09%` — cerca de 1 em 1.160. **Não é azar.**

⚠️ E a Barra é a que **mais gasta** das três (R$ 233,80). Maior verba, zero visibilidade.

**Já descartado em 21/09, elo por elo:** landing tem o link · responde 302 · rastreio
zero-width embutido · aponta para `552139550932` · é a inbox 147 Mila_Barra · WAHA `working`.
A corrente do nosso lado está inteira.

**Sobra o lado do CQC/onpromedia:** o link `4A3A1A30` provavelmente não está cadastrado para
disparar webhook, ou dispara com `origem` diferente de `google` — e nesse caso nosso filtro o
descarta antes de gravar, que é o comportamento correto para o braço do Meta. **Só o Rayan /
onpromedia resolve.**

### 🔴 CORREÇÃO do que registrei em 21/09 — a janela de 90 dias

Escrevi que *"Vera Mattos clicou em 07/05 (137 dias, já fora)"* e que o *"Daniel Silva sai da
janela em ~4 dias"*. **Errado: confundi data do LEAD com data do CLIQUE.**

| Lead | Lead criado em | **Clique real** |
|---|---|---|
| Vera Mattos | 07/05 | **18/09** |
| Daniel Silva | 27/06 | **18/09** |
| Carlinho | 26/07 | 18/09 |
| Ulyses Alv | 27/07 | 17/09 |

São leads antigos que **voltaram e clicaram agora** — reengajamento, o caso que a regra binária
de canal trata como `preservado_reengajamento`. Medido: **0 cliques fora da janela de 90 dias,
0 saindo nos próximos 15**. Ninguém corre risco de recusa por idade, ao contrário do que ficou
escrito no dia 21.

Os 6 leads antigos com gclid **todos têm linha** em `google_ads_cliques` — nenhum cairia em
`sem_data_de_clique`.

### Demais sinais

- Fila da Fase 3: **0** — nenhum lead com gclid matriculou ainda
- Leads com gclid: 21 → **23**
- Cliques sem campanha resolvida: **1** (clique novo, latência normal do `click_view`)
- Conversões enviadas: **0** — segue em dry run, cron ainda não criado

### ⚠️ Estado do repositório nesta sessão

Não commitei nada além deste arquivo nem fiz push. O repo está com **trabalho de outra sessão
em andamento**: 1 commit local não enviado (`09f5c211`, fix de mobile), working tree com
`AlunosPage.tsx`, `kpisAlunosVivosCanonicos.ts` e 8 migrations novas não commitadas, e 12
commits no remoto (performance/cache e reconciliação). Nada disso toca o Google Ads.
A integração é decisão de quem está com o trabalho aberto.

## LAPE-42/43 — histórico conferido, e caches que quebravam em transação só-leitura (~16h BRT)

- **Histórico da `sync_run_items` completo:** job 296 rodou 02h15→02h19, 7.830 runs em `sync_run_retencao`; conferência com 20 runs (21.943 linhas) = 0/0. Backlog de faturas rodou no horário novo (04h11); setembro atualizado 15h18, fila vazia, nenhum run órfão.
- **O Luciano (Devin) mexeu nas duas frentes em 23/09 20h28–21h27**, depois que paramos: cache no nome público `get_kpis_alunos_canonicos` reusando a nossa impressão digital (`20260924236000`), cache TTL em 11 outras RPCs (`paginas_rpc_cache`, `20260925020000`), aquecedor a cada 4 min (`dashboard-aquecer-caches`, jobid 305: **277 execuções/24h, ~84 min de banco/dia, 10 falhas, máx 131 s**) e poda diária da `sync_run_items` (`sync-run-items-expurgo-diario`, jobid 304, usando o nosso histórico).
- 🔴 **Defeito achado e corrigido (`20260926010000`, OK do Hugo):** 12 funções gravam cache na chamada; três têm chamador STABLE (`get_situacao_alunos_resumo_v1` → Sol/TOM/Lia/app; `get_agenda_semana_v2`; bases do financeiro). Em cache miss via PostgREST, o INSERT levantava `25006` e a chamada inteira falhava — provado em transação read only. Todo INSERT/DELETE em `*_cache` das 12 agora está em bloco `exception when read_only_sql_transaction`. Prova depois: situação e agenda semana respondem em read only; em transação normal o cache segue gravando (11→12). Não deu para ver se já falhou com usuário real (logs do Postgres fora pelo MCP).
- **Sol sem EXECUTE em `get_kpis_alunos_canonicos` pela 2ª vez** (o wrapper novo não repôs) — devolvido na mesma migration.
- **Arquivos:** `…192000` (poda diária) apagado, substituído pela do Luciano; `…191000` (troca da tabela) movido para `migration-drafts/…_NAO_APLICADA` — segue necessário, apagar linha não devolve os 13 GB ao disco, mas precisa ser reconciliado com a poda dele. `npm run mapa:banco`: 606 tabelas/views, 1.626 funções, sem objeto sem domínio.
- **Pendente:** (1) front de Alunos ainda chama os `_cache_v1`, agora redundantes — decidir voltar ao nome público; (2) `get_kpis_alunos_admin_operacional` com cache **só por TTL de 15 min**, sem impressão digital — contra a regra de dado que não pode estar velho; (3) avaliar o custo do aquecedor; (4) troca da tabela com o Hugo; (5) avisar o Luciano do que mudou.
- **Resolvido na sequência (~16h30, OK do Hugo):** (1) front da Lista de Alunos voltou a chamar os nomes públicos (`get_kpis_alunos_admin_operacional`, `get_kpis_alunos_canonicos`), mantendo só a carga em paralelo dos dois KPIs e da anamnese; (2) `20260926020000`: a chave do cache do KPI operacional passou a incluir `kpis_alunos_cache_impressao_v1()` — paridade md5 com o leitor interno (`34186b0a`, Barra/set), 2ª chamada 1 ms. `tests/emusysMatriculasStatusV131Frontend.test.mjs`: 8/2, os 2 que falham já falhavam no HEAD (carteira e ModalRelatorio).

## LAPE-32 — aba Contratos no celular (~18h BRT)

A quinta aba do Administrativo ganhou tela própria. Das sete, duas estão
portadas agora (Lançamentos e Contratos); as outras cinco seguem com a faixa.

### A pergunta muda de lugar

A tabela do computador tem nove colunas e ordenação livre: ela responde *"quais
contratos vencem na janela X"*. No balcão a pergunta é **"quem eu chamo para
renovar, e em que ordem"** — então o eixo virou URGÊNCIA, com blocos "Já
venceu" → "até 7 dias" → "8 a 14" → "15 a 30" → "depois". Mesma decisão da fila
da Chamada: o corte é de conteúdo, não de largura.

⚠️ **`Já venceu` precisou existir.** O recorte "Este mês" lista o que termina na
competência, não o que cabe numa janela de dias — no fim do mês isso inclui data
já passada. Sem a faixa própria, o contrato mais urgente de todos cairia no
mesmo balde de "até 7 dias".

### 🔴 O ganho que só existe na tela pequena

`telefone` e `whatsapp` **já chegam** em `ContratoVencendo` e a tabela do
desktop **descarta os dois** (conferido: `TabContratosVencendo.tsx` não menciona
nenhum). No celular "quem eu chamo" virou um toque. É o mesmo tipo de achado do
`colisoesDeSala` na Agenda — a tela menor diz mais, não menos.

📊 **E o dado que isso revelou: 15 contratos têm número utilizável e 14 não.**
Quase metade dos contratos vencendo não tem como ser contactada pelo cadastro.
Não é defeito da tela; é o que o cadastro tem.

### 🔴 Já existiam CINCO implementações de link de WhatsApp

`PainelFarmer/DashboardTab`, `Alunos/ContatoPopover`, `ModalFichaAluno` (duas) e
`lib/fichaLink`. Cada uma com sua normalização — a do `ContatoPopover` faz
`'55' + dígitos` **sem recusar número curto**, o que gera link que abre o
WhatsApp em branco.

Esta não é a sexta: o número sai de `numeroDeEnvioDoCadastro` (a mesma ordem
whatsapp→telefone da edge e da RPC) e passa por `normalizarTelefone`, que recusa
menos de 10 dígitos. Há teste proibindo a lib de voltar a limpar dígitos ou
prefixar o DDI por conta própria.

### O que a medição mudou no desenho

| defeito medido | causa | decisão |
|---|---|---|
| 9 de 12 linhas truncadas | curso 80px + professor 86px + **unidade 51px** em 257px úteis | unidade ganhou linha própria, **só no Consolidado** |
| nome de aluno cortado (faltavam 86px) | `truncate` no título | `line-clamp-2` — cortar o curso custa contexto, cortar o aluno custa saber em quem se clica |
| 2 alvos de 40px | alternador Aula/Fatura | 44px |
| ícone cinza em 14 linhas sem explicação | `title` só aparece no hover, que no telefone não existe | `PhoneOff` no lugar do balão apagado |

**Placar a 390px:** rolagem lateral 0 · vazamento 0 · alvos abaixo de 44px 0 ·
truncados 2 (faltam 2px e 5px, nome de curso) · 3 telas.

⚠️ **Não inventei limiar de "poucas aulas".** Quantas contam como poucas não foi
medido; o número vai como dado ao lado da data. Há teste que reprova se alguém
acrescentar o corte.

### Por que a faixa saiu

Nos **cinco** recortes a rolagem lateral é zero — ou seja, a faixa, que diz
literalmente *"Funciona, mas rola para o lado"*, já estava afirmando algo falso
sobre esta aba.

⚠️ **"Sem fatura" NÃO tem tela própria** e fica declarado: é outra consulta, cai
no `PainelAlunosSemFatura`, que o CSS de tabela-como-card deixa legível (0
vazamento, 0 rolagem) mas ainda tem **7 alvos abaixo de 44px e 12,6 telas**.

### Travas

Dois testes reprovaram ao marcar a aba como portada — `administrativoMobile` e
`administrativoAbasColapsam`. **É o comportamento certo**: eles existem para
impedir que a faixa suma sem porte. Ambos atualizados, e o segundo ganhou um
assert novo que **cobra a prova**, não a lista: a aba declarada portada precisa
renderizar `<ContratosMobile>` e ter a bifurcação por shell.

`tests/contratosMobile.test.mjs`: 17 asserts, **6 de 6 mutantes pegos** (vencido
no balde errado, coluna do critério, DDI à mão, sinal saindo da contagem em vez
do booleano, faixa vazia virando bloco, contagem sem o "≥").

⚠️ O teste compila com **esbuild** em vez de importar direto: `node --test` não
resolve o import sem extensão de `numeroDaConversa.ts`, e alterar aquele módulo
compartilhado só para meu teste rodar seria conserto pelo lado errado.

**Desktop provado intocado** a 1440px: 9 colunas originais, `display: table`,
nenhum link `wa.me`, shell mobile não montado. A bifurcação tem guarda mecânica
— o bloco de render do desktop sai byte a byte igual (9.524 chars).

Suíte: **1.319 testes, 0 falhas**.

## LAPE-32 — trilho que não dizia ter mais, e a escada de filtros da Lojinha (~19h BRT)

Duas perguntas do Hugo olhando o app no telefone, as duas respondidas medindo a 390px
no Playwright antes de mexer em qualquer linha.

### "Não consigo arrastar pro lado" — era o mouse, mas havia defeito ao lado

Medido: o trilho **rola** (`scrollWidth` 1322 contra `clientWidth` 380, `touch-action: auto`,
`scrollLeft` programático funciona). Arrastar com o **ponteiro** nunca funciona num contêiner
de rolagem nativo — o dedo funciona. A mecânica estava certa.

🔴 O que a mesma medição revelou: **7 dos 9 chips ficam fora da vista**, e "Alunos novos"
(42 lançamentos) começa a **1179px** — três telas de arrasto. O trilho não dizia que havia
mais nada, e "não consigo arrastar" é o sintoma disso: ninguém tenta arrastar o que parece
terminar ali.

Regras puras em **`src/lib/trilhoRolavel.ts`** (`alvoDeRolagem`, `bordasDoTrilho`,
`mascaraDoTrilho`), ligadas por `src/mobile/useTrilhoRolavel.ts`:

- ⚠️ O esmaecimento é **dinâmico**, não máscara fixa à direita. A fixa (que o trilho de abas
  desta mesma tela usa) mente nas duas pontas: promete conteúdo depois do fim e nunca diz que
  ficou fila para trás. Medido nas três posições: começo (esquerda sólida / direita esmaecida),
  meio (as duas), fim (esquerda esmaecida / direita sólida).
- ⚠️ A rolagem é escrita em **`scrollLeft`, nunca por `scrollIntoView`** — este mexe nos dois
  eixos, e o ancestral vertical aqui é o `<main>` do shell: escolher um filtro daria um salto
  na lista inteira.
- ⚠️ Chip **já visível não move o trilho**. Centralizar o primeiro chip — que abre aceso —
  empurraria o trilho para longe do começo, escondendo o que já estava certo.
- ⚠️ `WebkitMaskImage` é obrigatório: sem ele o esmaecimento não existe no iOS.

Medido depois: clicar em "Alunos novos" leva o trilho de 0 → 942 e o chip fica inteiro à vista.

**Não estendi para os outros 4 trilhos mobile** (Distribuição, Turmas, Histórico LTV,
Contratos): medidos, são **5 de 7 visíveis** com 49–129px escondidos — não é o mesmo defeito
do Administrativo (2,5 de 9, 942px). Declarado, não escondido.

### Os filtros do Catálogo (Lojinha)

Medido a 390px: `flex-wrap` produzia uma **escada** — larguras fixas 192/176/160/139/144, cinco
bordas direitas diferentes e ~130px de vazio por linha. E **3 controles com 40px** de altura
(busca e os dois selects), abaixo do alvo de 44.

- Os 40px nasciam do `Input` e do `SelectTrigger`: ganharam `max-lg:min-h-[44px]`, a **mesma
  regra que o `Button` já tinha**. Corrigido na raiz, alcança o app inteiro.
- A barra vira **grade de 2 colunas no celular** (`max-lg:grid max-lg:grid-cols-2`), com a busca
  ocupando a linha inteira. Depois: **duas bordas (186 e 351) no lugar de cinco**, 44px em todos.
- Achado de passagem: o emoji do item "Todos Status" estava **corrompido no fonte**
  (byte solto, aparecia como `�`). Virou ⚪.

Placar da Lojinha a 390px: rolagem lateral **0**, alvos abaixo de 44px **0**, vazamento real **0**
(os dois "vazamentos" detectados são abas dentro do próprio trilho rolável).

Desktop provado a 1440px: barra em `flex`, larguras 192/176/160/139/144, alturas 40/40/40/36/36
— idênticas.

### Três tropeços que valem registro

- 🔴 **Declarei perda de trabalho que não houve.** Durante a medição, o `git status` veio limpo,
  o `AdministrativoMobile.tsx` apareceu sem minhas mudanças e o reflog mostrava
  `reset: moving to HEAD`. Concluí reversão por sessão paralela. **Era a janela de um
  `git stash`/`pop` de outra frente** — o estado voltou íntegro minutos depois. Lição: com várias
  sessões no mesmo repo, ler `git status` uma vez não é ler o estado; conferir o disco antes de
  concluir perda.
- 🔴 **`\b` dentro de heredoc vira backspace (0x08).** Escrevi os asserts novos por
  `cat <<'EOF'` e `/\bfetch\b/` virou `/<BS>fetch<BS>/`, que nunca casa. A mutação pegou:
  **1/3 na primeira rodada**, com `fetch` e `await` passando batido. Já estava documentado no
  CLAUDE.md pelo caso da repescagem da evasão, e repeti. Depois do conserto: **4/4**.
- ⚠️ **O teste vetava `useEffect` por inteiro** em `administrativoMobile.test.mjs` ("busca no
  efeito é fonte própria por outro nome") e reprovou código certo — o efeito que rola o trilho
  não busca nada. Vetar o MECANISMO em vez da substância é o mesmo defeito da suíte de agentes
  de 06/09. Hoje proíbe `fetch`/`.rpc(`/`axios`/`use*Query` e `await` dentro de efeito.

`tests/trilhoRolavel.test.mjs`: 10 asserts, **6/6 mutantes pegos**. Suíte: **1.329 testes,
0 falhas**. `tsc` sem erro nos arquivos da frente.

## LAPE-42 — Lista de Alunos aparece na hora ao voltar (~19h BRT)

**Pergunta do Hugo:** "botei cache e por que ainda carrega quando volto para Alunos?" — o cache era só de servidor (RPCs pesadas); a página jogava a lista fora ao sair e refazia ~26 leituras.

**Diagnóstico medido:**
- As leituras "em dobro" que eu tinha visto eram **artefato do `React.StrictMode`** (`index.tsx`), que em dev monta a tela duas vezes — em produção não existem. A proposta de "cortar duplicadas" foi retirada.
- A lista de `alunos` custa **150 ms no banco** (894 kB, sem coluna calculada, RLS já com InitPlan); sozinha pelo HTTP 0,45–0,95 s, disputando com as outras leituras da página 1,2–3,0 s (`pg_stat_statements`: média 1 s, máx 6,6 s). O tempo é **fila**, não consulta.

**Feito:** cópia em memória, padrão *stale-while-revalidate* (mesma forma do cache da Agenda):
- `src/lib/alunosListaCopia.ts` (regra pura) + `alunosListaCopiaSessao.ts` (descarta no `SIGNED_OUT`).
- Só memória — a lista tem telefone, nascimento e diagnóstico da anamnese (OWASP: nada de PII em storage do navegador). Some no F5.
- Chave usuário+unidade+período; gravada com a chave **dos dados** (`chaveDosDados`), porque há um render com unidade nova e lista velha. Validade 30 min, máx. 6 cópias.
- A cópia nunca afirma dinheiro: inadimplência gravada como `undefined`, alerta de `status_pagamento` escondido.
- Enquanto mostra a cópia: selo âmbar "Atualizando… lista de HH:MM" no lugar de "carteira ao vivo" e área inteira `inert` (sem clique/foco) — decisão do Hugo: só se age sobre dado fresco. Falha na atualização vira "Não consegui atualizar" + "Tentar de novo", nunca trava em silêncio.
- `ModalNovoAluno.onSalvar` passava o id do aluno para `carregarDados`; virou `() => carregarDados()`.

**Resultado (dev, Consolidado, 1440px):** voltar para Alunos → tabela em **0,2 s** (antes 3–5 s de spinner), ações liberadas em 5,2 s, depois "ao vivo". `tests/alunosListaCopia.test.mjs` 10/10, 2 mutantes mortos; suíte principal 1.339 testes, 0 falhas.

**⚠️ Deslize meu:** rodei `git stash`/`pop` para comparar o `tsc` e isso guardou por ~2 min o trabalho não commitado da sessão do LAPE-32 — voltou íntegro, mas não repetir (várias sessões no mesmo working tree).

**Pendências:** primeira entrada e F5 seguem em 3–5 s (só melhora aliviando o banco: aquecedor jobid 305, leitura paralela das 2 páginas). Não commitado — aguardando OK.

## Presença "sumindo" na Chamada e cache por versão em todos os caches de servidor (~20h BRT)

**Relato:** Arthur não conseguia "dar presença". Hugo: *"não faz sentido ter cache se a gente tem que esperar o cache resetar para ver o que clicou"*.

**Causa medida:** a presença GRAVAVA (escritas `agenda_secretaria` em toda hora do dia, inclusive 19h), mas `get_agenda_dia_v2` — lida pela Agenda e pela Chamada — guardava o dia **5 min por tempo** em `paginas_rpc_cache` (criado em 24/09, frente de desempenho do Luciano/Devin). A Chamada relia a foto velha. Eu tinha mexido nessa função à tarde (fix de só-leitura) sem conferir a invalidação. Não era a cópia da Lista de Alunos.

**Feito:**
1. `20260926030000` — Agenda sem cache (0,3 s quente, 1,8 s frio no Consolidado), fotos apagadas, ACL conferida. Commit 612e6986.
2. Auditoria: **12 caches** de 23-25/09, todos por TTL (inadimplência e faturas 10 min, comercial/conciliação 30 min, etc.); só os KPIs de alunos tinham impressão digital (que atrasa 1–60 s). A chave de inadimplência e faturas Emusys **não tinha escopo do usuário**.
3. `20260926040000` — **cache por versão**: `cache_versao_log` alimentado por gatilho de statement em **46 tabelas** (fecho de 65 funções + views; só `auth` fora do public; `sync_run_items` fora de propósito), `cache_dependencias` (12 funções), `cache_versao_v1` (count:max:sum + escopo) na chave de cada uma, poda a cada 10 min (jobid 309) com carimbo em `automacao_log`.
   - Ensaio com rollback provou: 2ª leitura sem escrita = cache; escrita → versão `0:0:0`→`1:1:1` → recalcula; escrita de `authenticated` passa pelo gatilho; função sem dependências é recusada; custo da versão ~1 ms.
   - ⚠️ 1º ensaio deu **deadlock** com o sync (46 `create trigger` + `drop trigger` numa transação) — refeito em 3 partes: A (estrutura), B (gatilhos um por transação, lock_timeout 3 s, retry — 46/46 na 1ª), C (chaves, só depois de os 46 existirem).
   - Produção: 12/12 chamadas ok como admin; log já recebendo `sync_runs`/`emusys_experimentais_raw`.

**👁️ OBSERVAR:** crescimento de `cache_versao_log` e das tabelas de cache (poda a cada 10 min), e se a taxa de acerto do cache cai demais no horário comercial (escritas frequentes = recálculo frequente — é o preço de nunca mostrar dado velho). Checagem: `select detalhes from automacao_log where evento='cache_versao' order by id desc limit 3;`.

**Pendência:** avisar o Luciano — as funções dele agora têm `public.cache_versao_v1(...)` na chave; recriá-las a partir dos arquivos de 24-25/09 apaga a correção em silêncio.
