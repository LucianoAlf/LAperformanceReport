# 2026-06-25

## Sanitização de arquivos lixo
- **14:40 BRT** — Limpeza da raiz do repo (commit `4b90929`): removidos 41 arquivos lixo — 6 `.txt` de debug (validação do SKILL.md), 6 `.md` de planejamento legado + `workflow_emusys_fixed.json`, e `mockups/` + `wireframes/` (28 protótipos HTML).
- **14:50 BRT** — Arquivamento de docs (commit `5adb217`): 39 docs pontuais/redundantes movidos p/ `docs/archive/` — saneamento CG Maio2026 (V1–V4), migrações FASE2A/2B + REGLA_KPI V2/V3/V4 (já aplicadas), 8 `PLANO*.md` de fev, `APP_TSX_SUPER_FOLHA.md` (229KB, dump morto). Preservados os 3 SQLs vivos referenciados no CLAUDE.md (FASE1, REGLA_KPI_V5, VALIDACAO_V5_FINANCEIRO).
- **Gotcha descoberta:** `App.tsx`/`constants.tsx`/`types.ts` da RAIZ são código VIVO (`src/router.tsx` importa `../App`). Quase removidos como "código morto" — build acusou, restaurados antes do commit. Registrado em `.claude/memory/padroes-codigo.md`.
- **Não tocado** (decisão do usuário): `backups/dump_pre_migracao_leads.backup` (1.7MB versionado), `scripts/` (88 one-shot), `data/img` (9 fotos untracked).

## Boas-vindas da Equipe (carrossel por unidade) — Sucesso do Aluno
- **Tarde/noite** — Validação iterativa via curl do formato no WhatsApp (UAZAPI): descoberto que `/send/carousel` faz tanto carrossel com botões quanto galeria (sem `buttons`); `text:""` no card aceito; botão URL com texto custom "Entrar na comunidade"; **carrossel não renderiza em iPhone** (entregue mas invisível — confirmado com a Anne). Encoding UTF-8 via arquivo resolve mojibake do curl no Windows.
- **Spec + plano** (commits `09591e6`, `fb0c966`) em `docs/superpowers/`. Brainstorming definiu: tela em Sucesso do Aluno que lista automações + edita texto; carrossel manual (Fase 1); equipe fixa em tabela.
- **Implementação** (branch `feat/boas-vindas-equipe-sucesso-aluno`, subagent-driven): tabela `staff_unidade` + seed (Recreio/CG/Barra + CEOs globais); colunas `link_comunidade`/`secretaria_*` em `unidades`; template editável `boas_vindas_equipe` em `crm_templates_whatsapp`; edge `enviar-boas-vindas-equipe` (commit `51cd54f`, self-contained, `verify_jwt:true`, testada ok em produção); frontend hook+`AutomacoesTab`+subaba (commits `b88fae3`,`4361bc5`,`d5118d9`, build verde). Bucket `staff-fotos` (público) com as fotos. Edge temporária `upload-staff-foto` removida.
- **Pendente:** confirmar números reais das secretarias (mapa ambíguo, Recreio `3955-1135` sem 9º dígito); merge da branch; `AdminChatPanel.tsx` apareceu modificado no working tree (formatação de msg interativa) — não é da feature, deixado unstaged.
