# 2026-06-25

## Sanitização de arquivos lixo
- **14:40 BRT** — Limpeza da raiz do repo (commit `4b90929`): removidos 41 arquivos lixo — 6 `.txt` de debug (validação do SKILL.md), 6 `.md` de planejamento legado + `workflow_emusys_fixed.json`, e `mockups/` + `wireframes/` (28 protótipos HTML).
- **14:50 BRT** — Arquivamento de docs (commit `5adb217`): 39 docs pontuais/redundantes movidos p/ `docs/archive/` — saneamento CG Maio2026 (V1–V4), migrações FASE2A/2B + REGLA_KPI V2/V3/V4 (já aplicadas), 8 `PLANO*.md` de fev, `APP_TSX_SUPER_FOLHA.md` (229KB, dump morto). Preservados os 3 SQLs vivos referenciados no CLAUDE.md (FASE1, REGLA_KPI_V5, VALIDACAO_V5_FINANCEIRO).
- **Gotcha descoberta:** `App.tsx`/`constants.tsx`/`types.ts` da RAIZ são código VIVO (`src/router.tsx` importa `../App`). Quase removidos como "código morto" — build acusou, restaurados antes do commit. Registrado em `.claude/memory/padroes-codigo.md`.
- **Não tocado** (decisão do usuário): `backups/dump_pre_migracao_leads.backup` (1.7MB versionado), `scripts/` (88 one-shot), `data/img` (9 fotos untracked).
