# 2026-06-20 — LA Performance Report

## Webhook caixa Sol (Sucesso do Aluno) — fix + monitoramento
- Causa raiz confirmada nos logs: `webhook-whatsapp-inbox` foi para **v45 com `verify_jwt: true`** → todas as entregas do UAZAPI rejeitadas com **401** → caixa 3 (Sol) parou de receber silenciosamente (~37 min).
- Fix: redeploy com `--no-verify-jwt` → v46. Logs confirmam transição **401 (v45) → 200 (v46)**; chamada real do UAZAPI processada (5s exec) com 200.
- Health check: endpoint `?_health=1` no `webhook-whatsapp-inbox` retorna 200 sem processar (permite ping sem auth).
- Nova edge `monitor-saude-webhook`: pinga o webhook sem JWT a cada 10 min (pg_cron, jobid=23); se != 200 envia alerta WhatsApp para Luciano (`5521966583325`).
- Commit `a4ed193`: health check + monitor.
- Verificação no banco: únicas entradas reais na caixa 3 são testes do número `5521964171223` (Hugo); resto são saídas da Fabi. Nenhum aluno externo mandou ainda — falta teste fim-a-fim com terceiro.

## enviar-boas-vindas-matricula — notificação para equipe
- `NUMERO_FABI` substituído por array `NOTIFICAR_EQUIPE` (Fabi + Jessyca); loop envia notificação de nova matrícula a cada membro e registra na caixa Sol.
- Commit `70767d2`.

## Régua do professor — troca de fonte p/ lead_experimentais (16h)
- **Diagnóstico**: a taxa de conversão do professor (`get_kpis_professor_periodo`) lia de `leads` (1 exp/pessoa) → subcontava realizadas (351/388) + perdia 62 por agregação → **taxa inflada**. Fonte certa = `lead_experimentais` (1 linha/aula). Simulação com CSV de CG (17 exp): match 100%, status 16/17 ok, curso 100% nulo.
- **Edge `sync-presenca-emusys`** (deploy CLI, internal v33): grava `curso_interesse_id`; coleta experimentais canceladas; **auto-faltou subordinado ao `/aulas/`** (só marca faltou com prova de aula, limiar 7→15d) — mata a falta-por-silêncio (caso José). Descoberta: "Rodolfo cancelada" era divergência interna Emusys (tela agendamento ≠ `/aulas/`), não bug.
- **Backfill mar–jun** (janelas 7d + throttling; `dias=30` em rajada estoura rate limit Emusys): curso **27%→75%**, +56 experimentais recuperadas.
- **RPC** (migration `rpc_professor_experimentais_fonte_lead_experimentais`): CTE `experimentais` agora de `lead_experimentais`. Taxas desinflam — Erick Osmy 67%→31%, Lucas Guimarães 100%→60%, Valdo 58%→43%. Validado: RPC em produção bate com a simulação.
- **Decisão Alf**: experimental de aluno existente (2º instrumento) conta na régua. Pendente decisão #1 (taxa usada p/ premiar?).
- Doc: `docs/hugo/2026-06-20-regua-professor-fonte-lead-experimentais.md`.

## Regra global
- Adicionada regra "Notas Diárias por Projeto" no `~/.claude/CLAUDE.md`: log diário em `<projeto>/daily-notes/AAAA-MM-DD.md`.
