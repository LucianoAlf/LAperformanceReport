# 2026-08-12

## Webhook de lead do Emusys parado há 21h + virada para o observador (~10h30 BRT)

### O que aconteceu

Pergunta do Hugo ("mexemos na função de pegar leads do webhook?") levou a um incidente
ativo, não a uma dúvida de histórico.

**Causa-raiz:** o commit `65e10fb3` (11/08 14:53 BRT, gerado pelo Devin) importou
`data_nascimento` do Emusys e, na migration `20260811160000`, fez
`CREATE OR REPLACE FUNCTION upsert_lead(..., p_data_nascimento date DEFAULT NULL)`.
Lista de parâmetros diferente **não substitui — cria overload**. Ficaram duas funções, e
como a nova tem `DEFAULT` no 11º argumento, uma chamada com 10 args casa nas duas:

```
function upsert_lead(unknown, unknown, unknown, unknown, unknown, unknown,
                     integer, unknown, boolean, date) is not unique
```

É a armadilha já documentada no CLAUDE.md, repetida.

**Impacto medido:**
- Primeiro erro **11/08 15:01 BRT**, 8 min depois do commit. 97 execuções do workflow
  `EB0LibpOJCLhKp7M` em erro, 100% do volume, até 12/08 09:28.
- Leads/dia antes: 40, 33, 38, 27, 12, 48. Depois das 15h de 11/08: **4**. Em 12/08 até
  a descoberta: **zero**.
- **22 leads** não entraram na base (todos com telefone).
- Atingiu as campanhas em silêncio: `agente-webhook/index.ts:591` chama a mesma RPC com
  10 args nomeados e **não checa o `error`** — a tool `transfer` seguia transferindo, mas
  o lead parava de entrar no funil comercial sem nenhum sinal.

### A rede de segurança existia e estava desligada

O observador (`debug-webhook-emusys-observador`) **não** caía no erro: ele chama a RPC pelo
PostgREST com as 11 chaves **nomeadas**, o que resolve sem ambiguidade. Ele registrou os
40 leads do período em sombra, com unidade e argumentos corretos. O último deles é a
**Rafaela Gomes** — exatamente o lead da execução 638056 que falhou no n8n.

Teria absorvido a queda inteira se `OBSERVADOR_ESCREVE` incluísse lead. Foi o que motivou
a decisão do Hugo de tirar o lead do n8n e passar de vez para o observador.

### Feito nesta sessão

Branch `fix/observador-assume-leads-emusys`, commit `54a66eeb`.

1. **Migration `20260812133837_upsert_lead_acao_inserted_paridade`** — a função de 11 args
   gravava `v_action := 'created'` onde a antiga gravava `'inserted'`. Esse valor vai para
   `leads_automacao_log.acao`, e `TabAutomacaoLeads.tsx` mapeia `['inserted','lead_inserido']`
   para o badge "Novos": todo lead novo sumiria do filtro. Havia 1.433 `inserted` no log e
   **zero** `created` (a função nasceu ambígua e nunca rodou), então nada dependia do valor
   novo. Corpo lido de `pg_get_functiondef` e reescrito com guarda — sem transcrição manual.
2. **`resolverUnidade` passa a usar `escola_id`** (39=CG, 40=Recreio, 316=Barra), com o nome
   como reserva. Decisão do Hugo: o ID é estável, o nome muda se renomearem a escola.
   **Sem default de unidade** — o n8n manda todo `escola_id` desconhecido para a Barra em
   silêncio; aqui, ID desconhecido vira `ignorado` com status `warn`.
3. Corrigido o comentário do cabeçalho que dizia que matrícula "segue no n8n". Falso: o
   webhook chega ao observador e vira log bruto, mas quem processa é a edge
   `processar-matricula-emusys` (o `workflow_id` no log prova em 100% das ações reais).

**Deploy:** v31, via CLI com `--no-verify-jwt`. Usei CLI em vez do MCP de propósito: o MCP
exigiria transcrever 48.750 caracteres no tool call, e este repo já teve incidente de
mojibake por transcrição (`5a11c150`). Antes de subir, conferi que o código deployado (v30)
era **idêntico** ao `HEAD` — git não estava divergindo de produção.

**Smoke test pós-deploy** (POST sem `Authorization`, `escola_id=316` e **sem** `escola_nome`):
HTTP 200, unidade resolvida como Barra (provando que veio do ID), `p_canal='INSTAGRAM'`
extraído mesmo com o campo vindo como string — formato que o n8n descarta, porque ele exige
`.valor.valor` — e **zero** leads criados (sombra preservada).

### Levantado sobre a paridade n8n → observador

Lido o workflow na fonte (`n8n_get_workflow` mode full), não de memória:

| n8n | Observador |
|---|---|
| `lead_criado` → `upsert_lead` | mesma RPC + `data_nascimento` |
| `lead_editado` → mesma RPC | idem |
| `lead_arquivado` → UPDATE por **interpolação de string** | mesmo critério, **parametrizado** |
| Ramos NocoDB | terminam em nó **sem saída** — já mortos |
| `Gravar Raw Lead` → `leads_automacao_log` | **zero eventos `webhook_lead_raw` em 10 dias** — o nó tem `continueOnFail` e falha calado |

O observador ainda **adiciona** o que o n8n descarta: `agente_comercial`,
`motivo_arquivamento` e etapa do pipeline derivada do estágio (que nunca regride).

### Pendências

- 👁️ **OBSERVAR — `OBSERVADOR_TOKEN` não está definido.** Hoje inofensivo (só grava log),
  mas **assim que a escrita de lead for ligada, o endereço vira porta aberta para injetar
  lead no funil**. Headers do Emusys foram medidos: não manda nada custom, então o caminho é
  `?token=` na URL cadastrada lá. **Sequência sem janela de queda:** (1) Hugo cadastra a URL
  com `?token=SEGREDO` no Emusys — a edge ainda ignora, então nada muda; (2) só então setar
  o secret `OBSERVADOR_TOKEN`; (3) então ligar `OBSERVADOR_ESCREVE`. Inverter derruba o
  webhook em 401 silencioso.
- **DROP da `upsert_lead` de 10 args — pendente de OK do Hugo.** Enquanto não sair, o n8n
  segue quebrado e as campanhas seguem sem criar lead no funil. A de 11 args é superconjunto
  provado linha a linha (fora `data_nascimento`, só mudou `email` para não sobrescrever com
  vazio e dois aliases novos de canal: `FAMILY`→Indicação, `INSTAGRAM`→Instagram).
- **Backfill dos 22 leads** a partir do `payload_bruto` em `automacao_log` — não precisa da
  API do Emusys. Mais as 18 edições que se perderam (o lead existe, mas pode estar com
  etapa/status defasados).
- **Ordem da virada:** ligar o observador **primeiro**, confirmar em evento real, e só então
  desligar o ramo de lead no n8n. O inverso abre janela sem ninguém escrevendo, e o Emusys
  **não reenvia** — foi exatamente assim que os 22 leads se perderam. Com os dois ligados não
  há duplicata: a RPC casa por `emusys_lead_id`+unidade e o segundo vira UPDATE.

### Anotado de passagem (não investigado)

`matricula_finalizacao` tem **41 processadas contra 21 observadas** nos últimos 15 dias, e
`matricula_alterada` 82 contra 73 — enquanto `matricula_nova` (37/37) e
`matricula_trancamento` (5/5) batem. Ou a edge de matrícula é chamada por outra via além do
webhook, ou o observador está perdendo eventos. Não afeta a migração de lead.

## Virada do lead para o observador — preparação concluída (~11h30 BRT)

### Resolvido

- **`main` alinhada com produção.** As migrations já estavam aplicadas e a edge deployada, mas o
  código vivia numa branch — um deploy a partir da `main` teria revertido a correção em silêncio
  (o mesmo padrão do PR #75 em 08/08). Merge feito, push, branch removida.
- **n8n confirmado de volta**: execução `638404` às 14:03 UTC, `success` — a primeira depois do
  drop (13:58). Os leads voltaram a entrar pelo caminho normal.
- **Repositório tornado privado** pelo Hugo. Estava **público** até 12/08 — `config.toml`,
  migrations e nomes de todas as edges estavam acessíveis a qualquer pessoa. (O histórico de
  quando virou público só o dono vê, em `settings/security-log`.)

### `data_nascimento` passa a vir da EXPERIMENTAL

Pergunta do Hugo: dava para pegar o nascimento pelo evento de experimental em vez do de lead?
Dá — e é fonte muito melhor. Medido em 30 dias de `automacao_log`:

| Fonte | eventos | preenchido | |
|---|---|---|---|
| `lead_criado` → `lead.data_nascimento_aluno` | 780 | 36 | **4,6%** |
| `lead_editado` → idem | 1.565 | 351 | 22,4% |
| **experimental** → `aula.data_nascimento_aluno` | 199 | **177** | **88,9%** |

Faz sentido: na criação do lead ninguém pergunta a idade, mas para **agendar** a experimental a
secretaria precisa dela — é o que decide LAMK (até 11) × EMLA (12+) e o professor.
⚠️ O campo fica em `aula.`, não em `lead.` — foi por isso que a primeira medição deu zero.

Implementado no `processarExperimental`: grava `leads.data_nascimento` quando vier e o lead estiver
sem, com `.is('data_nascimento', null)` no WHERE (nunca sobrescreve). O trigger
`trg_calcular_faixa_etaria_lead` deriva a faixa sozinho. Bloco em try/catch — não bloqueia o
registro da experimental. Deployado.

**Por que a faixa importa:** `ModalAgendar.tsx` avisa quando o lead vai para uma turma incompatível
(adulto em turma que parece infantil e vice-versa). Sem faixa, o aviso não dispara.

### Backfill dos 618 — DESCARTADO por decisão do Hugo ("só daqui pra frente")

Há **618 leads ativos com experimental e sem `faixa_etaria`** (não 23, como a migration de ontem
mediu). Só 5 têm experimental **futura**; 352 são de mais de 3 meses. Fontes descartadas:
`emusys_experimentais_raw` **não tem o campo** (0 de 55.100 — o payload dela vem da API `/aulas`,
não do webhook) e o `automacao_log` guarda ~45 dias, rendendo só 7. O resto exigiria varrer a API
do Emusys. Decisão: não fazer; o fluxo novo resolve daqui para frente.

### Token — validado sem arriscar a captação

⚠️ O campo de URL do Emusys **exibe o endereço truncado na tela**. Se o cadastro tivesse cortado o
token, ativar `OBSERVADOR_TOKEN` faria a edge rejeitar 100% dos webhooks — e o Emusys **não
reenvia**. Então, em vez de ativar e torcer, o observador passou a **medir** o token recebido:
`automacao_log.detalhes.token_diagnostico` guarda origem, comprimento e os 12 primeiros hex do
SHA-256. Não grava o valor — o hash já prova identidade (mesma regra do `HEADERS_SENSIVEIS`).

Resultado: um `matricula_renovacao` real da **Barra** às 11:24 chegou com `via=query`, `len=64` e
hash `b9b71666ca74` — **o esperado**. O cadastro não truncou.

### Pendente

- **Falta evento com token de Campo Grande e Recreio.** As duas tiveram eventos hoje, mas todos
  ANTES do cadastro (CG 10:50, Recreio 10:32), então não provam nada. Só setar o secret depois de
  ver o token vindo das três — senão a unidade que ficou com a URL antiga para de ser recebida.
- Ligar `OBSERVADOR_ESCREVE` com `lead_criado,lead_editado,lead_arquivado`; confirmar em evento
  real; só então desligar o ramo de lead no n8n.
- **3 leads de família** fora do backfill (mesmo telefone, mesma unidade): Tiago Cunha/Valentina
  Caminha, Brenda/Pérola Oliveira, Hugo Sobrinho/Thiago. Criar cada um sobrescreveria nome e
  `emusys_lead_id` do lead existente, porque a RPC casa por telefone quando não acha o id.
- Remover o `token_diagnostico` depois que a trava estiver ativa e validada.

## Observador assumiu o lead + o problema do lead-criança (~12h BRT)

### Virada concluída (em sobreposição com o n8n)

Token validado nas **3 unidades** (Barra 11:24, CG 11:46, Recreio 11:48 — todos `len=64`, hash
`b9b71666ca74`, zero divergentes, zero sem token). Com isso:

- `OBSERVADOR_TOKEN` setado; trava validada nos 3 cenários: **401** sem token, **401** com token
  errado, **200** com o correto.
- `OBSERVADOR_ESCREVE` passou a `aula_experimental_criada,aula_experimental_reagendada,aula_experimental_cancelada,lead_criado,lead_editado,lead_arquivado`.

⚠️ **Truque que evitou um estrago:** o CLI não mostra o valor de um secret, só o **digest** sha256.
Antes de sobrescrever o `OBSERVADOR_ESCREVE` eu descobri o valor anterior comparando o digest com
candidatos (`printf '%s' "..." | sha256sum`) — era só os 3 eventos de experimental. Escrever apenas
os eventos de lead teria **desligado a experimental**, que já rodava em produção.

⚠️ O token aparece **em texto puro** na URL registrada nos logs da edge no dashboard. Inerente a
token em query string; o Emusys não manda header custom (medido nos headers recebidos).

**n8n continua ligado de propósito** — é a rede de segurança durante a sobreposição. Sem duplicata:
a RPC casa por `emusys_lead_id`+unidade e o segundo vira UPDATE. Critério objetivo para desligá-lo
(4 queries prontas) em `.claude/memory/virada-lead-observador-em-curso.md`.

### O lead-criança: não é dúvida conceitual, é uma constraint

Pergunta do Hugo ("criança é um lead? é um problema que eu queria resolver há tempo"). A causa:

```sql
CREATE UNIQUE INDEX idx_leads_telefone_unidade_unique ON leads (telefone, unidade_id)
  WHERE telefone IS NOT NULL AND arquivado = false
```

O banco **proíbe** dois leads ativos com o mesmo telefone na mesma unidade. Por isso há **zero**
telefones compartilhados em `leads` — não por ausência de famílias, mas porque o modelo não deixa.
Quando a família traz o 2º filho, o lead dele não entra ou **sobrescreve o do irmão**.

| Medição (12/08) | |
|---|---|
| Grupos de irmãos entre alunos matriculados | **113** |
| Crianças envolvidas | **244** (15% da base) |
| Maior família | 4 |
| Eventos de lead com `nome_responsavel_aluno` | 314 em 45 dias (13,4%) |
| Desses, responsável ≠ aluno (= criança) | **304 (97%)** |

**A resposta já existe do lado do aluno:** `alunos` tem `responsavel_nome`, `responsavel_telefone`
e `responsavel_parentesco` (1.278 de 1.635 preenchidos). O aluno é a criança, o responsável é
atributo dela. `leads` **não tem nenhum desses campos**, e o Emusys **manda** o responsável no
payload — jogamos fora. A mesma pessoa é modelada de um jeito como lead e de outro como aluno.

**Caminho proposto (frente própria):** `leads` espelha `alunos`, e a unicidade passa de
`(telefone, unidade)` para `(telefone, unidade, nome normalizado)`.
⚠️ **Não feito de passagem de propósito:** mexe no índice único de `leads` e na `upsert_lead` — a
mesma função cujo overload causou as 21h de webhook parado nesta mesma manhã. Registrado em
`.claude/memory/leads-familia-irmaos-modelagem.md`.

## Virada concluída: observador é o único gravando lead (~13h40 BRT)

O Hugo **removeu a conexão `Webhook1 → tem numero?2`** no n8n (13:31 BRT). Não desabilitou nós nem
o workflow: cortou a aresta, e todo o ramo que levava aos `Upsert Lead` ficou órfão. Mais limpo
que desabilitar nó a nó, e reversível — basta religar.

**Verificado com evento real:** lead **Eliane** (id 12821, `emusys_lead_id` 14783, Campo Grande)
criado às 13:36:23 **só pelo observador** (`acao='processado'`). A execução do n8n no mesmo
segundo (`639267`) durou **85ms** contra os ~300-1.800ms anteriores — assinatura de que o ramo de
gravação saiu do caminho.

### O n8n virou AUDITOR (ideia do Hugo)

Ele continua recebendo o webhook e registrando execução. **Cada execução = um webhook que o Emusys
entregou.** Cruzando com o `automacao_log` no período de sobreposição: **15 execuções × 15 eventos,
casando segundo a segundo, zero perda.**

Isso cobre o ponto cego que o desligamento criaria: o observador grava o payload bruto ANTES de
processar (por isso o backfill de 21 leads funcionou hoje), **mas não grava nada em dois casos** —
rejeição por token (o bloco `(0) TOKEN` sai antes do `(1) LOG BRUTO`, de propósito, para não virar
vetor de flood) e edge fora do ar. Nesses, lead e log se perdem juntos e o Emusys não reenvia.
Com o n8n ligado, a execução dele denuncia o buraco.

⚠️ **Não desligar o workflow do n8n** por causa disso.
⚠️ O nó `Gravar Raw Lead` está falhando calado (zero `webhook_lead_raw` há semanas, tem
`continueOnFail`) — consertá-lo devolveria a **segunda cópia do payload**. Hoje o auditor prova que
o evento existiu, mas não guarda o conteúdo.

Queries da auditoria em `.claude/memory/virada-lead-observador-em-curso.md`.

### Ainda em observação

`lead_arquivado` **nunca foi visto** passando pelo observador (18 eventos em 45 dias, o mais raro)
e é o único dos três que faz UPDATE destrutivo. É o que olhar nos próximos dias.

## Sync de matrículas: fase B parada desde 11/08 (regressão do escopo operacional)

**Origem:** o Arthur comparou a aba Contratos com a tela "Renovação de Matrículas" do Emusys
(Barra, 30 dias). Emusys 10 por última aula e 8 por última fatura; nós 9 e 7.

**Divergência 1 — Gabriela da Costa (Teclado).** Emusys diz última aula 18/08, nossa jornada
diz 11/08, e o hook corta `dias >= 0` — sumiu da lista. A aula de 18/08 **existe** em
`aulas_emusys` (nº 41, `reagendada=true`), capturada pela grade de 15 min. Quem não sabia era
a jornada.

**Causa raiz (provada em 3 níveis).** O commit `f5edbea2` (11/08 18:11 BRT) criou o escopo
`operacional` e a migration `20260811210319` (21:03 UTC) reagendou os 3 crons com
`&escopo=operacional`. O `return` do bloco operacional foi **inserido** no meio do handler —
o resto do pipeline não saiu do lugar, ficou inalcançável. Confirmado no repo, no **código
deployado** (baixado e lido) e por teste ao vivo: sync da Barra respondeu
`estados_atual.gravadas: 274` e `jornadas: {atualizadas: 0, puladas: 0, erros: 0}`.

**O que parou junto** (fase B, 7 blocos): jornada, decisões canônicas, régua de classificação,
`lead_id`, reconciliação por aluno (sugestões + fila + **conversão de renovação pendente em
não-renovação**, que escreve em `movimentacoes_admin`), limpeza de alertas e varredura reversa.
Evidência: `matriculas_divergencias` sem item novo desde 11/08 21:00 (CG).

⚠️ **Não é "quebrou o que funcionava".** O motivo da mudança era real e está na própria
migration: a varredura completa (2.500+ contratos) estourava o idle timeout de 150s e deixava
foto velha publicada. O `return` consertou isso e levou junto 7 blocos que **não chamam a API**
— trabalham sobre `porId`, já carregado.

**Fix proposto (não aplicado — aguardando decisão do Hugo):** mover só o bloco da jornada para
antes do `return`. Verificado: (a) as 4 dependências (`depara`, `profMapJornada`,
`alunoIdPorMatriculaEmusys/AlunoEmusys`) já são montadas antes, porque `buildEstadoAtualRows`
também precisa delas; (b) **nenhum bloco posterior lê a jornada** — a única ocorrência de
`aluno_jornada_matricula_disciplina` na edge é o próprio upsert; (c) tem que ser **movido**, não
copiado, senão roda 2× no escopo completo. Duração atual do operacional para comparar depois:
Barra 13,5s, Recreio ~23s, CG ~27s (teto 150s).

⚠️ A jornada alimenta `vw_jornada_aluno_atual` (20+ consumidores). Restaurar o fluxo **move
números** — para o valor correto, sem mudar fórmula nenhuma. A Taxa de Renovação não vem daí.

**Feito hoje:** só comentários na edge (56 linhas, zero linha de código) — mapa do pipeline
FASE A/FASE B no cabeçalho, aviso no `return` do operacional listando o que fica de fora, e
rótulos B1-B6 nos blocos. Era o que faltava para a armadilha ser visível.

### Achados colaterais (não tratados)

1. **Cada cron dispara 3 execuções simultâneas** da mesma unidade (Barra hoje: 02:40:00.58,
   02:40:01.52, 02:40:06.99). Triplica o consumo da API; o CG levou `429` em 11/08 21:10.
2. **Eduardo Chagas Mendonça (Barra):** venc. da última fatura 05/09 no Emusys, 05/08 na nossa
   derivação (`data_primeira_fatura + nr_faturas-1`). `emusys_faturas` só tem 2 linhas dele, não
   dá para fechar a causa — precisa de `GET /matriculas` ao vivo. Contraria a validação
   "7 de 7 no dia exato" de 28/07.
3. **`CLAUDE.md` diz que o cron roda às 02h BRT.** É 02h **UTC** = 23h BRT.
4. `emusys_matriculas_sync_execucoes` só existe desde 11/08 21:10 — não dá para auditar antes.
