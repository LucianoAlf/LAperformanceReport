# 2026-07-31

## Carteira do Professor vazia para perfil `unidade` + cutover do MRR para o V3 (~15h BRT)

Sintoma reportado: modal da Carteira do professor Gabriel Antony abria com "Alunos por Curso"
vazio, LAMK/EMLA 0% e MRR/ticket R$ 0 — com 43 alunos ativos no card.

### Causa raiz: o RBAC granular nunca foi ligado

`get_jornada_professor` (fonte do detalhe da carteira) gateava por
`fn_usuario_atual_tem_permissao('professores.editar', ...)`. Essa função consulta
`usuario_perfis` — que está **VAZIA (0 linhas)**. O RBAC criado em 04/02/2026
(migrations `20260204110727..111011`) populou `permissoes`, `perfis` e
`perfil_permissoes`, mas **nunca teve seed de `usuario_perfis`**. O último elo ficou solto.

Como `usuario_tem_permissao` tem um bypass no topo (`usuarios.perfil = 'admin' → TRUE`), só
admin passava. Os 20 usuários de perfil `unidade`/`professor` recebiam **lista vazia, sem erro
e sem 403** — falha silenciosa. Como quem testa costuma ser admin, o bug ficou invisível.

Medição de quem realmente governa o acesso hoje:

| Camada | legado (`usuarios.perfil`) | RBAC novo (`usuario_perfis`) |
|---|---|---|
| RLS policies | 59 de 616 | **1** de 616 |
| Front (ocorrências/arquivos) | 227 em 58 | **6** em 3 |

O RBAC é uma ilha. Mas **9 funções do banco já dependem dele** — e são as canônicas recentes:
`get_jornada_professor`, `fn_aluno_entra_base_ativa_v131`,
`buscar_alunos_ativos_atuais_canonicos`, `get_alunos_ativos_atuais_canonicos`,
`get_trancamentos_atuais_canonicos`, `get_trancamentos_periodo_canonicos`,
`fn_pode_ler_aluno_pedagogico`, `fn_health_score_professor_v3_ator_gerenciador`,
`pode_sincronizar_professor_disciplinas_emusys_v1`.

**~~Pendência aberta~~ → RESOLVIDA no fim do dia.** As outras 8 devolviam vazio para todo
não-admin. `fn_aluno_entra_base_ativa_v131` define quem é aluno ativo e alimenta KPI — estava
cortando **todas** as linhas por perfil, sem aviso. Corrigido ao popular `usuario_perfis`
(ver seção da migração do RBAC abaixo); nenhum remendo por função foi necessário.

### Fix aplicado (migrations)

1. `fix_get_jornada_professor_permissao_carteira` — troca `professores.editar` por
   `professores.carteira` (a permissão dedicada a essa tela). Aplicada com diagnóstico ainda
   não confirmado; **sozinha não resolvia nada**, já que ninguém tem nenhuma das duas.
2. `fix_get_jornada_professor_aceitar_perfil_legado` — soma o caminho legado ao gate, sem
   remover o RBAC (que segue valendo quando `usuario_perfis` for populada):
   `u.perfil='admin' OR (u.perfil='unidade' AND u.unidade_id = j.unidade_id)`.

Validado por simulação de JWT, **sem vazamento entre unidades**:

| | prof 6 (Barra) | prof 15 (CG) | prof 32 (Recreio) |
|---|---|---|---|
| hugobarra (`unidade`/Barra) | 42 ✅ | 0 ✅ | 0 ✅ |
| hugo@gmail (`admin`) | 42 | 51 | 52 |

### Segundo bug: MRR/ticket zerados — EM ABERTO, tentativa de fix revertida

Com a carteira já visível, MRR e ticket seguem R$ 0. O card é montado de **três fontes**:
alunos/média-turma/permanência do Health Score V3, MRR de `get_carteira_professores`, e
ticket = `mrr ÷ alunos_v3` (numerador de uma fonte, denominador de outra).

**Tentativa (revertida antes do push):** migrar MRR/ticket para `kpis.mrr_carteira` /
`kpis.ticket_medio` do V3 e parar de chamar `get_carteira_professores`. A decisão se apoiou
no doc de homologação de 22/07, que descrevia a RPC como "apenas enriquecimento financeiro".

**Por que foi revertido:** ao integrar com o remoto apareceram 11 commits do ciclo
"Emusys matrículas v1.3.1" (30/07) — dois deles sobre essa mesma RPC:

- `20260730131747_carteira_professores_estado_operacional_v131` — reescreve a RPC para usar
  `fn_aluno_entra_base_ativa_v131`. Comentário na migration: *"trancadas, concluidas,
  interrompidas e estados ambiguos nao entram na base viva"*.
- `20260730132222_hardening_carteira_professores_v131` — fixa `search_path`, revoga
  `public`/`anon`.

Ou seja: **a RPC não é legado, foi modernizada ontem** para ser a carteira operacional
canônica. O doc de 22/07 que eu li é anterior a esse trabalho.

⚠️ **E a troca provavelmente pioraria o número.** `get_kpis_professor_periodo_canonico_v3`
**não usa** `fn_aluno_entra_base_ativa_v131` (verificado). A diferença Gabriel 18.595 (RPC)
vs 24.888 (V3) é possivelmente o V3 contando matrícula trancada/concluída como receita viva
— não "recorte de competência", como cheguei a afirmar.

**RESOLVIDO (~18h BRT).** Causa: `fn_aluno_entra_base_ativa_v131` (que a RPC passou a usar em
30/07) exige `alunos.ver` via RBAC. Com `usuario_perfis` vazia → `false` para todo aluno →
CTE `alunos_base` vazia → RPC devolve zero linhas → `Number(row?.mrr_total ?? 0)` = 0.

⚠️ **Meus testes via MCP mascararam isso por horas.** A função tem um escape
`session_user = 'postgres'`. O `set local role authenticated` troca o `current_user`, **não** o
`session_user` — que na conexão do MCP é `postgres`. Então toda vez que "confirmei que funciona
com o JWT do usuário", a função respondia pelo atalho de superusuário, não pelo caminho do
browser. **Lição: `set local role` não reproduz o browser em função que checa `session_user`.**

### Migração do RBAC — `usuario_perfis` populada pela 1ª vez (~18h BRT)

Decisão com o Alf (doc `rbac-la-report-decisao.md` + PR #16): **manter o RBAC**, porque o legado
não expressa "pessoa × N unidades × ação" — `usuarios.unidade_id` é coluna singular.

Descoberta que viabilizou a migração: as **59 RLS policies não consultam `usuarios` direto** —
todas chamam `is_admin()` e `get_user_unidade_id()`. Migrar significa reescrever **2 funções**,
não 59 policies. E `Admin` e `Gerente` têm as **mesmas 47 permissões**; a diferença é só o
escopo do vínculo — o mesmo eixo do legado, o que torna o espelho fiel.

Piloto com `hugo_teste_barra` (Gerente @ Barra) validou no browser: **MRR voltou** (R$ 18.595
no Gabriel — o valor da RPC v1.3.1, confirmando que a troca de fonte que quase subi teria
inflado para 24.888 contando trancadas/concluídas).

Aplicado — 32 vínculos / 28 pessoas:

| Perfil | Pessoas | Escopo |
|---|---|---|
| Admin | 9 | global (`unidade_id NULL`) |
| Gerente | 17 | própria unidade |
| **Sucesso do Aluno** (novo) | 2 | Fabi e Jessica × 3 unidades |

**Perfil `Sucesso do Aluno`** criado (migration `20260731175307`, 24 permissões): departamento
novo, complementa o `Sucesso do Aluno - Evasao` do PR #16 (que governa a *ação* sensível via
helper estrito). Fora dele: `usuarios.*`, `permissoes.gerenciar`, `perfis.gerenciar`,
`configuracoes.gerenciar`, `alunos.excluir`, `auditoria.ver`. Decisão do Alf: elas veem tudo e
mandam mensagem nas 3, mas **não são admin**.

⚠️ **Elas AINDA são admin.** `usuarios.perfil` continua `'admin'` — obrigatório nesta etapa,
porque as 59 policies ainda leem o legado; trocar agora as deixaria sem acesso
(`unidade_id NULL` não casa com nada). A restrição do perfil novo só vale no passo 3.

**Sequência combinada:** (1) popular `usuario_perfis` espelhando o legado ✅; (2)
`get_user_unidade_id` vira plural e as policies mudam de `=` para `IN` ✅; (3) trocar
`usuarios.perfil` delas — **depende do conserto do front, ver bloqueio abaixo**.

📌 **Correção de uma afirmação minha:** eu disse que tirar o admin delas exigiria reescrever
`is_admin()`. **Não exige.** Basta trocar `usuarios.perfil` para `'unidade'` e colocá-las no
piloto: `is_admin()` passa a responder `false` (correto), e o acesso vem das 3 unidades do
RBAC. Reescrever `is_admin()` é limpeza de dívida, não pré-requisito.

**Pendências:** Matheus Felipe (id 32, único `professor`) **não migrado** — o perfil Professor
tem só 6 permissões e não sabemos se cobrem o uso dele; segue com as 8 funções negando.
Migrations `20260731163353` e `20260731164500` (de outra sessão) estão no banco **sem arquivo
no repo**.

### Passo 2: policies aceitam N unidades por pessoa (~18h30 BRT)

`get_user_unidade_ids()` (plural, `setof uuid`) + tabela `rbac_piloto_usuarios`. As **40
policies** que faziam `unidade_id = get_user_unidade_id()` passaram a `IN (select
get_user_unidade_ids())` — comandos gerados a partir do `pg_policies`, sem digitação manual.

**Equivalência por construção:** fora da lista piloto a função devolve a única unidade do
legado, e `x IN (um valor)` ≡ `x = valor`. Snapshot antes/depois: **319 medições
(29 usuários × 11 tabelas), 0 divergências**.

⚠️ O primeiro script gerado estava **errado** — trocava a função mas mantinha o `=`, gerando
`= (select ...)`, que estouraria em runtime no primeiro usuário com 2 unidades. Pego na
conferência antes de aplicar.

**Piloto:** `hugo_teste_barra` (perfil `unidade`, **não** admin) + 3 vínculos → passou a ver
1.606 alunos das 3 unidades no banco (372 Barra / 705 CG / 529 Recreio). É exatamente o
cenário da Fabi, provado.

### ⛔ Bloqueio descoberto: o front trava na unidade

Na tela, o mesmo usuário **continua vendo só a Barra**. O banco libera 3, o front pede 1.

`useUnidadeFiltro.ts:36` → `filtroAtivo = isAdmin ? unidadeSelecionada : unidadeId`. Não sendo
admin, o filtro é fixado em `usuarios.unidade_id` (coluna legada, singular) e as telas mandam
`p_unidade_id = Barra` em toda consulta. Os vínculos RBAC são ignorados pela interface.
`AppHeader.tsx:65` só renderiza o seletor de unidade `if (isAdmin)`.

⚠️ **A Fabi quebraria diferente:** `unidade_id` dela é NULL → filtro nulo → provavelmente vê
as 3 somadas, sem conseguir separar. Dois modos de falha distintos, nenhum desejado.

**Conserto (3 arquivos, não as 227 checagens):** `AuthContext` expõe a lista de unidades;
`useUnidadeFiltro` troca `canChangeUnidade = isAdmin` por "tem mais de uma unidade";
`AppHeader` mostra o dropdown por quantidade de unidades. Admin cai naturalmente no caso
"mais de uma" e não muda. **Não feito — próxima sessão.**

### ⚠️ Risco: usuário novo nasce fora do RBAC

`GerenciarUsuarios.tsx` grava **só** `usuarios.perfil` — não cria vínculo em `usuario_perfis`.
Enquanto os dois sistemas coexistirem, **todo usuário criado pela tela nasce quebrado**: as 8
funções canônicas negam tudo pra ele, silenciosamente (mesmo sintoma de hoje: Carteira vazia,
MRR R$ 0). Conserto: criar o vínculo junto na tela, ou um trigger em `usuarios`.

### Plano de migração completa (levantado, não executado)

| # | Etapa | Tamanho |
|---|---|---|
| 1 | Front: seletor multi-unidade (`AuthContext`, `useUnidadeFiltro`, `AppHeader`) | 3 arquivos |
| 2 | Sincronizar criação de usuário (tela ou trigger) | 1 arquivo / 1 trigger |
| 3 | `is_admin()` passa a ler o RBAC — as 101 policies herdam sem edição | 1 função |
| 4 | ~15 funções que leem `usuarios.perfil` direto (não passam por `is_admin()`) | 15 funções |
| 5 | 227 checagens no front → `hasPermission()` | 58 arquivos |
| 6 | Remover `usuarios.perfil` | último |

**Para o objetivo da Fabi bastam 1 + 3-parcial.** As demais são limpeza de dívida.

**Riscos medidos.** *Alto:* (a) falha silenciosa — permissão negada devolve lista vazia, não
erro; foi assim que 8 funções ficaram quebradas por meses. Só se detecta com alguém testando
**como não-admin**. (b) as 227 checagens do front não têm teste automatizado algum.
*Médio:* (c) **migrar não reduz privilégio** — `Gerente` tem as mesmas 47 permissões do
`Admin` (incl. `usuarios.criar`, `permissoes.gerenciar`); os 17 de unidade viram "admins da
própria unidade". Fiel ao de hoje, mas quem espera aperto de segurança não vai ter.
(d) escrita (INSERT/UPDATE) não foi testada — o snapshot mediu leitura.
*Baixo:* relatórios/edges (rodam em `service_role`, ignoram RLS), views (1 de 89), e as 101
policies (herdam de `is_admin()`).

**Não mensurável hoje:** quem faz o quê. `cargo` preenchido em 6/29 e `auditoria_acesso` com
3 linhas, última de fevereiro. Refinar perfis (Hunter/Farmer/Visualizador) seria chute; o
espelho do legado é seguro justamente por não decidir nada.

### Estado ao fim do dia

- ✅ Carteira do professor destravada para perfil `unidade` (em produção)
- ✅ MRR/ticket voltaram (causa: `fn_aluno_entra_base_ativa_v131` + RBAC vazio)
- ✅ RBAC populado: 34 vínculos; 40 policies aceitam N unidades; 0 divergências
- ⏸️ Fabi e Jessica **continuam admin** — falta o conserto do front antes de trocar o perfil
- ⏸️ Matheus Felipe não migrado
- ⏸️ `is_admin()` e as 227 checagens do front: intocados, legado ainda governa
- ⚠️ `hugo_teste_barra` fica com 3 unidades no banco e 1 na tela (é o piloto do próximo passo).
  Para zerar: `delete from rbac_piloto_usuarios where usuario_id = 27;`
- ⚠️ Os 34 vínculos de `usuario_perfis` são **dados**, não migration. Restore de backup
  anterior a hoje faz as 8 funções voltarem a quebrar.

### Lições

1. Escrever RPC nova gateada por `fn_usuario_atual_tem_permissao` hoje = **função que só
   funciona para admin**, silenciosamente. Enquanto `usuario_perfis` estiver vazia, todo gate
   novo precisa aceitar o perfil legado — ou o RBAC precisa ser populado de fato.
2. **`git fetch` antes de decidir que algo é legado.** A conclusão "essa RPC é resquício de um
   cutover pela metade" veio de um doc de 22/07 e do `main` local, que estava 11 commits atrás.
   O trabalho de 30/07 dizia o contrário. Doc desatualizado + branch desatualizado produziram
   um diagnóstico confiante e errado.

## Ticket medio da carteira do professor esta sendo diluido (~19h BRT) — INVESTIGACAO, nada alterado

Pedido do Luciano: na aba Carteira (modulo Professores), alunos de projeto de banda
e bolsistas parciais estariam derrubando o ticket medio. Exemplo dado: Willian de
Andrade (Barra).

### Confirmado — e nao e o que parece

O bug nao esta na RPC, esta no frontend. `get_carteira_professores` calcula o ticket
corretamente: numerador e denominador so contam quem tem `valor_parcela > 0`. Para o
Willian ela devolve **R$ 441,92**.

`TabCarteiraProfessores.tsx:242` **descarta** esse valor e recalcula:

```ts
ticket_medio: totalAlunos > 0 ? mrrTotal / totalAlunos : 0
```

`mrrTotal` vem da RPC (soma **so de quem paga**), mas `totalAlunos` vem do snapshot
health score V3 (`numero_alunos`), que e o **headcount inteiro, incluindo os de valor
zero**. Numerador e denominador de universos diferentes. Willian: 12.815,60 / 41 =
**R$ 312,58** na tela, contra 441,92 da RPC. Os 10 alunos de projeto de banda dele
(Power Kids / Minha Banda Para Sempre, todos `valor_parcela = 0`) entram no
denominador sem entrar no numerador.

### Alcance medido — 24 professores

| Professor | Alunos | Zerados | Ticket real | Na tela | Perda |
|---|---|---|---|---|---|
| Ramon Pina Morais | 52 | 39 | 513,33 | 108,59 | **404,74** |
| Marcos (Marquinhos) | 19 | 10 | 470,54 | 198,12 | 272,42 |
| Rodrigo Pinheiro | 37 | 14 | 431,68 | 233,34 | 198,34 |
| Renan Amorim | 27 | 9 | 424,89 | 267,52 | 157,37 |
| Lucas da Silva Guimaraes | 44 | 16 | 424,79 | 270,32 | 154,47 |
| **Willian de Andrade** | 41 | 10 | **441,92** | **312,58** | **129,34** |
| ... mais 18 professores | | | | | 15 a 99 |

Alan Samico aparece com ticket 0 legitimamente: os 5 alunos dele sao todos de banda.

O mesmo vale para o card agregado da aba (`TabCarteiraProfessores.tsx:334`), que soma
os MRR e divide pelo total de alunos.

### CORRECAO — a marcacao de bolsista/banda EXISTE (achado do Hugo)

Escrevi acima que nao havia marcacao de bolsista por aluno. **Errado.** Eu tinha
procurado por colunas com "bolsa" no nome; a marcacao esta em `alunos.tipo_matricula_id`
-> tabela `tipos_matricula`, que tem exatamente os flags do caso:

| id | tipo | entra_ticket_medio | conta_como_pagante | alunos ativos |
|---|---|---|---|---|
| 1 | Regular | true | true | 974 |
| 2 | Segundo Curso | true | true | 54 |
| 3 | Bolsista Integral | **false** | false | 40 |
| 4 | Bolsista Parcial | **false** | false | 16 |
| 5 | Matricula em Banda | **false** | false | 102 |
| 6 | Transferencia Interna | true | true | 1 |

Esta preenchida: **zero alunos ativos sem tipo**. E e o criterio canonico da casa —
9 funcoes usam `entra_ticket_medio`, incluindo `get_kpis_alunos_financeiro_vivo_canonico`
(o ticket do modulo Alunos) e `snapshot_dados_mensais`.

**`get_carteira_professores` e a excecao: nao le `tipo_matricula_id` em lugar nenhum.**
Ela aproxima "quem entra no ticket" por `valor_parcela > 0`, o que erra nos dois sentidos:

- **Bolsista parcial pagante entra no ticket e nao deveria.** Sao 13 dos 16 parciais
  (valor > 0). O Luciano estava certo nessa parte tambem — so nao e "derrubando por
  serem baratos", e por estarem sendo contados quando o criterio canonico diz que nao.
- **Bolsista integral em curso regular fica de fora do denominador por acidente** (paga
  zero), e nao por regra. Coincide, mas por motivo errado.

### Ticket pelo criterio canonico (`entra_ticket_medio`)

| Professor | Alunos | Fora do ticket | Na tela | Canonico |
|---|---|---|---|---|
| Ramon Pina Morais | 52 | 39 | 108,59 | **434,36** |
| Marquinhos Saturnino | 19 | 10 | 198,12 | 418,26 |
| Lucas da Silva Guimaraes | 44 | 16 | 270,32 | 424,79 |
| Rodrigo Pinheiro | 37 | 14 | 233,34 | 375,37 |
| Renan Amorim | 27 | 8 | 267,52 | 380,16 |
| **Willian de Andrade** | 41 | 10 | **312,58** | **413,41** |
| Kaio Felipe | 47 | 9 | 311,69 | 381,91 |
| Rafael (Akeem) | 51 | 7 | 377,50 | 433,18 |

Usar `entra_ticket_medio` resolve os tres problemas de uma vez (banda, bolsista e a
assimetria do segundo curso descrita abaixo), **e** alinha a carteira ao resto do
sistema. Nao ha decisao de negocio nova a tomar: a regra ja esta escrita na tabela.

### Assimetria adicional na propria RPC (menor, mas existe)

No `ticket_medio` da `get_carteira_professores`, o numerador soma **todos** os
`valor_parcela > 0` (inclusive segundo curso) mas o denominador conta so
`not is_segundo_curso`. Isso **infla** o ticket. Willian tem 2 segundos cursos: divide
12.815,60 por 29 em vez de 31, dando 441,92 onde a media simples dos pagantes seria
413,41. Vale decidir qual e o criterio canonico antes de consertar o front — senao
troca-se uma diluicao por uma inflacao.

### Pendente de decisao (nada foi alterado)

1. Trocar o criterio da carteira de `valor_parcela > 0` para `tipos_matricula.entra_ticket_medio`,
   em dois lugares: dentro de `get_carteira_professores` e em `TabCarteiraProfessores.tsx:242`
   (que precisa parar de recalcular e passar a usar o `ticket_medio` da RPC).
2. Unica decisao aberta: o card "alunos ativos" continua mostrando o headcount inteiro
   (41 para o Willian, incluindo banda) ou passa a mostrar so quem entra no ticket?
   Sao numeros com significados diferentes — provavelmente vale manter o headcount no
   card e so consertar o ticket.

### Varredura: nao e um lugar so — sao 4 calculos de ticket de professor, todos diferentes

A regra escrita confirma o criterio (METRICAS.md L82-86): ticket = `entra_financeiro_ativo`
**E** `tipo_matricula.entra_ticket_medio = true` **E** `valor_parcela > 0`, dedup por
pessoa canonica. A carteira do professor nao cumpre **nenhuma** das tres.

E o mesmo KPI tem 4 implementacoes divergentes:

| Fonte | Base de alunos | Banda | Denominador do ticket |
|---|---|---|---|
| `get_carteira_professores` | `fn_aluno_entra_base_ativa_v131` | **entra** | pagantes, exceto 2o curso (numerador inclui) |
| `maria_lareport_roi_professores_base_v2` | `status in ('ativo','trancado')` | sai | pagantes |
| `get_kpis_professor_periodo_canonico_v2` | — | nao trata | nao usa o flag |
| `TabCarteiraProfessores.tsx:242` | headcount V3 | entra | **headcount inteiro** |

Nenhuma das quatro le `entra_ticket_medio`.

**Divergencia viva medida — Willian de Andrade, mesmo dia:**
- Tela (aba Carteira): **R$ 312,58**
- Relatorio da Maria (`roi_professores_base_v2`): **R$ 413,41** (410,73 Barra + 415,91 Recreio)
- Canonico por `entra_ticket_medio`: **R$ 413,41**

Ou seja, o relatorio que a IA gera ja esta ~certo e a tela esta R$ 100 abaixo. Se
alguem cruzar os dois, os numeros nao batem.

**A Maria conta trancado, a carteira nao.** `roi_professores_base_v2` usa
`status in ('ativo','trancado')`; sao 27 alunos trancados com professor, dos quais 26
pagam e apenas 5 entram na base canonica v131. Fora do Willian isso vira divergencia
entre as duas telas.

### O problema maior, ja registrado e ainda nao resolvido

METRICAS.md item 7 (Clayton, 07/07): o numerador do ticket **nao deveria sair de
`alunos.valor_parcela`** (campo cadastral estatico) e sim da **fatura da competencia**
(`valor_pago` se paga; devido atualizado se aberta). A regra foi validada pelo Alf e
os 4 calculos listados la ainda nao foram migrados. **A carteira do professor e uma 5a
instancia do mesmo problema.**

Consequencia pratica: consertar o flag alinha a carteira ao resto do LA Report, mas
**nao** faz o numero bater com o Financeiro do Emusys. Sao dois consertos distintos.

### Contradicao a resolver antes de codar: segundo curso

METRICAS.md L77 diz que segundo curso e **excluido** do ticket medio. Mas
`tipos_matricula` marca Segundo Curso com `entra_ticket_medio = true`. Nao e erro: o
flag deixa entrar e a **dedup por pessoa** e que remove a linha extra.

So que na carteira **por professor** a dedup por pessoa nao se aplica do mesmo jeito —
se o aluno faz 2 cursos com 2 professores diferentes, ele e carteira dos dois. Copiar
"dedup por pessoa" para ca faria um dos professores perder o aluno. E preciso decidir o
criterio proprio da carteira (provavelmente: sem dedup, cada matricula conta para o seu
professor), e a `get_carteira_professores` hoje faz isso pela metade — tira o 2o curso
do denominador mas deixa no numerador, o que **infla**.

### CORRIGIDO — escopo: so a aba Carteira (decisao do Hugo)

Aplicado o conserto do ticket da aba Carteira. Maria (`roi_professores_base_v2`) e o
numerador via fatura ficaram **fora** deste escopo, de proposito.

**Migration `20260731203000_carteira_professores_ticket_por_tipo_matricula.sql`**
(DROP + CREATE — a assinatura ganhou colunas; unico chamador e a propria aba):
- `alunos_base` passa a fazer `left join tipos_matricula` e expor
  `entra_ticket = entra_ticket_medio AND valor_parcela > 0`.
- `ticket_medio` virou simetrico (mesmo universo nos dois lados), o que de quebra
  eliminou a inflacao do segundo curso.
- 2 colunas novas: `alunos_ticket` e `mrr_ticket` — sem elas o card agregado nao teria
  como somar, ja que o numerador do ticket deixou de ser igual ao MRR.
- `mrr_total` **inalterado** de proposito: bolsista parcial paga, e receita real.
  Sai do ticket, nao do faturamento. Verificado: R$ 415.941,12 antes e depois.

**`TabCarteiraProfessores.tsx`**: linha 242 usa `row.ticket_medio` em vez de recalcular;
o card agregado soma `mrr_ticket`/`alunos_ticket` em vez de MRR/headcount.

**Verificado em producao apos o deploy da RPC:**

| | Antes | Depois |
|---|---|---|
| Willian de Andrade | 312,58 | **413,41** |
| Ramon Pina | 108,59 | **434,36** |
| Kaio Felipe | 311,69 | **381,91** |
| Card agregado | 350,41 | **402,24** |
| MRR total | 415.941,12 | 415.941,12 (inalterado) |
| Alan Samico | 0 | 0 (correto — os 5 alunos dele sao todos de banda) |

`tsc --noEmit` limpo em `src/` (os erros de `scripts/importar_historico_ltv.js` sao
pre-existentes e nao relacionados) e `vite build` OK.

**Continua em aberto (nao mexido):**
1. `maria_lareport_roi_professores_base_v2` — nao le o flag e conta trancado
   (`status in ('ativo','trancado')`). Vai divergir da aba nos professores com trancado.
2. METRICAS.md item 7: numerador do ticket deveria vir da fatura da competencia, nao de
   `alunos.valor_parcela`. Este conserto alinha a carteira ao LA Report, **nao** ao
   Financeiro do Emusys.
3. `get_kpis_professor_periodo_canonico_v2` tambem nao usa o flag.

**Avisar o Luciano:** o ticket vai **subir** em 24 professores na aba. E mudanca
esperada, nao regressao.

### Correcao minha: o MRR canonico exclui bolsista parcial (eu tinha afirmado o contrario)

O Hugo perguntou se o MRR estava certo. Ao buscar a regra escrita, achei que a afirmacao
que eu tinha feito — "MRR e receita real e inclui bolsista parcial; ele sai do ticket,
nao do faturamento" — **contradiz a regra da casa**. Eu tinha escrito isso na migration
e no METRICAS.md.

`get_kpis_alunos_financeiro_vivo_canonico` define MRR com o **mesmo
`entra_ticket_medio`** do ticket. MRR e o numerador do ticket, nao um numero a parte.
Bolsista parcial paga e mesmo assim nao entra.

| | Valor |
|---|---|
| MRR canonico | R$ 412.297,62 |
| `mrr_total` da aba | R$ 415.941,12 |
| `mrr_ticket` (coluna criada hoje) | R$ 413.507,62 |

Os R$ 2.433,50 entre `mrr_total` e `mrr_ticket` sao exatamente os bolsistas parciais
pagantes. Ou seja **a coluna `mrr_ticket` ja e o MRR canonico** (o resto da diferenca
para 412.297,62 e dedup por pessoa e data de corte) — e o `mrr_total` e que esta fora
da regra. Nao precisava de duas colunas.

METRICAS.md corrigido: a formula do MRR virou secao propria e o aviso invertido foi
reescrito como pendencia explicita.

**Nao alterado (aguarda decisao):** alinhar `get_carteira_professores.mrr_total` ao
`entra_ticket_medio`. Derruba o MRR da aba em R$ 2.433,50 e torna `mrr_ticket`
redundante.

### Achado de brinde: METRICAS.md se contradiz sobre segundo curso

L77 diz que segundo curso e "excluido do ticket medio (dedup por pessoa)". Mas no codigo
a dedup **soma** os cursos da pessoa no numerador e conta a pessoa **uma vez** no
denominador — entao segundo curso **eleva** o ticket. E o que o proprio `tipos_matricula`
descreve ("eleva ticket medio, conta como 1 aluno"). Registrado no METRICAS.md como
contradicao em aberto; qual das duas e a intencao nao foi confirmado com ninguem.

### Qualidade de cadastro que apareceu na auditoria do MRR (nao corrigido)

| Caso | Alunos | No MRR |
|---|---|---|
| status `trancado`/`inativo` somando mensalidade | 6 | R$ 2.415,40 |
| `status_pagamento = 'sem_parcela'` mas `valor_parcela > 0` | 15 | R$ 5.760,60 |
| inadimplentes (discutivel, nao erro) | 23 | R$ 8.928,24 |

Os 6 primeiros tem `data_saida` nula e estao marcados `em_dia`. Entre eles,
**Elis Tineli Gomes Cotta aparece duas vezes** (ids 497 e 1590), professores e valores
diferentes — cara de duplicata. Nao mexido: e qualidade de cadastro, nao calculo.

## Sincronizacao de migrations orfas (relatorio admin 2o/3o curso) (~20h BRT)

Investigado o relatorio diario administrativo do Recreio (reclamacao da Vitoria: "27
alunos de 2o curso, report puxando 24" + Caio Vinicius contado como 2o curso quando
ja faz o 3o). Causa: `get_kpis_alunos_admin_operacional` ja tinha sido corrigida por
outra sessao hoje (`20260731163353_relatorio_admin_canonico_multicurso_trancamentos` e
`20260731164500_restringe_kpis_admin_operacional`, 16h33 e 16h45), aplicadas via MCP
**direto no banco, sem arquivo no repo** — mesmo gap ja citado na linha 135 deste
arquivo. Rodando a RPC hoje para o Recreio: `matriculas_2_curso=27`,
`alunos_com_2_curso=26`, `alunos_com_exatamente_2_cursos=25`,
`alunos_com_exatamente_3_cursos=1` (o Caio, agora corretamente distinguido de quem
tem so 2 cursos). O numero que a Vitoria esperava (27) ja bate.

**Reconstrui os 2 arquivos de migration** a partir do `pg_get_functiondef` real do
banco (mesmo texto, so documentando o que ja esta rodando) e commitei — nao apliquei
nada novo, so fechei o gap de versionamento.

⚠️ **Achado maior durante o `supabase db pull`:** o historico de migrations do projeto
esta **muito mais divergente do repo** do que so essas 2 — o `db pull` listou dezenas
de versoes (de 2026-01 a 2026-07) marcadas como so-remoto ou so-local, e abortou pedindo
uma bateria de `supabase migration repair --status reverted/applied` pra reconciliar.
**Nao rodei nenhum repair** (mexe na tabela de tracking do projeto inteiro, risco alto,
fora do escopo do pedido de hoje). Fica registrado como pendencia separada: alguem
precisa decidir, com calma, como reconciliar esse historico antes que ele cause um
`db push`/`db reset` que apague ou duplique algo.
