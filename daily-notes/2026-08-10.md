# 2026-08-10

## Aferição das pendências 👁️ OBSERVAR (~11h BRT)

Checagem das 4 pendências em observação. **Nada alterado nesta etapa.**

| # | Pendência | Situação |
|---|---|---|
| 11 | Varredura de atribuição Meta Ads | ✅ Saudável — log em 10/10 dias, `vinculado` caindo (22→10/dia), `anuncio_SEM_origem` = 0 em 9 dos 11 dias (os 2 são `source_app='whatsapp'`, fora do mapa por design) |
| 13 | Observador nas experimentais | ✅ `modo=escrita` em 100%, duplicata e fantasma zerados, `observacoes` em 28 de 39 (era 0 de 311). Sem evento dom 09 e seg 10 até 11h — é domingo/segunda de manhã, endereço vivo (29 eventos de lead hoje) |
| 13.8 | Corrida experimental × lead | ✅ 2ª evidência: Luana Ferreira 08/08, `tentativas_rpc = 4`. Zero `lead_not_found` pós-deploy. Aferir 13/08 |
| 15 | Headers / IP do webhook | ✅ 325 entregas, **1 IP** (`52.67.146.71`), 0 sem header. O 2º IP era o `ping_headers_teste` manual |
| 14 | `professor_experimental_id` | ⚠️ `copia_sem_lastro` = 0 pós-v33 (bom), mas 1 `DIVERGE` real → virou o trabalho do dia |

Conclusão: **#11 pronto para fechar** (10 dias limpos). **#15 já entregou o que dava medir** — vira decisão sobre fechar o endpoint, não mais observação.

## Investigação do #14 — matrícula no mesmo dia da experimental (~12h BRT)

**Caso disparador:** Amelie Fontoura (#1925, CG). Aula 08/08 11:00 → matrícula 12:24 → sync promoveu a linha às 14:45. Na matrícula a v33 procurou a experimental, encontrou a linha 1347, mas o filtro `.in('status', ['experimental_realizada','convertido'])` a descartou (ainda era `experimental_agendada`) → gravou `null`.

**Causa raiz:** o campo é preenchido num único instante, a partir de um **status derivado** que só existe depois do sync. Janela medida entre a aula e a promoção (01-09/08): de **0,0h a 25,2h**.

**Por que ninguém corrige depois:** o `sync-presenca-emusys` só **lê** `alunos` (dois `.select('id')`, linhas 710 e 813). Nunca escreve. Prova empírica: a Amelie ficou 46h com a resposta parada em `lead_experimentais` e o campo vazio.

**Dimensão real** — dos 10 alunos com campo vazio desde junho: **2** são a corrida do mesmo dia, **8** não têm linha nenhuma em `lead_experimentais`.

### Achados colaterais (medidos, não aplicados)

1. **O webhook de experimental NÃO traz id de aula.** Varridas as 178 entregas: 20 chaves em `aula`, nenhuma é `id`. O `body.id` do topo é **id de evento** — Rafael Bredoff teve `criada`=77108 e `reagendada`=77113 com 14s de diferença, mesma aula. ⚠️ **O CLAUDE.md está errado** ao dizer que o webhook captura `body.id` como id da aula; o `pendencias.md` está certo.
2. **`aula_cancelada` traz `aula.id` — e é id de aula real.** 203/203 entregas, 0 iguais ao id de evento, 203/203 casam em `aulas_emusys`. Ou seja: é **inconsistência entre webhooks do Emusys**, não capacidade que falta. Fortalece muito o pedido.
3. **5 vínculos órfãos em `lead_experimentais`** (`emusys_aula_id` apontando para nada). Causa provada: 4 deles (irmãos Soares de Moura, 05/08) têm exatamente o `body.id` dos webhooks de reagendamento daquele dia. ⚠️ As faixas de id **se sobrepõem** (evento 65.898+, aula 236.826–831.490) — não dá para separar por magnitude. Testada colisão silenciosa: **0 de 246** com nome ou data divergente.
4. **`professor_experimental_id` de `lead_experimentais` é confiável:** 241 de 243 batem com a API (99%). As 2 exceções são o **mesmo professor** em cadastro mesclado ("Erick Osmy (mesclado 54)" × "Erick Osmy" 52), ambas canceladas.
5. **Os "ambíguos" são majoritariamente sujeira nossa.** Dos 4 casos com 2 professores no mesmo curso, comparados contra o espelho da API: Carlos Eduardo e José Gabriel têm **1 aula só** na API (o 2º professor é linha duplicada nossa); Karina está fora da janela do espelho (que só cobre experimental a partir de 15/06).

   🔴 **CASO JÚLIA BARROSO — errei duas vezes, resolvido ao fim do dia.**

   **Erro 1:** afirmei que "o professor está errado na nossa base — API diz Lohana, nós dizemos Leticia".
   **Erro 2 (a "correção" que fiz depois):** ao ver a curadoria do Alf (`20260810141049`) concluir que a experimental era da Leticia, escrevi que a grade não é confiável para isso e que só o histórico do lead arbitra.

   **O que os dados mostram (apurado após o Hugo apontar que `/aulas` expõe `categoria`):** a grade tem **2** experimentais, não uma disputada — `815546` em 16/07 com **Lohana** e `823968` em 29/07 com **Leticia**. E o `data_hora_inicio_original` da 815546 é **15/07 14:30**, exatamente a nossa linha 1126 (Erick): aquela aula foi reagendada de dia **e reatribuída** de Erick → Lohana, e nossa 1126 é o fantasma pré-reagendamento dela.

   Ou seja: eu estava comparando a Lohana do 16/07 com a Leticia do 29/07 — **aulas diferentes**. A anotação do CRM que embasou a curadoria é de 02/08, posterior às duas, e com toda a probabilidade se refere à de 29/07, que a grade **também** atribui à Leticia. **As fontes concordam.** A grade estava certa e completa; quem estava errado era o nosso registro (3 linhas para 2 aulas).

   ✅ **Lição real:** `GET /aulas` **é** a fonte boa para "quem deu a experimental" — inclusive com reatribuição, porque `reagendada` + `data_hora_inicio_original` reconstroem o que aconteceu. **Casar por AULA, nunca por aluno ou data isolada.** Antes de declarar a grade errada, conferir se as linhas comparadas são da mesma aula. Registrado no mapa (§1.2).
6. **Cobertura de `emusys_lead_id` é 100% em jul/ago** nos dois lados. Junho é transição (69%) porque a API só passou a devolver `id_lead` em 21/06.

## Conserto aplicado — RPC `propagar_professor_experimental` (~13h BRT)

Migration `propagar_professor_experimental_para_matricula`. `SECURITY DEFINER`, ACL conferida = `{postgres=X, service_role=X}` (o `revoke ... from anon` nominal é obrigatório neste schema).

Quatro travas: (1) só `professor_experimental_id IS NULL` — nunca sobrescreve; (2) só lastro **único**; (3) `data_experimental <= data_matricula`; (4) recusa linha com edição humana no campo (`audit_log origem='manual'`). A trava 1 aparece duas vezes — na CTE e no UPDATE — a segunda é guarda contra corrida.

Serve a dois usos: **com** `p_emusys_lead_id` → um lead (chamada do sync); **sem argumento** → varre tudo (backfill).

### Validação em sombra, antes de aplicar

- **Não-regressão:** aplicada a lógica **sem** a trava `IS NULL`, só para comparar com o gravado. 319 alunos com valor: **125 concordam**, **4 discordam**, 190 ela não opinaria. Das 4 discordâncias, **3 são o bug da v32** (`gravado = titular`) — onde ela discorda, ela está certa. Nenhuma seria tocada.
- **ROLLBACK test:** `BEGIN; select propagar_professor_experimental(); ROLLBACK;` → devolveu **14**, idêntico à sombra. Confirmado que o corpo da função equivale ao SQL validado à mão. Pós-rollback: 15 ainda vazios, sem resíduo.

### Resultado

**14 linhas preenchidas.** Manuela de Menezes Lopes (#1759) **bloqueada** pela trava de edição humana — a Dai havia esvaziado o campo em 08/06. Amelie (#1925) recebeu a professora 62 (Adriana Mesquita), que é exatamente o valor parado em `lead_experimentais` havia 46h.

6 dos 14 têm professor da experimental **diferente** do titular — informação que a cópia da v32 jamais produziria.

Idempotência confirmada: segunda execução devolveu **0**.

## Sync do repo + correção trazida pelo pull (~16h30 BRT)

Estava **68 commits atrás**. `git stash` do `CLAUDE.md` → `pull --ff-only` → `stash pop` sem conflito (o remoto tinha mexido no mesmo arquivo, 41 inserções). Hoje: 0 atrás, 0 à frente.

⚠️ **Outra sessão trabalhando no mesmo banco hoje** — migrations de troca de professor entraram minutos depois da minha.

O pull trouxe `20260810141049_curadoria_humana_de_4_experimentais_conferidas_no_emusys` (Alf, 14:10), que **contradiz** o que eu havia afirmado sobre a Júlia Barroso. Correção registrada no achado 5 acima.

## Etapa 2 — feita por TRIGGER, não pela edge (~17h BRT)

Ao abrir o `sync-presenca-emusys` para enganchar a chamada, descobri que **dois** pontos promovem `lead_experimentais` ali (reconciliação ~845, que foi a que promoveu a Amelie, e `confirmarExperimentais` ~1351) — e mais três fora dela (`fn_reconciliar_experimental_aulas`, `registrar_experimental`, edição manual). Enganchar na edge cobriria 2 de 5 e ainda cobraria o redeploy.

**Decisão (validada com o Hugo): trigger.** A regra é invariante da relação `lead_experimentais ↔ alunos`, não passo do sync — mesmo padrão de `fn_experimental_recebe_id_da_aula` e `trg_usuarios_sincroniza_rbac`, que foi para o banco justamente por haver dois caminhos de escrita. Cobre os 5, custa o mesmo por evento e **dispensa o redeploy**, que era o único risco sério que restava.

Migration `20260810165934_trigger_propaga_professor_experimental_na_promocao`. `AFTER INSERT OR UPDATE OF status`, 4 guardas de saída, `exception when others` → **nunca bloqueia a promoção**, e grava em `leads_automacao_log` só quando preenche.

**Custo medido:** 7,9 ms / 1.951 buffers por lead (a varredura sem argumento, usada no backfill, custa 823 ms / 22.324).

**Teste end-to-end, em transação com ROLLBACK:** reconstituído o estado de 08/08 12:24 (Amelie com campo `null`, experimental 1347 de volta a `agendada`), a promoção disparou o trigger → campo voltou a **62**, log com `alunos_preenchidos: 1`. Rollback sem resíduo.

## Pendências desta frente

- **Camada 1 (não feita):** cascata `GET /aulas?lead_id=…` na `processar-matricula-emusys`. O trigger **não** cobre matrícula cujo lead não tem linha nenhuma em `lead_experimentais` — eram **8 dos 10** casos vazios desde junho.
- **Camada 1 (não feita):** cascata para `GET /aulas?lead_id=…` na `processar-matricula-emusys` quando a consulta local não resolver. Cobre os 8 sem linha nenhuma. ⚠️ Deve ficar **depois do insert** (padrão do `complementarDescontoMatricula`, linha 1305), nunca antes — hoje a resolução roda na linha 1175, antes do INSERT, e **não há timeout em lugar nenhum do arquivo**.
- **Pedidos à API do Emusys:** consolidados em **7 itens** na pendência #17, em ordem de custo, cada um com evidência medida. O #1 é `aula.id` nos 3 webhooks de experimental — **paridade**, não feature nova, já que o `aula_cancelada` envia em 203/203 entregas. Precedente a citar: o `professor_id` seguiu esse caminho e saiu na v1.4.1.
- **3 alunos ainda com a cópia da v32** (Felipe #1526, Sofia #1633, Isaque #1806). Corrigir muda valor existente — decisão separada, não entrou no backfill.
- **Gabriel Balestieri #1593:** discordância que **não** é cópia do titular (gravado 16 Larissa, lógica propõe 25 Matheus Felipe). Merece um olhar.

## Fallback UAZAPI pro envio de relatórios da Sol (~20h BRT)

**Gatilho:** número da Sol caiu, relatórios pararam de sair pros grupos de WhatsApp. Investigação revelou uma cadeia mais complexa do que o esperado, com **dois** bridges de relatório distintos:

1. `sol-openclaw-report-bridge.service` (servidor **alfredo**, 187.127.9.25) — polla `fila_relatorios_sol_hermes`/`fila_relatorios_whatsapp` e envia via `openclaw message send` (WhatsApp Web vinculado por QR do OpenClaw, também desconectado: `channels status` → `stopped, disconnected`). **Não estava versionado em nenhum repo** — só existia ao vivo no servidor. Confirmado que `fila_relatorios_whatsapp` (fila legada) não recebe linha nova desde 08/08 → parece bypassada pela migração pro `fila_relatorios_sol_hermes`.
2. **Causa raiz real:** `lareport_whatsapp_single.py` (la-hq, `/home/sol/.openclaw/workspace/scripts/`) chama `http://127.0.0.1:3000/send-report`, que é servido pelo bridge **nativo Hermes da própria Sol** (`hermes-agent/scripts/whatsapp-bridge/bridge.js` + `report-single-message.js`, sessão Baileys vinculada por QR, filha do `hermes-gateway-sol.service`). Essa sessão está `disconnected` → todo `/send-report` retorna `503 {"error":"not_connected_to_whatsapp"}`. Confirmado ao vivo em `fila_relatorios_sol_hermes` (ids 52-55, `gerencial_ia` de hoje, erro `bridge_http_503`).

**Decisão (Hugo):** fallback usando o número da **Lia** (UAZAPI, `whatsapp_caixas` id 3 "Lia - Sucesso do Aluno", 552123425316) — escopo restrito a relatórios automáticos, sem envolver o "cérebro"/prompt dela (chamada HTTP direta à API UAZAPI, sem passar pelo Hermes da Lia nem pela VPS dela).

**Implementado em ambos os pontos** (defesa em profundidade, já que não estava claro de antemão qual bridge estava realmente no caminho ativo):
- `lareport_whatsapp_single.py`: `send_single_report()` agora tenta o bridge nativo primeiro e cai pra `_send_via_uazapi_fallback()` (POST `/send/text` na UAZAPI, token lido de `whatsapp_caixas` via REST, cache 5min) se falhar.
- `sol-openclaw-report-bridge.cjs`: `sendWhatsApp()` idem (tenta `openclaw message send`, cai pro mesmo fallback UAZAPI). Também corrigido bug separado: `fetchNextQueueItem` da fila nativa só reprocessava `status='sol_pendente'` — itens que caíam pra `'erro'` ficavam travados pra sempre (fila legada já tratava isso corretamente). Agora inclui `'erro'` com janela de 6h.

**Testado ao vivo:** 1 mensagem de teste (grupo CG) + reenvio real dos 3 relatórios `gerencial_ia` que ficaram presos hoje (ids 53/54/55 — CG, Barra, Recreio), todos `transport=uazapi_fallback`, entregues. Id 52 (duplicata do 55, mesmo texto, batch anterior incompleto) marcado como coberto, não reenviado.

**Versionado** (não commitado ainda, aguardando revisão): `fiscal-mila/agents/sol/scripts/lareport_whatsapp_single.py` e `fiscal-mila/agents/alfredo/tools/sol-openclaw-report-bridge.cjs` (novo diretório, script nunca tinha sido versionado).

**Correção (Hugo, mesmo dia):** o número da Sol foi **restrito** (não é queda transitória) — reconectar a sessão não resolve por ora. O fallback UAZAPI/Lia fica valendo por tempo indeterminado até decisão sobre o número dela. Confirmado que a Lia já é membro dos 6 grupos usados pelo pipeline de relatórios (3 "RELATÓRIOS DIÁRIOS" + 3 "FINANCEIRO", um par por unidade) via `GET /group/list` da UAZAPI — nenhuma ação de convite necessária.

Também pendente: reconectar/aposentar formalmente o WhatsApp vinculado do OpenClaw no alfredo (`openclaw channels login --channel whatsapp`), hoje desconectado e cobrindo só a fila legada que não recebe mais linha nova.
