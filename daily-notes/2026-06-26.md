# 2026-06-26

## Notificação 360 do professor (WhatsApp) — erro 463 intermitente
- **~11h BRT** — Relato de erro ao clicar "Notificar Professor via WhatsApp" (modal `ModalWhatsAppPreview` → edge `professor-360-whatsapp`). Erro do print: `2 UNKNOWN: server returned error 463` (engine gows/whatsmeow).
- **Diagnóstico (verificado na fonte):** a edge envia pela caixa "Sol" (id 2, `funcao=sistema`), que está com `provedor='waha'` (`waha.agenticflowio.com.br`, session `5_198_552139554415`). O servidor WAHA estava no ar (HTTP 401=vivo) e a sessão `WORKING`/`gows.connected=true`; o 463 foi **soluço transitório** do whatsmeow. Teste de envio manual pro `5521964171223` passou (HTTP 201). A instância **UAZAPI** do mesmo número está **desconectada desde 29/05** (`disconnected`/`loggedIn:false`) → migrar pra UAZAPI exigiria reparear (descartado por ora).
- **Correção (commit `3290734`, deploy ok):** adicionado **retry com backoff** em `enviarWhatsApp()` da edge `professor-360-whatsapp` — até 3 tentativas (backoff 1.5s/3s), só em erro transitório (5xx/429/timeout/conexão); 4xx de request não repete. Resolve a falha intermitente sem trocar de provedor. Edge é invoke autenticado do front (deploy normal, sem `--no-verify-jwt`).
