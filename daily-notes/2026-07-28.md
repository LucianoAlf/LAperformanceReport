# 2026-07-28

## Skill emusys-api desatualizada — 5 versões atrás (~14h BRT)

Ao investigar uma dúvida do Luciano sobre renovações previstas, descobrimos que a skill `emusys-api` (em `~/.claude/skills/`) estava travada na v1.2.2 enquanto a API já estava na **v1.2.7**.

Causa da cegueira: `mcp__emusys__getPage` no changelog devolve conteúdo **cacheado** — trouxe até v1.2.3 no mesmo dia em que `mcp__emusys__searchDocumentation` já trazia v1.2.7. Ou seja, quem consultava o changelog pela via óbvia via um changelog velho.

Atualizações incorporadas na skill:

- **v1.2.3** (13/07): `reagendada` + `data_hora_inicio_original` no schema `Aula` — derruba a limitação "não dá pra filtrar aulas reagendadas" que estava documentada como impossível.
- **v1.2.4** (28/07): fix de `telefone_aluno` vindo `null` em aulas `categoria=experimental`.
- **v1.2.5** (28/07): `forcar_individual` em `POST /crm/aula_experimental`.
- **v1.2.6** (28/07): fix de turma cheia sendo oferecida/agendada para experimental.
- **v1.2.7** (28/07): `contar_alunos_experimentais` na checagem de lotação.
- `contrato_atual.flag_nao_vai_renovar` e `contrato_atual.contrato_assinado` — oficializados no OpenAPI spec (antes nem constavam).

Além disso, a skill ganhou uma seção **"Auto-atualização"**: protocolo obrigatório de, a cada carregamento, checar o changelog via `searchDocumentation` (não `getPage`), comparar com o campo "Versão atual" e editar o próprio `SKILL.md` quando divergir.

## Renovação prevista × renovação em vigor (~15h BRT)

Dúvida do Luciano: o report marcava como "renovação prevista" aluno que já estava com o contrato novo em vigor.

Confirmado em chamada real à API que `data_primeira_fatura` e `data_original_primeira_aula` são **independentes** — ex. Vinícius Lopa (CG): fatura vencendo 20/07/2026 mas aulas do novo contrato só começando em 13/08/2026. Uma lógica baseada só em "ausência de fatura no mês" confunde os dois casos.

Critério correto: comparar `contrato_atual.data_original_primeira_aula` com hoje, e/ou usar `flag_nao_vai_renovar` (marcador nativo do funil de renovação do Emusys).

## Spec: tela "Contratos vencendo" (~16h BRT)

Pedido do Arthur: replicar no report a tela **Escola → Renovação de Matrículas** do Emusys ("matrículas vencendo nos próximos 30 dias" + aulas restantes), que ele usa muito e hoje obriga a sair do report.

**Viabilidade validada ao vivo:** reproduzimos a lista da Barra com **16/16 registros idênticos** ao print, usando só `GET /matriculas?status=ativa` paginado (268 ativas, 6 páginas) e filtrando `contrato_atual.data_original_ultima_aula` na janela de 30 dias. "Aulas restantes" = `nr_aulas_contratadas - nr_aulas_passadas`. "Venc. última fatura" = `max(data_vencimento)` de `GET /faturas?contrato_id=X` (o filtro `contrato_id` **não está na doc pública**, mas funciona — verificado).

Achado importante: **não existe filtro nativo** de "vencendo em N dias" — `data_inicial`/`data_final` de `/matriculas` filtram pela data da matrícula. O recorte é sempre em memória.

**O que NÃO dá pra replicar** (limitação da API, não nossa):
- Aba "Matrículas Renovadas": as colunas de projeção de valor pós-renovação dependem da **Tabela de Valores** da escola, que não tem endpoint.
- Aba "Renovação em Lote": só existe `GET` na API — dá pra **ler** `flag_nao_vai_renovar`, não pra marcar.

Primeiro spec desenhava tudo em cima de chamada ao vivo à API (edge nova, cache em memória, throttle, retry de 429). **Descartado** — ver abaixo.

## Reviravolta: quase tudo já estava no banco (~18h BRT)

Pergunta do Hugo ("a gente não tem já esses dados no LA Report?") derrubou o desenho anterior. Investigando o banco:

- **`vw_jornada_aluno_atual`** (já em produção, alimentada pelo `sync-matriculas-*` de 02h) devolve os **mesmos 16 alunos, mesmas datas** do print do Emusys. Aluno, curso, professor, última aula e aulas restantes vêm de graça.
- A feature virou essencialmente **uma view SQL**, sem edge nova, sem cron novo, sem chamada ao Emusys no caminho do usuário.

**"Venc. Última Fatura" também não precisa de sync novo:** é derivável de `data_primeira_fatura + (nr_faturas-1) meses`, com o dia vindo de `dia_vencimento` — tudo já presente no `contrato_atual` que o sync de matrículas já busca. Validado **11/11 contra o print**, incluindo o aluno com `nr_faturas=0` que aparece com a coluna vazia na tela do Emusys.

Spec reescrito do zero em `docs/superpowers/specs/2026-07-28-contratos-vencendo-emusys-design.md` (2 rodadas de review, ambas acharam erro factual meu).

## Bugs de produção descobertos no caminho

Três achados que **não têm relação com a feature nova** e continuam abertos:

1. **`sync-presenca-emusys` engole erro calado.** No loop de paginação, qualquer resposta não-ok faz `console.error` + `break` — devolve as aulas parciais como se fossem o dia inteiro, sem retry e sem sinalizar. Roda a cada 5 min. Presença alimenta score de professor, pesquisa de 1ª aula e modelo de risco de evasão.

2. **`sync-inadimplencia-emusys` falha em silêncio.** `inadimplencia_emusys_cache` está sem escrita desde 15/07 (13 dias) e **Campo Grande nunca teve uma linha**. Os 9 crons reportam `succeeded`. Vale checar se o `useSaudeCrons` cobre "cron rodou mas não gravou nada".

3. **A régua de renovação de Sucesso do Aluno perde alunos.** `vw_renovacoes_proximas` (consumida pelo `useMarcosJornada`) usa `alunos.data_fim_contrato`, que diverge do Emusys em **17,8%** dos casos — **14 alunos** com contrato vencendo em 30 dias não aparecem na régua. Causa: o sync nunca escreve esse campo sozinho, só `sugerirCampoRevisao` para aprovação humana na Conciliação; renovação não aprovada fica com data velha.

4. **`alunos.dia_vencimento` é default de formulário, não dado do Emusys.** 91,2% dos ativos estão no dia 5 porque `ModalNovoAluno` grava `5` fixo; nenhuma edge escreve o campo. Na Barra, 16 de 256 divergem do Emusys — e em 100% desses o local é 5 e o real é outro. Sincronizar o campo (previsto no spec) corrige isso de lambuja.

## Implementação (~17h-21h BRT)

Plano executado com subagentes (7 tasks), branch `feat/contratos-vencendo-emusys`, 16 commits.

**Entregue:** aba **Contratos** em `/app/administrativo` (2ª posição, ao lado de Lançamentos) — não em `/app/entrada` como planejei a princípio. O "do lado de Entrada" do Arthur era a aba da Caixa de Entrada dentro do Administrativo; a página `/app/entrada` nem está no sidebar e nada linka pra ela.

Peças: 4 colunas novas em `aluno_jornada_matricula_disciplina` (`nr_faturas`, `data_primeira_fatura`, `dia_vencimento_emusys`, `inadimplente_emusys`), view `vw_contratos_vencendo`, hook `useContratosVencendo`, componente `TabContratosVencendo`. Zero edge nova, zero cron novo, zero chamada ao Emusys no caminho do usuário.

**Decisão de arquitetura que só apareceu na implementação:** as colunas foram para a **jornada**, não para `alunos`. O `sync-matriculas-emusys` escreve na jornada direto (`upsertJornadasEmLote`), mas em `alunos` só chama `sugerirCampoRevisao` — que enfileira para aprovação humana. Nas colunas em `alunos`, a tela só funcionaria após 1224 aprovações manuais.

### Problemas encontrados e corrigidos durante a execução

1. **Regressão nossa (Critical):** `data_primeira_fatura` ia crua da API para uma coluna `date`. Uma data `"0000-12-18"` derrubava o lote de 20 do upsert → **57 linhas pararam de sincronizar** (2 Barra, 45 CG, 10 Recreio). Corrigido com `parseDateOnly` + corte de ano < 2000. Recuperou para 17 (essas são órfãs pré-existentes da API). ⚠️ O subagente tinha classificado como "pré-existente, fora de escopo" — não era.
2. **View juntava `alunos` duas vezes (Important):** `vw_jornada_aluno_atual` já faz join com `alunos`; a view nova fazia um segundo por `emusys_matricula_id`. Resultado: em 5 linhas o nome vinha de um cadastro e o valor de outro. Visível na tela (Natan aparecia sem matrícula e sem valor). Corrigido para `a.id = j.aluno_id`, o que tornou o `DISTINCT ON` desnecessário.
3. **Webhook zerava os campos (Important):** `upsertJornadaMatriculaDisciplina` mandava as 4 chaves com null, e o `ON CONFLICT DO UPDATE` apagava o que o sync gravara. ~23 webhooks/dia. Corrigido omitindo as chaves quando todas vêm nulas.
4. **Timezone:** `formatarData` fatiava a string ISO, errando o dia em `timestamptz` (o cron roda 02h UTC = 23h BRT do dia anterior). Corrigido com `Intl`/`America/Sao_Paulo`.

### Incidente de deploy

Um subagente subiu **duas versões quebradas** do `sync-matriculas-emusys` em produção (v70 com linha omitida, v71 com `index.ts` placeholder) antes da v72 correta. Verificado depois: nenhum cron rodou na janela (crons às 02h UTC, deploy às 20h UTC) e o `sync-matriculas` só é chamado manualmente fora disso. O bundle da v72 foi comparado byte a byte com o repositório — idêntico. Sem perda.

**Lição:** subagente não deve transcrever arquivo grande à mão para deploy.

### Regra DRY adicionada ao CLAUDE.md

A pedido do Hugo, e com esta feature como caso ilustrativo: o desenho inicial previa edge nova, cron novo, tabela nova e ~218 chamadas diárias à API — tudo isso porque ninguém checou se já existia algo. `vw_jornada_aluno_atual` já entregava 90% do necessário.

**Pendente:** decidir o merge (branch pronta, review final limpo).

## Inadimplência: o cache nunca funcionou (~19h BRT)

Pergunta do Hugo ("esse inadimplencia tá parado porque? tem outro endpoint que já mapeia?") revelou que o `sync-inadimplencia-emusys` **nunca funcionou uma vez sequer**, desde o deploy em 15/07.

**Causa raiz:** os 9 crons mandam só o header `x-sync-token`; a edge está com `verify_jwt=true` (nunca ganhou entrada no `config.toml`, ao contrário da irmã `sync-professor-disciplinas-emusys`). O gateway do Supabase devolve `401 UNAUTHORIZED_NO_AUTH_HEADER` **antes do código da função rodar** — a validação por `x-sync-token` que existe dentro dela nunca é alcançada. Comprovado nas 5 execuções ainda retidas em `net._http_response`.

**Por que ninguém viu:** o `pg_cron` marca `succeeded` porque só avalia se o `net.http_post` foi *enfileirado* — o status HTTP não volta pra ele.

As 682 linhas do cache (Recreio 422, Barra 260, **CG zero**) eram resíduo de cliques manuais no botão "Atualizar agora", que mandava o JWT do usuário. Por isso a feature "parecia" funcionar no teste.

**Conserto:** a Lista de Alunos passou a ler `aluno_jornada_matricula_disciplina.inadimplente_emusys` — mesmo campo, mesma API, gravado pelo `sync-matriculas-emusys`, cujo cron funciona. Essa coluna já existia desde ontem; **não precisou de coluna nova nenhuma**. Validado: banner mostra 39 pessoas (42 matrículas), batendo com o banco.

Aposentados: 9 crons (`cron.unschedule`), edge deletada, tabela renomeada para `inadimplencia_emusys_cache_legado` (convenção do projeto).

### Bug pré-existente encontrado e corrigido junto

O ⚠️ de divergência ao lado da parcela comparava o valor **cheio** da API contra `alunos.valor_parcela`, que é o **líquido** (`valor_mensalidade − desconto_condicional`, regra canônica em `processar-matricula-emusys`). Resultado: acendia em **747 de 1171** matrículas ativas — todo aluno com desconto condicional. Contra líquido, seriam 31.

Removido da tela em vez de corrigido: divergência de valor **já é feature pronta** na aba Conciliação Emusys (`matriculas_divergencias`, `tipo_divergencia='valor_divergente'`), com fila, aprovação humana e fixação de campo. Manter os dois seria duplicar. O somatório em R$ do banner também passou a usar o líquido (antes inflava com o valor cheio).

### Erro meu no caminho

Criei duas colunas na jornada (`valor_mensalidade_emusys`, `desconto_condicional_emusys`) para alimentar esse ⚠️ **antes de checar se já existia solução** — e existia, na Conciliação. Afirmei "não tem valor no banco" olhando só a tabela da jornada, sem procurar em `alunos` (que já tem `valor_cheio`/`desconto_condicional`) nem na fila de divergências. Foi o Hugo que apontou, duas vezes, antes de eu perceber.

Revertido: colunas dropadas, edges redeployadas a partir do git (v74/v70, verificadas), efeito líquido no schema = zero.

**Ordem que importou:** reverter o código e redeployar as edges **antes** de dropar as colunas — a v73 deployada escrevia nelas, e dropar primeiro quebraria o cron das 02h.

**Lição (2ª vez no mesmo dia que a regra DRY pega algo):** "não existe" precisa ser verificado no banco inteiro e nas features existentes, não na primeira tabela que vem à cabeça.

### Pendências relacionadas

- `sync-presenca-emusys` continua engolindo erro calado (roda a cada 5 min, alimenta score de professor e modelo de churn)
- `get_saude_syncs_emusys` / `useSaudeCrons` não detectam "cron rodou mas não gravou nada" — foi essa cegueira que deixou a falha durar 13 dias

### Correção: botão "Atualizar agora" restaurado

Eu havia removido o botão junto com a edge aposentada — extrapolando o escopo, já que ele **não era erro meu de hoje**: existia desde a feature original (`3cb54be`) e funcionava. Prova: o clique do Hugo às 19h03 populou as 3 unidades em 9 segundos, inclusive Campo Grande pela primeira vez (ele manda o JWT do usuário, que passa no `verify_jwt`).

Restaurado com alvo novo: edge **`atualizar-inadimplencia-emusys`**, que escreve **na própria jornada** em vez de num cache paralelo — mantendo fonte única. Ela faz só `UPDATE` de `inadimplente_emusys` em linhas existentes (match por `unidade_id` + `emusys_matricula_id`), nunca `upsert`, e aborta sem gravar se a paginação da API vier incompleta.

**E desta vez com `verify_jwt = false` no `config.toml`** — a linha exata que faltava na antecessora. Testada em produção pelo caminho do cron (`x-sync-token`): `{"unidade":"Barra","matriculas_lidas":268,"inadimplentes":5,"linhas_jornada_atualizadas":269}`.

Saldo final: o banner ganhou dado correto e auto-sustentado nas 3 unidades, **sem perder** o refresh sob demanda que já existia.
