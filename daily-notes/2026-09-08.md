# 2026-09-08

## Carteira do professor conta aluno de atividade extra (~11h BRT)

Pedido do Arthur (áudio 04/09): tirar os alunos de banda da carteira dos professores
de banda. Exemplo dele: **Mauricio Cabral Liberato de Matos Junior** (Barra) contava
como aluno do **Will** (Willian De Andrade Da Silva, prof. da banda) sendo aluno do
**Lucas Amorim Souza** (Bateria) — aparece na carteira dos dois.

**Diagnóstico (só leitura, nada alterado):**

- O flag `cursos.is_projeto_banda` **está correto no dado** — os 4 cursos extras
  (Power Kids 41, Minha Banda Para Sempre 33, GarageBand 32, Percussion Kids 1)
  têm `is_projeto_banda = true`, e a RPC de detalhe marca `elegivel_media = false`
  para o Maurício no Will.
- O filtro só é aplicado em **média de alunos por turma**, nunca na contagem da
  carteira:
  - `get_carteira_professor_periodo_canonica`: `carteira_alunos = count(distinct
    pessoa_chave)` **sem** `filter (where elegivel_media)`; só `alunos_via_turmas`
    e `turmas_elegiveis_media` filtram.
  - `get_carteira_professores(uuid)` (fallback contratual da aba Carteira):
    `total_alunos = count(*)`; `is_projeto_banda` só entra em `conta_turma`.
- Medido hoje: **107 linhas de atividade extra dentro de carteiras**, **91 delas
  duplicando a mesma pessoa em dois professores**, **15 professores afetados**.
  Piores: Ramon Pina Morais/Recreio 26 de 37 (carteira real 11), Lucas da Silva
  Guimarães/Recreio 13 de 38, Will/Barra 10 de 26 (real 16). Matheus Lana ("Lano"
  do áudio) 3 de 27.
- Cadeia afetada: a carteira alimenta o **Health Score V3**
  (`get_health_score_professor_v3_totais_carteira_canonica_v1` usa `carteira_alunos`
  como `pessoas_unicas_total`) e o snapshot `professor_carteira_mensal_canonica`
  (jun/jul/ago **já congelados com banda dentro** — corrigir a função não conserta
  mês fechado, e o detalhe prefere `carteira_total_auditado` quando ele existe).
- Ticket/MRR do professor **não** têm o problema: já filtram por
  `tipos_matricula.entra_ticket_medio`.

**Pendente de decisão (Alf/Arthur):** se a carteira passa a excluir atividade extra,
como fica o professor que **só** dá banda (Alan Samico/CG hoje 5 de 5) e o Health
Score dele; e se os snapshots de jun-ago são recapturados ou ficam como estão.

## Varredura: onde mais a atividade extra infla contagem (~12h BRT)

Pergunta do Hugo: além da carteira, o que mais conta banda indevidamente.
Medido no banco, competência set/2026 (ago/2026 para presença).

**Raiz comum:** `vw_alunos_estado_operacional_v131` **não conhece o conceito de
atividade extra**. As 107 linhas extras ativas entram em **todos os 6 flags**
(`entra_base_ativa`, `entra_carteira_professor`, `entra_financeiro_ativo`,
`entra_denominador_presenca`, `entra_health_score`, `entra_churn_atual`). Quem
filtra é cada consumidor — e o domínio do professor não filtra.

**Afetado:**

| Lugar | Efeito medido |
|---|---|
| `carteira_alunos` (Professores → Carteira e Performance) | 107 linhas, 15 professores |
| `total_turmas` do professor | Will 17 exibido × 15 elegíveis |
| Health Score V3 | `carteira_alunos` = `pessoas_unicas_total` em `get_health_score_professor_v3_totais_carteira_canonica_v1` |
| `professor_carteira_mensal_canonica` | jun/jul/ago congelados com banda dentro |
| Frequência/presença do professor | ago/26: Alan Samico 40/40 (100%), Ramon Pina 252/336 REC e 100/126 CG, Will 78/221 (35%) |
| Risco de evasão (`vw_risco_evasao_atual`) | 115 linhas de atividade extra pontuadas como matrícula própria |
| LA Teacher `app_minha_carteira` | 115 linhas, 12 professores (provavelmente **correto** — ele dá a aula) |

**NÃO afetado (verificado, não presumido):**

- **Alunos ativos / pagantes / MRR / ticket**: CG tem 411 pessoas − 7 trancados = 404
  e o KPI devolve **401** — exatamente as 3 pessoas que só fazem banda excluídas.
  `get_kpis_alunos_canonicos_base_p01q` monta `is_banda`/`is_coral` e filtra.
- **Média de alunos por turma**: usa `turmas_elegiveis_media`/`alunos_via_turmas`.
- **Ticket/MRR do professor**: filtra por `tipos_matricula.entra_ticket_medio`.
- **Retenção/evasão**: já coberto por `movimentacao_conta_nos_kpis_v1` (27/08).

**Achado que resolve o desenho:** `fn_carteira_fatiada(professor_id)` — consumida por
`app_professor_carteira_contagem` (agente que fala com o professor) — **já implementa
a resposta certa**: devolve `regulares`, `em_atividade_extra`, `so_atividade_extra` e
o detalhe por curso, com nota embutida mandando *"fale SEPARADO... não some os dois
num número só, e não omita a atividade extra: o professor dá aula pra ela e vê essa
gente na agenda"*. Will: 40 pessoas = 30 regulares + 10 só extra (6 Minha Banda,
4 Power Kids). **A decisão de produto já existe; as telas do LA Report não a seguem.**
E `alunos_via_turmas` (16 no Will) já é a carteira regular — o número certo é
calculado e simplesmente não é o exibido.

⚠️ Divergência lateral não investigada: Recreio deu 338 no KPI contra 337 na contagem
manual de pessoas (+1). Não tem relação com banda.

## Veredito contra a regra escrita (~12h30 BRT)

Conferido em `docs/REGRAS-DE-NEGOCIO.md` (não de memória):

- **§3.5** — atividade extra é "**excluída** de: alunos ativos, alunos pagantes,
  ticket médio, MRR, LTV, churn, médias de turma, **carteira do professor**,
  **score do professor**, matrículas novas canônicas".
- **§7.1 Carteira do professor ✅ (validada)** — "Inclui banda/atividade extra?
  **Não.**" E o mesmo §7.1 declara que a Carteira **e o Health Score V3** leem
  `get_carteira_professor_periodo_canonica` → o HS herda a violação por construção.
- **§7.2 Score do professor ✅** — "Alunos de atividade extra ficam fora do
  denominador" (já corrigido em 27/08).
- **§9.2 Faltas 📋** — "Projeto banda **é incluído**, mas com flag para o front
  exibir badge 'Banda' — decisão: **sinalizar, não esconder**".

⚠️ **Portanto NÃO é "nada disso conta".** Só carteira, médias de turma e score do
professor têm regra explícita de exclusão. **Presença/frequência tem regra explícita
no sentido CONTRÁRIO** (§9.2). Risco de evasão e `total_turmas` são **lacunas** — o
doc não fala deles; tratá-los como violação seria inventar regra.

O princípio "sinalizar, não esconder" (§9.2) é o mesmo da nota embutida em
`fn_carteira_fatiada` — reforça separar em duas colunas em vez de sumir com o número.

## Vaza sim: relatório de coordenação e ranking IA (~13h BRT)

**1. Relatório de coordenação — vaza, e já está FECHADO.**
Snapshot `relatorio_coordenacao` ago/2026 Barra, `status='fechado'`, v2:

- `carteira_carga.alunos_na_carteira = 283` — a Barra tem **256 alunos ativos**.
  O relatório entrega um número **maior que a base da unidade**.
- `carteira_carga.media_por_professor = 14.2`.
- Por professor: **Will 27** (`alunos_via_turmas` 19, `total_turmas` 20 × 18 elegíveis);
  Matheus Lana 29 (31 turmas × 30 elegíveis).
- A edge `gemini-relatorio-coordenacao` imprime isso em texto:
  `• Carteira: N aluno(s)` ([index.ts:418](supabase/functions/gemini-relatorio-coordenacao/index.ts#L418))
  e `• Alunos acompanhados nas carteiras: *283*` ([index.ts:513](supabase/functions/gemini-relatorio-coordenacao/index.ts#L513)).
- ⚠️ **Congela sozinho todo mês**: cron `capturar-relatorio-coordenacao-v2-mensal`
  (jobid 200, `15 3 2 * *` = 00:15 BRT do dia 2).

**2. Ranking de professores IA — vaza.**
`gemini-ranking-professores` monta `rankingCarteira` com `numero_alunos.valor_bruto`
do Health Score (= carteira inflada): média da unidade, maior/menor carteira e
**Top 3 por tamanho de carteira** entregues à IA
([index.ts:136/326](supabase/functions/gemini-ranking-professores/index.ts#L136)).
Professor de banda sobe no ranking por aluno que não é dele. Sob demanda, sem cron.

**3. Health Score — contaminado no texto, NÃO na nota.**
`numero_alunos` tem `papel: 'diagnostico'` e `peso_efetivo: 0` (peso_original 10) —
a própria edge escreve "diagnóstico; não altera a nota". ⚠️ Não verifiquei se a
carteira entra como denominador de **outro** pilar via
`get_health_score_professor_v3_totais_carteira_canonica_v1`; afirmar que o score
está limpo exigiria essa checagem.

**Não vaza (verificado):**
- **Relatório gerencial**: não tem carteira por professor. Os 4 rankings que publica são
  presença, média de turma, retenção e matriculadores — média de turma e retenção já
  filtram, matriculador usa matrícula nova canônica, e presença inclui banda **por
  decisão** (§9.2). Conferido `top_professores_presenca` de Recreio/ago: nenhum dos
  professores de banda aparece no pódio.
- **Relatório admin/comercial** (mensal e diário WhatsApp): fala de alunos ativos e
  pagantes, que estão corretos.
- **Relatório diário de presença**: lista chamada não fechada, não conta carteira.

## Cruzamento das fontes de regra (~13h30 BRT)

Conferido `.claude/memory/regras-negocio-canonicas.md` contra `docs/REGRAS-DE-NEGOCIO.md`:
**alinhados no conteúdo, divergentes no grau de confiança.**

- memory §1.3 (l.44): banda "excluído de: médias de turma, **carteira do professor**,
  score do professor, ticket médio, LTV, churn, contagem de pagantes" — idêntico ao §3.5.
- memory §4.1 (l.220): "Inclui banda? **Não**". memory §4.2 (l.227) e R4 (l.249) idem.

⚠️ **A divergência que importa é o selo:** `docs` §7.1 marca ✅ **VALIDADA PELO ALF**;
memory §4.1 marca 📋 **"INFERIDA DO CÓDIGO ATUAL — implementada no código, não
explicitamente validada"**. Esse 📋 é **factualmente falso**: o código não faz isso, e
nunca fez. Alguém leu o nome `is_projeto_banda` e inferiu o comportamento sem medir —
é a mesma assinatura do bug do churn de bolsista (27/08): regra escrita há meses,
zero implementação, ninguém conferiu porque o número continuava plausível.

⚠️ memory §4.1 ainda descreve a fórmula legada (`COUNT(alunos) WHERE professor_atual_id
AND status='ativo'`); `docs` §7.1 já aponta `get_carteira_professor_periodo_canonica`
como fonte canônica. Defasagem (memory 10/08 × docs 04/09), não contradição.

⚠️ memory é **silenciosa** sobre faltas/presença com banda — o §9.2 do docs
("sinalizar, não esconder") só existe no doc grande. Quem consultar só a memory
concluiria, errado, que presença também deveria excluir banda.

## Decisão do Luciano (WhatsApp, 08/09 13:31 BRT)

Pergunta do Hugo (13:18): *"Essas paradas de média de alunos/turma, health score, risco
de evasão e a contagem total de turmas não devem contar com bandas né?"* → **"não"**,
ou seja, **não devem contar**.

Isso **fecha as duas lacunas** que o doc não cobria — `total_turmas` e risco de evasão —
e estende a exclusão ao Health Score. Registrado em `docs/REGRAS-DE-NEGOCIO.md` §3.5
e §7.1, e o selo falso 📋 de `regras-negocio-canonicas.md` §4.1 virou ✅ + aviso de
"nunca foi implementado".

⚠️ **A pergunta não cobriu 3 pontos, e eles seguem abertos:**
1. **Carteira do professor** não foi citada — mas já era ✅ validada (§7.1), então não
   precisa de decisão nova; só de implementação.
2. **Separar × excluir na TELA.** "Não deve contar" é sobre métrica agregada. A carteira
   é também uma **lista de quem o professor atende** — excluir puro tira o Alan Samico
   (5 de 5 em banda) da tela inteira. Proposta: métrica exclui, tela mostra os dois
   números (é o que `fn_carteira_fatiada` já faz e o que §9.2 decidiu para faltas).
3. **Snapshots jun/jul/ago** já fechados com o número inflado — recapturar ou erratum?

⚠️ **Presença/faltas ficou de fora da pergunta e NÃO deve ser mexida**: §9.2 decidiu o
contrário ("banda é incluído, com badge — sinalizar, não esconder"). É o único dos 7
achados em que contar banda é o comportamento correto.

⚠️ **Risco de evasão muda a tela de Sucesso do Aluno**: são 115 linhas hoje pontuadas
como matrícula própria; excluí-las altera a lista que a equipe acompanha, não só um KPI.

## Correção aplicada (~15h BRT) — LAPE-23

Branch `fix/carteira-professor-exclui-atividade-extra`. `sync-repo` antes: a main local
estava **131 commits atrás**; puxei e reapliquei as edições de doc sobre a base nova.

**Banco (3 funções, aplicadas e validadas em produção):**

1. `get_carteira_professor_periodo_canonica` — `carteira_alunos` e `total_turmas` agora
   filtram `is_projeto_banda`. Snapshot auditado mantém precedência (histórico fechado
   preservado, conforme decisão).
2. `get_carteira_professor_periodo_composicao_v1` — **NOVA**. Fonte única da separação
   regular × atividade extra, com detalhe por curso. Não reimplementar no front.
3. `capturar_carteira_professores_mensal` — a terceira implementação, que grava o
   snapshot do dia 1º e tem precedência sobre o vivo. Sem ela, o snapshot de outubro
   sobrescreveria a correção.
4. `get_carteira_professores` (legada) — `total_alunos`, LAMK, EMLA e `tempo_medio_meses`.

Verificado pós-aplicação: Barra 282→**270** (238 turmas), CG 465→**430** (306, 1 professor
zerado), Recreio 405→**362** (318) — bate com a previsão. Legada: Will 26→**16**, com
LAMK 7 + EMLA 9 fechando no total. Composição do Will: 16 regular + 10 extra
(6 Minha Banda, 4 Power Kids). ACL das 4 sem `anon`.

**Front:** aba Professores → Carteira ganhou a coluna **"Atividade extra"**, no mesmo
padrão do badge de Trancados que já existia ali (`+N não conta`, borda tracejada, cor
sky para não confundir com o amber de trancado nem com o violet da carteira que conta),
com tooltip listando os cursos. ⚠️ O filtro da lista passou a aceitar
`total_alunos > 0 || atividade_extra > 0` — sem isso o Alan Samico (só banda) sumiria
da tela, que é o oposto de "sinalizar, não esconder". Build ok.

**Não precisou mexer nas 2 edges:** `gemini-relatorio-coordenacao` e
`gemini-ranking-professores` leem o número do banco — já saem corrigidas sozinhas.

### Pendências conscientes
- 👁️ **Aba Performance** só herdou o número certo; não ganhou a coluna de composição
  (a Carteira é a aba onde se olha composição). Avaliar se o Arthur quer lá também.
- ⚠️ **Sem teste automatizado**: a regra vive em SQL e o repo não tem harness de teste
  de banco. Query de checagem: comparar `carteira_alunos` da canônica contra
  `carteira_regular` da composição — devem ser iguais em toda competência sem snapshot.
- 👁️ **Texto dos relatórios** não foi alterado (ainda diz só "Carteira: N aluno(s)").
  Acrescentar "(não inclui atividade extra)" exige deploy de edge, e o
  `gemini-relatorio-coordenacao` mudou +76 linhas no remoto há pouco — exige conferir
  alinhamento produção × repo antes.
- 👁️ **Risco de evasão** (115 linhas) e **Health Score** herdam a carteira corrigida,
  mas o pilar de risco em si não foi auditado nesta rodada.

### Varredura do front: existe uma QUARTA implementação, e ela é código morto

`vw_kpis_professor_completo` calcula `carteira_alunos` como `count(*) from alunos where
status='ativo'` — sem filtro de banda, sem trancado, e com `avg(valor_parcela)` de ticket
(o cálculo marcado 🚫 LEGADO nas regras). **Não foi corrigida porque não tem consumidor:**
zero funções/views no banco a referenciam, e no front só `GestaoMensal/TabProfessores.tsx`,
que **ninguém importa** — o `GestaoMensalPage` renderiza `TabProfessoresNew`, e este usa
`buscarKpisProfessoresCanonicos` (RPC canônica, já corrigida). Mesmo padrão de
`TabRetencao.tsx`/`SnapshotDiario.tsx` já registrados no CLAUDE.md.

Consumidores do front que **herdam a correção sem tocar em código**:
`TabPerformanceProfessores`, `ProfessoresPage`, `ModalDetalhesProfessorPerformance`,
`GestaoMensal/TabProfessoresNew`, `useKPIsProfessor`, `professoresKpisCanonicos` e as
2 edges de relatório.

## 🔴 A coluna nova nasceu quebrada — dois defeitos que só a tela revelou (~16h BRT)

O Hugo abriu a aba: badge do Will **16 alunos** ✅, mas a coluna "Atividade extra"
mostrando **`—`** em vez de `+10`.

**Defeito 1 — permissão.** `get_carteira_professor_periodo_composicao_v1` nasceu
`SECURITY INVOKER` e chama `get_carteira_professor_periodo_detalhe_canonico_v1`, que
**não tem execute para `authenticated`** (só postgres/service_role). Dava
`permission denied` em runtime, o `consultarSupabaseOpcional` engolia, e a coluna ficava
vazia **sem erro visível**. ⚠️ Eu validei tudo como `service_role` — que ignora RLS e tem
execute em tudo, logo **não prova nada**. É a armadilha que o CLAUDE.md já registra duas
vezes (`get_agenda_dia` em 02/08, `emusys_disciplinas_catalogo` em 03/08) e ela se
repetiu igual.

**Defeito 2 — escopo, introduzido pela correção do 1.** Ao virar `SECURITY DEFINER`,
copiei o predicado de escopo das views de contratos vencendo:
`current_user in ('service_role','postgres')`. **Em DEFINER, `current_user` é o DONO da
função, não o chamador** — então o predicado liberava tudo. Medido: usuário
`cg@lamusic.com.br` (perfil unidade, Campo Grande) lia **as 3 unidades** e conseguia
pedir a Barra explicitamente (20 linhas). O discriminador certo é a **presença de claim
JWT** (`auth.uid() is null` = chamada interna de cron/service_role/outra DEFINER);
`auth.role()` segue não servindo pelo motivo já documentado.

⚠️ **O predicado das views NÃO é copiável para função DEFINER.** As views são
`security_invoker = false` mas `current_user` nelas ainda é o chamador; numa função
DEFINER, não.

**Validado nos 3 perfis (o que faltou na primeira vez):**

| Perfil | Vê | Pedindo a Barra |
|---|---|---|
| admin (lucianoalf) | Barra, CG, Recreio — Will 16 regular + 10 extra | ok |
| unidade (cg@lamusic) | **só Campo Grande** | **0 linhas** |
| service_role / interno | 3 unidades, 74 linhas | ok |

Commit `730be754`, direto na main. Migration `20260908180000` grava **guardas** que
falham se alguém devolver a função para INVOKER, reintroduzir `current_user` no escopo,
remover o discriminador de claim ou reabrir execute para `anon`.

⚠️ **Lição para a próxima**: build + tsc verdes e RPC testada como `service_role` não
dizem nada sobre uma tela. As duas falhas eram invisíveis fora do navegador.

## Badge por curso + tooltips de cálculo (~17h BRT)

Pedido do Hugo ao ver a tela: marcar na coluna **Curso** quais são de banda, e explicar
como MRR/Ticket são calculados. Os dois são o mesmo problema — **indicador cuja regra
não está ao alcance da mão vira pergunta para o time de dados, ou conclusão errada.**
O §9.2 já previa o badge ("banda é incluído, mas com flag para o front exibir badge"),
só nunca tinha chegado nesta lista.

- **`AlunoCarteiraCanonico.cursos_atividade_extra`** (lib `carteiraProfessorDetalheCanonica`):
  marcado por **`curso_id` + `is_projeto_banda`**, nunca por nome — nome de curso varia
  entre unidades e casar por texto reintroduziria o filtro frágil que a regra aposentou.
  Falha na consulta dos cursos **não derruba a carteira**: sem o conjunto, a lista
  aparece sem badge (degradação visível > marcação errada).
- **Badge "não conta"** por curso na lista expandida, com tooltip do efeito.
- **`ThComExplicacao`**: cabeçalhos de Alunos, Trancados, Atividade extra, MRR, Ticket,
  Média/Turma e Health Score passam a explicar a fórmula no hover — inclusive o "MRR ÷
  alunos que entram no ticket, nunca ÷ total", que é a armadilha registrada no código.

⚠️ **Quebrei 2 testes e o tsc não pegou.** Coloquei o filtro dentro de
`montarPessoasCarteira`, que não recebia o parâmetro — `cursosProjeto is not defined` em
runtime. `npm run build` e `tsc --noEmit` passaram **verdes**; quem pegou foi
`tests/healthScoreProfessorV3Consumers.test.mjs`, que **executa** a função em vez de
conferir texto. Confirmei comparando contra a base com `git stash`: era 23/3, foi para
21/5, voltou a **23/3** (as 3 restantes são pré-existentes).

Teste novo **`tests/carteiraAtividadeExtraBadge.test.mjs`** (4 casos, roda a função real):
pessoa com curso regular + banda no mesmo professor aparece 1× com só a banda marcada;
sem o conjunto nada é marcado; **proíbe por asserção** voltar a casar por nome de curso;
e exige que os 7 cabeçalhos expliquem o cálculo.

## Relatório mensal: total de evasões contava banda, rodapé carimbava a data errada (~17h BRT)

Clayton reclamou no grupo RELATÓRIOS DIÁRIOS RC de dois pontos do mensal de agosto do
Recreio: os itens 10 e 11 dos avisos prévios, e "São 28 saídas no mês de agosto, está
contando 37". Investigado, medido e corrigido — commit `eb8b6466`, migration
`20260908203000`, edge `relatorio-admin-whatsapp` **v119**.

### O que estava errado

**1. Total de evasões somava banda (37 onde o sistema diz 29).** O bloco EVASÕES publicava a
soma bruta enquanto o PRÓPRIO relatório calculava churn com 29 (`8,7% — 29 saídas em 334
pagantes`) e `dados_mensais.evasoes` também diz 29. As 8 a mais eram GarageBand/Power Kids —
e **7 dessas 8 pessoas seguem com matrícula ativa em curso regular** (só a Elena, Laura,
Helena, Elis, Ísis, Valentina e Lis; a 8ª, David Kayat, saiu de tudo). Contar isso como saída
diz que a escola perdeu aluno que não perdeu. `REGRAS-DE-NEGOCIO` §3.5 e §5 já mandavam
excluir; o relatório mensal era o único lugar que não aplicava.

**2. Rodapé carimbava a hora do ENVIO sobre dados do fechamento.** Dizia "Gerado em 08/09 às
14:43" com dados de 31/08. Foi o que permitiu o **mesmo mês ser enviado duas vezes** (04/09 com
44/30, 08/09 com 37/23) sem ninguém perceber que o segundo era retificação.

**3. Avisos prévios com 11 onde a base já tinha 9.** Manuela Borges e Maria Isabel foram
lançadas em 28 e 31/08 com `mes_saida` setembro (errado — as outras 9 da lista usam a régua
"primeiro mês fora", que para aviso de agosto dá outubro). A Fefe arquivou as duas em 04/09
15:19-15:21 e relançou com outubro. O fechamento é de 01/09 e congelou as antigas; as
retificações v5-v8 são append-only e **nunca remontam o bloco que não declaram**.

### O que foi feito

- Total passa a ser o mesmo número do churn. Banda/bolsista/2º curso/transferência vão para um
  bloco **"Não entram no total (regra da casa)"**, e a soma fecha (23 + 6 = 29).
- Quem fica fora do total **continua na lista, marcado** — sumir com a linha esconderia da
  coordenação quem saiu da banda, que é justamente o que ela precisa saber.
- Rodapé separa `📅 Dados do fechamento de DD/MM` de `📤 Enviado em DD/MM às HH:MM`.
- Migration retifica os avisos de ago/2026 do Recreio: **v9, 11 → 9**, com guarda de escopo
  (aborta se qualquer outra chave do payload mudar) e guarda de quantidade (aborta se ≠ 9).

### Armadilhas que só o dado real revelou

⚠️ **`payload.capturado_em` ≠ coluna `capturado_em`** do snapshot. O payload guarda quando os
dados foram congelados (ago/2026: `2026-09-01T01:00Z`); a coluna, quando a linha foi gravada
(`19:34Z`). O formatador só enxerga o payload — e é o certo a usar.

⚠️ **A conversão de fuso não é detalhe.** `capturado_em` vem em UTC e cai depois das 21h BRT
nas três competências: sem converter, agosto diria "fechamento de **01/09**" — data do mês
seguinte, pior que o defeito original. Com fuso: 30/06, 31/07, 31/08.

⚠️ **Lista e total são validados um contra o outro** (`validarQuantidade`). Corrigir a lista
sem corrigir `resumo.avisos_previos` não faz o relatório sair errado — faz ele **parar de
sair**, com `RELATORIO_ADMIN_MENSAL_DIVERGENTE`.

⚠️ **Não existe disparo automático do mensal.** Procurei cron na la-hq (9 linhas de relatório
no crontab do `sol`, nenhuma mensal) e scripts que escrevam na fila (só worker e aviso prévio).
É o botão "Enviar para o grupo" do `ModalRelatorio` → RPC `sol_hermes_report_enqueue` →
`process-sol-report-queue.py`. **Os dois envios de agosto foram cliques manuais.**

⚠️ **Deploy pela CLI, não pelo MCP.** `npx supabase functions deploy --use-api` lê os 6 arquivos
do disco e respeita o `verify_jwt = false` do `config.toml` — o MCP resetaria para `true`.
Conferido antes: produção (v118) era **byte a byte igual ao repo em HEAD~1**; depois (v119),
igual a HEAD nos 6 arquivos.

### Pendente

🔴 **LAPE-24** — 7 evasões de agosto contadas em setembro. A API do Emusys mostra última aula
em 27-29/08 e zero aula em setembro, mas os 7 têm parcela 09/2026 emitida e aviso prévio com
`mes_saida` setembro. É decisão de regra (Alf/Luciano), não técnica: muda churn e Fideliza+ de
dois meses. Hoje setembro/Recreio tem 9 evasões, **7 delas de agosto**.

## Evasão: terceira checagem, nada mudou (~BRT)

Hugo perguntou de novo "já teve progresso na evasão? alguma resposta automática?" — mesma
pergunta de 04/09 e 05/09. Reconferido no banco: **nada mudou em nenhum campo.**

| sinal | 04/09 | 05/09 | 08/09 |
|---|---|---|---|
| agradecimentos enviados | 0 | 0 | **0** |
| log `agradecimento_evasao` | 2 recusas de teste (02/09) | idem | **as mesmas 2** |
| classificações da IA | 0 | 0 | **0** |
| respostas de evasão | nenhuma desde 31/08 | idem | **idem** |
| último 1º toque | 26/08 | 26/08 | **26/08 (13 dias)** |
| última repescagem | 31/08 14:09 | idem | **idem** |
| total de pesquisas | 51 | 51 | **51** |
| kill switch | ligado | ligado | **ligado** |
| prontas esperando clique | 102 | 102 | **103** (72 ago + 31 set) |

O gargalo segue sendo o 1º toque manual. A fila cresceu 1 e nada saiu.

**Observação sobre o processo, não sobre o dado:** esta é a terceira vez em cinco dias que a
única forma de saber o estado foi o Hugo perguntar e eu rodar as queries à mão. É exatamente o
custo que motivou o pedido do vigia (conversa de 05/09) — que ficou parado numa pergunta minha
sem resposta: se o desenho de três vereditos (verde / vermelho / cinza-nunca-exercitado) com a
IA só na borda serve. **Decisão pendente do Hugo**, junto com onde o vigia mora e para onde o
alerta vai.

## Relatório Gerencial com IA: mesmo defeito do total, segundo consumidor (~18h30 BRT)

Print do Clayton do **Relatório Gerencial com IA** mostrando, no mesmo bloco,
`Churn: 8,68%` e `Total de saídas: 37`. E 8,68% é **29 ÷ 334 pagantes** — o bloco se
contradizia na própria tela.

**Não era a correção da tarde falhando.** Aquela foi na edge `relatorio-admin-whatsapp`
(v119). Este relatório é a edge **`gemini-relatorio-gerencial`**, com formatador próprio,
que imprimia `indicadores_retencao.total_evasoes` cru.

**Decisão: NÃO mexer na raiz.** O impulso era corrigir o campo em
`get_relatorio_admin_mensal_rico_base_v1` (linha 295 lê `resumo->>'evasoes'` = 37,
enquanto a linha 328 calcula o churn com `evasoes_base + nao_renovacoes` = 29). Só que
37 tem significado próprio: o formatador do WhatsApp publica esse número **rotulado**
("Saídas totais: 37 (29 pagantes + 8 banda)") e o **valida contra a lista**
(`validarQuantidade`, linha 312). Trocá-lo por 29 faria o relatório do Clayton parar de
sair com `RELATORIO_ADMIN_MENSAL_DIVERGENTE:saidas_totais`. O defeito era o **rótulo** no
Gerencial, não o campo.

**O que mudou** (commit `59e2b18d`, edges v106 e v120):

- Bloco RETENÇÃO publica `Total de saídas: 29` (23 interrompidos + 6 não renovações) e
  manda banda/bolsista/2º curso/transferência para "Não entram no total (regra da casa)".
- ⚠️ **O campo cru também ia para dentro do prompt da IA** (`resumoParaIA`) — a análise
  escrita raciocinava sobre 37. Passa a receber `saidas_que_contam` e
  `saidas_fora_do_total`.
- A classificação virou módulo único, `_shared/composicao-saidas-mensais.ts`, e o
  formatador do WhatsApp passou a importá-la em vez de manter cópia local. Duas cópias da
  mesma regra foi como o 37 sobreviveu à correção da tarde.
- Divergência entre lista e total vira aviso logado, nunca silêncio.

**Verificações:** 7 testes novos com os números medidos em produção; os **8 do formatador
do WhatsApp seguem passando** (era o risco real da refatoração); `deno check` limpo;
produção conferida byte a byte contra o repo **antes** de editar e **depois** de deployar,
nos 3 + 7 arquivos. `verify_jwt` preservado (`true` no gerencial, `false` no admin) —
deploy pela CLI com `--use-api`.

⚠️ **Armadilha do próprio script de conferência:** a 1ª versão casava arquivo por
`basename` e `os.walk` devolveu o `index.ts` de outra função, acusando divergência falsa
logo após o deploy. Comparar por `<pasta>/<arquivo>`, nunca só pelo nome.

## Emusys diz 28 saídas, nosso canônico diz 29 (~18h BRT)

Pergunta do Hugo com o print do Painel de Métricas do Emusys (Recreio/2026):
"Saída de Alunos" agosto = **28**; "Alunos Ativos (fim do mês)" = **344**, que bate
exatamente com o nosso.

**A linha que sobra é a Layara Sales Magalhães** (evasão lançada em 06/08). No espelho da
API, sincronizado em 07/09 23:20, ela está **`status_matricula = 'ativa'`**, com contrato
até **20/03/2027** e aula prevista até 10/04/2027 — no Emusys ela não saiu. Tirando ela:
28. As 29 são pessoas distintas (não é duplicata) e **nenhuma** tem matrícula ativa no
nosso lado, então a hipótese de "saiu de um curso e ficou noutro" foi descartada.

⚠️ **Bater 28 não valida o critério.** Do outro lado do fluxo a diferença é maior:
entradas, nós **23** contra **26** do Emusys. Os dois sistemas não contam a mesma coisa, e
o total de ativos bate porque vem de contagem direta da base, não do fluxo — não serve de
prova. Fica como pendência de apuração, separada da correção acima.

## A evasão que não houve — e dois defeitos no caminho dela (~19h BRT)

Partiu do print do Emusys que o Hugo mandou: "Saída de Alunos" agosto/Recreio = **28**,
contra 29 no nosso canônico. Era o mesmo 28 que o Clayton dissera no áudio de ontem e que
eu não tinha conseguido reproduzir testando sete formas de contar. **Não era fórmula: era
um registro errado.**

**Layara Sales Magalhães** (aluno 1604) foi lançada como evasão **à mão** em 06/08 e não
saiu. Provas, nesta ordem:

- presença **afirmada em 22/08** — dezesseis dias depois da evasão;
- mensalidades de **08 e 09/2026 pagas** (R$ 430 cada, matrícula Emusys 1428);
- jornada `ativa`, **27 aulas futuras**, contrato até 10/04/2027;
- **aviso prévio do próprio Emusys** em 08/09 (`emusys_aviso_previo_id` 412) para sair em
  **outubro**.

⚠️ **É caso único.** Das 29 saídas, a única com presença posterior à data de saída. Carlos
Yan e Daniel Duque aparecem no sinal financeiro (parcela de setembro paga) mas estão
`finalizada`, com 0 e 1 aula futura e nenhuma presença depois — **parcela paga não é aluno
ativo**, e foi por isso que o sinal escolhido foi a PRESENÇA.

### Dois defeitos da mesma leva de 05/09 bloquearam o caminho

Ambos vieram de `origem_registro` ser acrescentada a duas tabelas sem espelho:

1. 🔴 **`arquivar_movimentacao_admin` quebrada** desde 05/09. A lixeira não recebeu a
   coluna e a RPC copiava por POSIÇÃO (`select v_linha.*`) → 42 valores para 41 lugares.
   Último arquivamento bem-sucedido: **04/09 16:22**. Com o DELETE bloqueado por trigger,
   a equipe ficou **4 dias sem conseguir excluir lançamento pela tela** — em três telas
   (`AdministrativoPage`, `TabelaAvisosVencidos`, `PlanilhaRetencao`), com 29 arquivamentos
   feitos por pessoas entre 12/08 e 04/09 (Vitória 16, Fefe 6, Gabi 6, Arthur 1). **Ninguém
   reportou.** É o mesmo beco da Catarina Petrolongo, agora sem nem a saída de excluir.
2. 🔴 **`sync_aluno_to_leads` com VALUES desalinhados** — `'sync_aluno'` na posição de
   `created_at`, dando `22007` em INSERT de aluno e em reativação (`evadido` → `ativo`).
   Não mordeu ninguém porque os 5 alunos criados desde 05/09 acharam lead e foram pelo
   outro ramo; o último lead `sync_aluno` é de 04/09 16:18, véspera do defeito.

### O que foi feito

| Frente | Commit |
|---|---|
| Evasão arquivada + status → ativo + ordem do trigger | `17ae72ef` |
| Lixeira: cópia por nome, recusa `COLUNA_SEM_ESPELHO`, teste | `7f186006` |
| Retificação do fechamento de agosto | `93080fc4` |

**Resultado no relatório de agosto:** saídas **29 → 28** (22 interrompidos + 6 não
renovações), churn **8,68% → 8,38%**, MRR perdido **11.937,50 → 11.507,50**. Avisos seguem
9, ticket 433,38.

### Armadilhas medidas

- 🔴 **A retificação do gerencial nasceu INERTE.** Depois das partes 1 e 2 a lista caiu
  para 30 e o resumo para 36, mas **churn e `mrr_perdido` não se moveram**: o snapshot
  `relatorio_admin_mensal` declara a fonte gerencial por **ID E HASH** em
  `fontes.relatorio_gerencial`, e a função lê AQUELE snapshot, não "o mais recente". Foi
  preciso uma **terceira** parte só para religar o ponteiro. A amarração existe de
  propósito — o certo é mover o ponteiro, nunca afrouxar a checagem. Sem conferir o número
  final eu teria declarado pronto o que não estava.
- ⚠️ **O denominador NÃO foi tocado, de propósito.** `alunos_pagantes` (334) e ticket ficam
  como estão. Medido hoje, já com a Layara corrigida: **337 pessoas ativas contra 339** no
  espelho do Emusys — o denominador tem erro próprio de 2 pessoas. Corrigir por causa de
  uma seria escolher arbitrariamente o erro encontrado hoje. O **"344 = 344" que batia com
  o Emusys em agosto era coincidência, não validação.** Custo assumido: churn conservador
  (28/334 = 8,38% em vez de 28/335 = 8,36%), 0,02 ponto contra os 0,30 corrigidos.
- ⚠️ **`anulado` não serviria aqui:** é respeitado por `movimentacoes_admin_vigentes`, mas
  `recalcular_dados_mensais_unguarded` e `get_kpis_alunos_canonicos_base_p01q` leem a
  TABELA CRUA. Arquivar resolve em todos os consumidores de uma vez.
- ⚠️ **Falso positivo do meu próprio teste:** a 1ª versão de
  `arquivarMovimentacaoEspelhoColunas` reprovou o SQL por achar `select v_linha.*` **no
  comentário que explica o defeito**. Corrigi o teste, não o SQL — substância, não
  vocabulário. E o provei ao contrário: removendo o espelho ele falha apontando a coluna e
  a migration culpada pelo nome.

**Pendências (nenhuma de código):** apurar os 2 ativos de diferença contra o Emusys;
divergência de **entradas** (23 nosso × 26 Emusys); **18 alunos `evadido` com aviso prévio
vigente**; e decidir se agosto será reenviado ao grupo — seria a **terceira** versão (04/09
com 44, 08/09 com 37, agora 28).
