# 2026-06-23

## Regra da parcela comercial: desconto fixo FORA da conta (`cheio − cond`)

Confirmado na fonte: o Luciano mudou a fórmula da parcela (commit `befdbbc`): **`valor_parcela = valor_cheio − desconto_condicional`**. O `desconto_fixo` fica REGISTRADO na coluna `alunos.desconto_fixo` mas **NÃO entra** na parcela comercial (é desconto de pontualidade, auditado à parte). Regra anterior (até 22/06) era `cheio − fixo − cond` — obsoleta.

- **Backfill (banco, já em produção):** 535 alunos ativos/trancados (CG 399, Barra 116, Recreio 20) estavam na fórmula antiga → recalculados `valor_parcela = cheio − cond`. `valor_cheio`/`desconto_fixo`/`desconto_condicional` **preservados**. Auditoria pré-aplicação: **0 de banda, 0 zerados/negativos, 0 travados** (`matriculas_campos_fixados`). Incluiu os 4 bolsistas parciais de CG (decisão Hugo: tirar de todo mundo). Bolsista parcial: `conta_como_pagante=false`, `entra_ticket_medio=false` (excluído de ticket/pagantes), então não distorce KPIs.
- **Bug do modal de mensalidade (`TabelaAlunos.tsx`):** `salvarMensalidade` + display calculavam `cheio − fixo − cond` → gravavam o derivado errado e **desfaziam o backfill a cada save**. Corrigido p/ `cheio − cond`; campo virou "Desconto fixo (registrado — não entra)" + texto explicativo. Era o ÚNICO ponto do frontend com a fórmula antiga (edges `sync`/`processar` matrícula já estavam certas).
- `valor_parcela` é coluna **materializada/derivada** (`numeric(10,2)`) — por isso mudar a regra exige backfill (não recalcula sozinho). As 4 colunas (`valor_cheio`, `desconto_fixo`, `desconto_condicional`, `valor_parcela`) são dedicadas em `alunos`. Regra documentada em `docs/METRICAS.md`.

---

## Autoreply nas caixas de disparo (Meta API oficial) — migrado p/ caixa + religado

Pendência: caixas de campanha (Meta Cloud API) recebem mensagens de quem acha que é atendimento, sem resposta. Já havia um autoreply no código, mas **nunca disparou** (0 envios em 735 inbound).

### Diagnóstico (verificado na fonte)
- Autoreply morava em `agentes.auto_reply_message` e era triplo-bloqueado: (1) nenhum agente tinha mensagem; (2) buscava por `unidade_id` mas os 2 números Meta têm `unidade_id=NULL`; (3) trava "só responde quem recebeu campanha" silenciava 39% (105/270 contatos).

### Implementação
- **Migration** `add_autoreply_numeros_meta`: colunas `auto_reply_ativo` (bool, default false) + `auto_reply_message` (text) em `numeros_meta`. Default = texto padrão com as 3 unidades.
- **Webhook `meta-webhook-campanhas` v11**: lê autoreply da própria caixa, responde QUALQUER inbound (sem trava de campanha), **debounce 10 min/contato** (último outbound `metadata->>auto_reply='true'`). `verify_jwt:false` preservado. ⚠️ deploy via MCP achata `_shared` → imports `./_shared/`.
- **Frontend** `ConfigTab` (modal Número Meta): toggle "Resposta automática (canal só de avisos)" + textarea condicional. Hook `useNumerosMeta` + tipos atualizados. Campo órfão de autoreply removido do `ModalEditarAgente`.
- **Ativado** na caixa "Envio de Avisos (Sem Agente)" (`931b0c24`); "Conta Principal" fica off. typecheck limpo nos arquivos tocados.
- Decisões Hugo: responde a todos / debounce 10min / texto editável na UI. Telefones WhatsApp confirmados: CG (21)96552-9851, Recreio (21)3955-1135, Barra (21)96957-5619.
- **Pendente:** commit + push (frontend só vai pro ar via deploy Vercel).

---

## Conciliação Emusys — novos tipos de divergência + filtros/paginação/ações (BRT)

Contexto: a verificação contra a API do Emusys (CG) achou inconsistências de classificação/valor que precisam de decisão humana, não correção automática. Levadas para a aba de Conciliação (fila), com a UI ganhando filtros, paginação e mais ações.

### Backend — `sync-matriculas-emusys` (deploy v5, dry-run)
- Motor `reconciliar()` refatorado: retorna `{ upd (AUTO), divergencias[] }` — antes era 1 coisa por aluno (early-return). Agora AUTO e fila coexistem; vários tipos por aluno.
- **`valor_divergente`**: quando a fonte é ambígua (`liquido < 0`/`cheio <= 0` = `desconto_fixo` embutido no `valor_mensalidade`), **não** auto-aplica; enfileira com sugestão `efetivo = cheio − cond`. Só enfileira se `efetivo > 0` (bolsista integral com mensal 0/fixo cheio → efetivo 0 → ignora, não-pagante legítimo). Bug corrigido: motor antigo aplicaria `valor_parcela` negativo.
- **`classificacao_divergente`**: bolsista→REGULAR só quando paga o **cheio integral** (`liquido === cheio`, sem desconto); REGULAR→bolsista quando API `bolsa=true` (INT se efetivo≤0, PARC se >0). O flag `bolsa` da API **não é confiável** sozinho.
- **Dedup**: `(aluno|tipo)` já com decisão humana não é reenfileirado (respeita a decisão).
- Popular (dry-run, 3 unidades): CG `classificacao_divergente=4` (Adriana/Lavynea/Olivia→REGULAR; Sofia→BOLSISTA_PARC, paga ~190), `valor_divergente=12`; Recreio `valor_divergente=2`; Barra 0. **Nada escrito em `alunos`** (dry-run).

### Frontend — `ConciliacaoMatriculas.tsx` (reescrito)
- Filtros: busca por nome, chips de tipo (cards de resumo clicáveis), Select de unidade (só no modo "todos").
- Paginação client-side (20/pág).
- Ações por linha (DropdownMenu): Aplicar dado da API, Editar valor (modal), Reclassificar tipo (modal), Manter, Ignorar.
- Ações em lote: checkbox + barra flutuante (Aplicar API / Ignorar).
- `resolverDivergencia()`: grava no aluno → fixa o campo (`matriculas_campos_fixados`) → registra decisão → marca `resolvido`. O campo fixado bloqueia sobrescrita futura da varredura.
- typecheck + `vite build` limpos.

### Cron (decisão Hugo = opção A "só fila")
- 3 jobs pg_cron `sync-matriculas-{cg,recreio,barra}` (jobids 24/25/26), 1×/dia defasados 20min (02:00/02:20/02:40 UTC), `net.http_post` + `?u=<un>` + anon key. **Roda em dry-run** → nunca toca em `alunos`, só popula a conciliação. v6: dry-run não grava log de AUTO. Ligar AUTO no futuro = `SYNC_MATRICULAS_DRYRUN=false`.

### Pendente
- Usuário testar a UI (aplicar uma decisão real); decidir Adriana/Lavynea/Olivia→REGULAR, Sofia→PARC.
- Commit (não feito ainda — aguardando OK).

---

## Sucesso do Aluno — Aba "Respostas" (análise + registro da pesquisa pós-1ª aula) — feature completa

Fluxo completo brainstorming → spec → plano → execução subagent-driven (6 tasks + revisão por task + revisão final Opus). Em produção em `main` (commits `ae3bad6..beb0892`). Spec: `docs/superpowers/specs/2026-06-23-analise-respostas-pesquisa-sucesso-aluno-design.md`; plano: `docs/superpowers/plans/2026-06-23-aba-respostas-pesquisa-sucesso-aluno.md`.

### Backend (sem migration — decisão Hugo: começar sem status "tratado")
- RPC **`get_analise_pesquisas(p_unidade_id,p_data_inicio,p_data_fim)→jsonb`**: KPIs (enviadas, taxa de resposta, nota média, distribuição 1–5★), recortes `por_professor`/`por_unidade`/`por_curso`, `evolucao` semanal. Filtra `tipo='pos_primeira_aula'`+`enviado_ok`, período por `enviado_em`, evolução por `respondido_em` (com guard `respondido_em IS NOT NULL`, fix da revisão final `ee65b52`). Recorte via vínculo atual do aluno (`professor_atual_id`/`curso_id`).
- RPC **`get_respostas_pesquisa(...)→linhas`**: lista individual (aluno, nota, curso, professor, unidade, datas, `remote_jid`). Ambas deployadas via MCP e validadas com fixtures.

### Frontend
- Hook `useAnalisePesquisas` (chama as 2 RPCs em paralelo) + componente `RespostasPesquisaTab` (KPIs, distribuição em barras, evolução Recharts, recortes, tabela de registro com notas ≤2★ em vermelho).
- Sub-aba **"Respostas"** adicionada em `PesquisasTab` (Acompanhamento → Pesquisas).
- **Deep-link "abrir conversa"** (Task 6): `CaixaEntradaTab` ganhou `alunoIdInicial?` (pré-seleção via `useEffect`+guard `useRef`); prop chain `SucessoClientePage → TabSucessoAluno → PesquisasTab → RespostasPesquisaTab`. `SucessoClientePage` troca a aba para 'caixa' e passa o aluno.
- `npm run build` OK.

### Conhecidos (fast-follow, não bloqueantes)
- Evolução por `respondido_em` × filtro por `enviado_em` (pode mostrar semana fora do mês). Recortes agrupam por nome (homônimos). Professor/curso = vínculo atual, não snapshot da 1ª aula. Sem status "tratado" nem feedback textual. Falta validação visual no navegador pelo Hugo (tabela `pesquisas_whatsapp` zerada hoje → estado vazio).

### Lançamento manual de respostas (dúvida Fabíola — respostas coletadas fora do sistema)
- **Migration `pesquisa_resposta_manual`**: coluna `pesquisas_whatsapp.manual` (bool default false) + RPC `registrar_resposta_pesquisa_manual(p_aluno_id,p_nota,p_data)` SECURITY DEFINER (RLS ativo sem policy de INSERT). Grava `tipo='pos_primeira_aula'`, `enviado_em=respondido_em`=data@12h BRT (15h UTC, p/ não vazar de dia), `enviado_ok=true`, `manual=true`, `unidade_id` puxado do aluno. Como as RPCs de análise derivam curso/professor/unidade do próprio aluno, basta o `aluno_id` certo.
- **Front**: `ModalLancarRespostaManual.tsx` (busca aluno por nome `ilike` debounce 300ms, limitada à unidade atual; seletor de estrelas 1-5 com hover; date picker default hoje, max hoje). Botão "+ Lançar resposta manual" no header da `RespostasPesquisaTab`; `onSaved` chama `recarregar` do hook. typecheck limpo nos arquivos tocados.
- **Pendente**: commit + push (vai pro ar via deploy Vercel).

### Dúvida Fabíola — relatório de contrato assinado/não assinado (Áudio 2)
- API Emusys `GET /matriculas` traz `contrato_atual` mas **NÃO expõe status de assinatura** (assinado/não assinado). Inviável puxar via API. O Emusys já tem relatório nativo: `Relatórios → Alunos → Segmentações → "Alunos sem Contrato Assinado"` — é onde a Fabíola já vê. Sem ação (não urgente, sem ganho em replicar).

### 🔴 BUG corrigido — captura da resposta não estava conectada (commit `eab755d`, webhook v48)
- **Descoberta:** ao perguntar se o clique do aluno popula a aba, verifiquei na fonte: `webhook-whatsapp-inbox` recebia o `buttonOrListid` mas só registrava como mensagem de texto "↩️ ..." — **NÃO chamava `processar-resposta-pesquisa`** (o comentário da edge dizia que sim, mas o wiring não existia / foi removido). Logs 24h: edge nunca invocada. Resultado: aluno respondia, mas `nota` nunca era gravada → aba sempre vazia.
- **Decisão (Hugo descartou trigger):** trigger em `admin_mensagens` seria frágil (o id da opção não vai pra coluna estruturada, só pro texto) e perderia o aviso ao gerente. Optado por restaurar o elo: webhook invoca a edge pronta (1 bloco, idempotente).
- **Fix:** `webhook-whatsapp-inbox` agora faz `supabase.functions.invoke('processar-resposta-pesquisa', { body: payload })` quando `msg.buttonOrListid` existe (após o handler de evasão, sem `continue` — resposta também fica visível na caixa). Deploy via **CLI** (importa `_shared/uazapi.ts`), v48, `verify_jwt=false` preservado.
- **Validado E2E real:** pesquisa plantada apontando p/ número do Hugo → enviada via `/send/menu` → Hugo clicou "Amei" → webhook → nota **5** gravada em `pesquisas_whatsapp` ✅. Fixture limpa.
- **Achado extra:** `unidades.telefone_gerente` = NULL nas 3 unidades → o aviso ao gerente (parte da edge) **nunca dispara** hoje. Registrar p/ cadastrar os telefones se quiserem essa notificação.

## 🔴 BUG corrigido — `valor_parcela` da edge ignorava o desconto fixo

- **Contexto:** o Hugo notou que o cron de sync de matrículas não rodou hoje. Investigação: jobids 24/25/26 **nunca executaram** (zero em `cron.job_run_details`) — foram criados após a janela diária (02:00–02:40 UTC); 1ª execução real = 23:00 BRT. O que "rodou ontem" foi **disparo manual** (22/06 19h + 23/06 00h), e **com `SYNC_MATRICULAS_DRYRUN=false`** (aplicando): 1419 reconciliações AUTO em `alunos` + 95 divergências na fila.
- **Achado (auditoria por amostra):** a edge calculava `valor_parcela = cheio − cond`, **ignorando o `desconto_fixo`**. Regra correta (confirmada pelo Hugo + 5 casos com os 2 descontos): **`valor_parcela = valor_cheio − desconto_fixo − desconto_condicional`** (parcela = cheio menos TODOS os descontos). O backfill via psql de 22/06 já usava a fórmula certa → daí a "colcha de retalhos" (edge errada × backfill certo no mesmo campo).
- **Tamanho real:** só **20 ativos** errados (CG 11, Barra 5, Recreio 4), todos por ignorar o fixo (parcela mais cara). Minha 1ª estimativa (~650) era falso-positivo por comparar com a fórmula errada.
- **Correção (2 passos, autorizada):**
  1. **UPDATE dos 20** (`valor_parcela = cheio − fixo − cond`, guard `>= 0`, respeita `matriculas_campos_fixados`). `BEGIN/COMMIT`, 20 linhas, 0 errados restantes. Nenhum vira negativo. Caso `desconto_fixo` negativo (acréscimo, ex. Davi 480→523) tratado certo pela fórmula.
  2. **Edge `sync-matriculas-emusys` corrigida + deploy (v8)**: `parcela = cheio − fixo − cond` (substitui `parcelaComercial`/`liquidoFinanceiro` por uma única `parcela`), no AUTO, na guarda de negativo (→ fila `valor_divergente`) e na classificação bolsa/REGULAR. Continua em **dry-run** (`SYNC_MATRICULAS_DRYRUN=true`, setado hoje 13:12 BRT).
- **Caso a verificar:** Caio Vinícius (Recreio) tem 2 linhas idênticas — possível duplicata de matrícula (arquivar) vs 2º curso.

## Backfill — ids do Emusys no aluno (interliga lead↔aluno↔experimental)
- Investigação da fila de conciliação de **experimentais**: causa raiz = RPC `get_conciliacao_experimentais_v2` calcula presença via `aluno_presenca.aluno_id`, mas 81% das experimentais são de lead com `aluno_id` NULL → falso "sem presença". (Camada 1 = RPC confiar no `lead_experimentais.status`, ainda PENDENTE.)
- Decidido interligar por **id do Emusys** (não por telefone — simulação provou telefone falha: 64% sem casar, 18% erro). Criada coluna `alunos.emusys_lead_id`; backfill aluno-a-aluno via `/matriculas` casando por (unidade+`emusys_matricula_id`): `emusys_student_id` + `emusys_lead_id` em **1174/1176** alunos. Gabarito 10/10. 100% aditivo (não tocou valor/status/tipo/curso). Detalhes: `.claude/memory/pendencias-2026-06-20-costura-experimental.md`.

## Timeline de Pesquisas do Aluno (pedido Fabíola) — feature completa

Fluxo brainstorming → spec → plano → execução subagent-driven (6 tasks + revisão). Régua de marcos **1ª aula → 3 meses → evasão** na ficha de detalhe do aluno. Spec: `docs/superpowers/specs/2026-06-23-timeline-pesquisas-aluno-design.md`; plano: `docs/superpowers/plans/2026-06-23-timeline-pesquisas-aluno.md`.

### Backend (migration `20260623_timeline_pesquisas_aluno.sql`)
- Colunas `comentario` (text) e `status` (text) em `pesquisas_whatsapp`; backfill `status='respondida'` (tem nota) / `'pendente'`.
- **RPC `get_timeline_pesquisas_aluno(p_aluno_id)→jsonb`**: régua fixa 3 marcos (`pos_primeira_aula` ativo; `tres_meses`/`evasao` `ativo=false`="Em breve"); `DISTINCT ON (tipo)` (respondida antes de pendente, depois mais recente); SECURITY DEFINER.
- **RPC `registrar_resposta_pesquisa_manual(p_aluno_id, p_data, p_tipo, p_nota, p_comentario, p_nao_respondeu)→uuid`** (substitui a de 3 params): valida tipo/nota; **upsert lógico por (aluno_id, tipo)** (atualiza o registro existente — inclusive o `pendente` criado pela edge de envio — só insere se não houver, nunca duplica); `status` derivado.
- 🔴 **Fix crítico**: `data_matricula` é NOT NULL e **916 alunos têm `data_inicio_contrato` NULL** → o INSERT usa `COALESCE(data_inicio_contrato, p_data)` para não quebrar. (A RPC manual de ontem nunca tinha sido executada de verdade — tabela vazia — então o problema só apareceu agora.)
- Edge **`processar-resposta-pesquisa` v4**: o UPDATE da captura por WhatsApp agora grava `status='respondida'`.

### Frontend
- **`TimelinePesquisasAluno`** (`alunoId`+`alunoNome`): timeline vertical; por marco mostra estrelas+comentário (respondida), selo "Não respondeu", "Aguardando resposta"+Registrar (pendente), "Sem registro"+Registrar (1ª aula vazia), "Em breve" (3 meses/evasão). Botão Registrar/Editar abre o modal já com aluno+marco fixos.
- **`ModalLancarRespostaManual`** generalizado: props `alunoFixo`/`tipoFixo` (modo contextual sem busca) + Textarea de comentário + toggle "Não respondeu". Modo busca (aba Respostas) mantido retrocompatível.
- Embutido em **`ModalDetalhesSucessoAluno`** (seção "Pesquisas") e **`ModalFichaAluno`** do módulo Alunos (nova aba "Pesquisas", `grid-cols-7`).
- `npm run build` OK.

### Fora de escopo (futuro)
Disparo das pesquisas de 3 meses/evasão; captura de comentário pelo WhatsApp; unificar a pesquisa de evasão atual (texto/áudio) na timeline.

---

## Correção da RPC de conciliação de experimentais (Camada 1)
- RPC `get_conciliacao_experimentais_v2` corrigida (migration `fix_conciliacao_experimentais_presenca_via_status`): presença via `status='experimental_realizada'` (não só `aluno_presenca`, que falhava p/ 81% dos leads sem `aluno_id`); denominador = realizadas confirmadas; numerador via `emusys_lead_id` (backfill de hoje). Fila de experimentais ~250 falsos "sem presença" → 2 reais. Taxa Exp→Mat destravada (mar 17% / abr 34% / mai 29% / jun 24%). Sem mudança de frontend.

---

## Sync de presença antecipado p/ 19:50 BRT (relatório das 20h)

Cleiton (consultor Recreio) reportou aula experimental de hoje com presença que não atualizou no relatório. Investigado sem alterar nada primeiro.

### Diagnóstico
- Aula = **Alice Pajunk** (Recreio, Canto, 17h, prof Lohana). `leads` virou `experimental_realizada` (17:57 BRT, via pipeline manual), mas `lead_experimentais` (fonte do relatório) ficou `experimental_agendada`.
- **Causa = timing, não bug:** `lead_experimentais` só é sincronizada pelo cron `sync-presenca-emusys`, que rodava 1×/dia às 22h. A última execução foi 22:40 de ontem — *antes* da aula existir. O cron das 22:40 de hoje resolveria sozinho.
- **Simulação read-only** (curl direto na API Emusys `/aulas/`, token Recreio): aula id 804422, 23/06 17:00, não cancelada, **Alice presença=`presente`** ✅. A lógica `confirmarExperimentais` casaria por data+prof+unidade+categoria → confirmaria. Provado que ia funcionar à noite.

### Mudança aplicada — 6 jobs (10min antes do fechamento, por unidade e por dia)
Decisão Hugo: cada unidade roda 10min antes de fechar, com horário diferente em semana vs sábado. Crons existentes (18/19/20) viraram os de **semana**; criados 3 novos (27/28/29) para **sábado**. Domingo não roda (sem aula).

| Unidade | Fecha (sem/sáb) | Cron semana (BRT) | Cron sábado (BRT) |
|---|---|---|---|
| Barra | 20h / 16h | 19:50 (`50 22 * * 1-5`) | 15:50 (`50 18 * * 6`) |
| Campo Grande | 21h / 15h | 20:50 (`50 23 * * 1-5`) | 14:50 (`50 17 * * 6`) |
| Recreio | 21h / 15h | 20:52 (`52 23 * * 1-5`) | 14:52 (`52 17 * * 6`) |

- CG/Recreio fecham no mesmo horário → Recreio +2min só p/ escalonar (tokens distintos não conflitam mesmo simultâneos).
- Dado real (14d): aulas começam de hora em hora (a partir das 19h só **19:00** e **20:00**), então nenhuma começa no horário do cron → captura o dia todo sem perder nada; o que começar depois entra no sync seguinte (janela 5-7d). Ordem presença→grade horária (01:30 UTC) preservada.
- (1º rascunho do dia foi 19:50/52/54 todo dia; substituído por este por dia da semana.)

---

## Incidente: relatório diário do John falhou — caixa "Sol" desconectada

John reportou erro ao enviar o relatório diário: *"session status is not as expected. Try again later or restart the session."* — mensagem da própria UAZAPI quando a instância não está `connected`.

### Diagnóstico
- Caixa **"Sol"** (`whatsapp_caixas` id 2, `funcao=sistema`) é **provedor `waha`** (não UAZAPI!) — `waha_url=https://waha.agenticflowio.com.br`, `waha_session=5_198_552139554415`. ⚠️ A caixa tem campos `uazapi_url/uazapi_token` preenchidos (instância UAZAPI homônima, mesmo número) que **enganam**: o `relatorio-admin-whatsapp` roteia pelo `provedor`, então o status real está no WAHA. Checar status sempre via `GET {waha_url}/api/sessions/{waha_session}` header `X-Api-Key`.
- A sessão WAHA estava caída (`disconnected by API` desde 29/05) → erro de envio do relatório.
- `relatorio-admin-whatsapp` resolve a caixa por `funcao: 'sistema'` → cai na "Sol". Outras: "Sol - Sucesso do Aluno" (id 3) e "Mila teste" (id 1).
- **Causa não é código** — sessão WhatsApp caiu. **Resolvido**: Hugo reconectou a sessão WAHA → status `WORKING`/online confirmado. John pode reenviar.

---

## Régua de conversão do professor — conserto na ORIGEM (Fases 1-3; sem backfill)

**Causa raiz (verificada na fonte n8n):** o webhook ativo `Fucq0bQwF4oeuWnv` chama o sub-workflow `j41tPbyjGXUQUxrN` ("[ LAPerformance/rayandash ]"), que grava em `lead_experimentais` via `registrar_experimental`. O payload do Emusys traz `body.aula.curso` e `body.id` (id da aula), mas o node "Extrair Dados" descartava os dois → curso entrava NULL e multi-instrumento colapsava (constraint `(lead_id, data)`).

**Feito hoje:**
- **n8n `j41tPbyjGXUQUxrN`**: "Extrair Dados" passou a capturar `curso` + `emusys_aula_id`; "Agendar Experimental" passa ambos ao `registrar_experimental`.
- **Migration `p03b`**: `registrar_experimental` unificada (12 params) com `p_curso` (resolve nome→curso_id) e `p_emusys_aula_id`; UPSERT por `emusys_aula_id`. Resolução de curso testada (Canto T→Canto, Violão Para Instrumento→Violão, etc.).
- **Migration `p03a`**: `lead_experimentais` + coluna `emusys_aula_id`; índice único por aula (`uq_lead_exp_aula`); drop das 2 constraints que travavam 2 exp/dia; `uq_lead_exp_legado` para registros sem aula_id; `lead_id` nullable.
- **Edge `sync-presenca-emusys` v37**: reconciliação casa por `emusys_aula_id` (não colapsa multi-instrumento), sobrescreve sempre curso/professor pelo `/aulas`, captura `id_lead`/`id_aluno`.
- **Limpeza**: edge temporária `sim-regua-professor-v2` deletada (Supabase + pasta local).

**Decisões do Hugo:**
- **Sem backfill** — régua nova vale só pra frente; histórico mar-jun fica como está.
- **Fase 4 (RPC) adiada** — multi-instrumento/curso já ficam certos pelas Fases 1-3 sem mexer na RPC; o que a Fase 4 agregaria (aluno antigo no numerador) rende pouco sem backfill.
- **Dúvida em aberto (Hugo × Alf)**: experimental de **aluno antigo** (2º instrumento) conta na régua do professor? Alf disse que sim (20/06), Hugo acha que só lead conta. Por ora régua = **só lead** (edge v37 não insere experimental de aluno antigo, p/ não inflar denominador). Alinhar os dois antes da Fase 4.

Migrations versionadas em `supabase/migrations/20260623_p03a_*.sql` e `_p03b_*.sql`. Pendente: commit.

---

## Caixa Financeiro: seletor de data navegável + fix de timezone (UTC→BRT)

Problema (relato John, CG): à noite o caixa "sumia" e aparecia "Abrir caixa do dia" + não dava pra enviar. Diagnóstico: caixa é por `unidade_id+data_caixa` (compartilhado por unidade — John e Gabi mesma unidade), mas a data do caixa era derivada do filtro de competência via `new Date().toISOString().slice(0,10)` (**UTC**) → após 21h BRT a data pulava pro dia seguinte e o caixa do dia (aberto pela Gabi) não casava. Botão "Enviar WhatsApp" também exige caixa **fechado** (`!caixaFechado` desabilita).

- **Fix timezone:** novo `hojeISOBrt()` em `lib/caixaFinanceiro.ts` (`toLocaleDateString('en-CA',{timeZone:'America/Sao_Paulo'})`). Dados gravados estavam corretos — bug era só de leitura/seleção, **sem backfill**.
- **Date picker próprio no Caixa:** `CaixaFinanceiroTab` ganhou estado interno `dataCaixa` (não vem mais do filtro de competência). UI no header: setas ◄ ► + `DatePicker` (`@/components/ui/date-picker`). Travas: `minDate`=dia 01 do mês corrente (BRT), `maxDate`=hoje. Mês corrente apenas, futuro bloqueado (decisão Hugo). Permite abrir/fechar/enviar caixa de dia passado dentro do mês.
- Removido guard `isCompetenciaAtual` (obsoleto) e a derivação UTC em `AdministrativoPage`. Hook/edge/RLS já suportavam data retroativa — só a UI travava. **Zero migração.** Typecheck limpo.
- ⚠️ Independente: envio do Recreio falhou hoje por sessão WAHA ("Sol") instável ("Session status is not as expected"). Não resolvido aqui — reconectar a sessão se reincidir.
- Pendente: commit.
