# 2026-08-25

## Giulia Magno Nunes Lobo (Barra) — matrícula apagada no Emusys (~11h BRT)

Kailane relatou que a aluna cancelou e pediu para remover do LA Report. Verificado direto na API do Emusys (token da Barra):

- `GET /matriculas?aluno_id=1263&status=todas` → **0 itens** (controle positivo na mesma bateria: `aluno_id=1257` devolve 1).
- `GET /matriculas?data_inicial=2026-08-20&data_final=2026-08-26` → devolve **852…859**, pulando a **851** (a dela).
- `GET /aulas?pessoa_id=1263` (ago–dez/2026) → **0 aulas**. Na turma `T_Sá_12` de 29/08 há uma aula (`261223`) com `alunos: []` — o slot dela, esvaziado.

**Não foi finalização, foi exclusão.** Matrícula finalizada voltaria como `status='inativa'` (+ `motivo_inativa`) desde a v1.3.1; some da API por completo só quando o registro é apagado. Consequência: **nenhum webhook dispara** (`matricula_finalizacao` não veio) — daí `movimentacoes_admin` estar zerada para o aluno 2331. Correto que esteja: aluna nunca teve aula (`nr_aulas_passadas=0`), não é evasão.

Estado do nosso lado:
- `emusys_matriculas_estado_atual` já pegou a ausência: `status_emusys='inativa'`, `motivo_auditoria='fora_do_snapshot_operacional'`, 21/08 21:43.
- `alunos` id **2331** continua `status='ativo'` (Teclado, Isaque, sáb 12h, R$ 365).
- `aluno_jornada_matricula_disciplina` continua `ativa`, última sync 21/08 18:23 (não sincroniza desde então).
- **6 vínculos futuros** em `aula_alunos_emusys` — ela ocupa slots na Agenda que não existem mais no Emusys.
- 0 presenças, 0 divergências na Conciliação.

Pendente: decidir a remoção (caminho é `alunos_arquivados`, a lixeira oficial, + limpar os 6 vínculos de aula). Não executado ainda — aguardando OK do Hugo.

⚠️ Padrão a observar: **matrícula apagada no Emusys não gera evento**. O aluno some da origem e continua ativo aqui até alguém reparar. Quem detecta é o `motivo_auditoria='fora_do_snapshot_operacional'` do estado atual, que hoje não vira nada na tela.

## Varredura das pendências em observação (~15h BRT)

Rodadas as queries de sucesso/fracasso de todas as pendências marcadas 👁️ OBSERVAR.

**Encerradas com sinal cumprido (4):**
- **Trava atômica do `sync-matriculas-emusys`** — 6 noites (19→24/08), **1 execução por unidade,
  todas `succeeded`**, CG incluído. Contra 18/08: 3 por unidade e as 3 do CG em `429`. Critério de
  3 noites superado. Barra em 20/08 teve 2 runs, mas a 2ª chegou 8s **depois** de a 1ª liberar o
  `running` — invocação atrasada, não furo da trava.
- **`trg_resolver_motivo_saida_movimentacao_admin`** — 28 movimentações em 10 dias, **`sem_id = 0`
  em todas**, zero `motivo_saida_desconhecido`. Cobriu inclusive as não-renovações criadas pela
  automação nova, que não existia quando o trigger foi escrito.
- **Não-renovação automática ao concluir contrato** (#190) — disparou sozinha pelo cron do CG:
  Anna Clara (21/08 23:00) e Adriana Christine (22/08 23:01), motivo id 18, `falhas = 0`.
  `fora_do_snapshot_operacional` 6 → 5, e nenhum dos 5 é conclusão por tratar.
- **`matricula_alterada` escrevendo no cadastro** (v88/v34) — **27 aplicações reais** em 10 dias
  (dia, horário, professor), incluindo a normalização "Segunda-feira" → "Terça" que era o objetivo
  de unificar a derivação no `_shared`.

**Correção de memória:** a checagem "`aulas_emusys.cancelada_origem='sync_ausente_emusys'` deve ser
0" é **falso positivo**. São 497 linhas, nascendo diariamente **desde julho** (antes de a v1 do
reconciliar existir) e sempre com `data_aula` no futuro: é o soft-cancel do
`sync-grade-futura-emusys`, que usa o mesmo rótulo. O sinal válido da reconciliação é o
`automacao_log` por `workflow_id`.

**Sem progresso / novos:**
- `falha_ao_atualizar_cadastro` **cessou em 20/08 07:34** (commit `7c451830`, #177) — e o erro não
  era `23505` de telefone como estava registrado, era `22P02 malformed array literal:
  "forma_pagamento_id"`.
- **Timeout de 5s voltou** (2× hoje). O fix de 13/08 cobriu só o jobid 16; **25 jobs ativos seguem
  sem `timeout_milliseconds`**.
- **`sync-presenca-emusys` falhando em silêncio**: 5× HTTP 500
  `FALHA_APLICAR_SNAPSHOT_EXPERIMENTAIS` + 2× 502 `FALHA_UPSTREAM_EMUSYS` nas 6h de retenção do
  `net._http_response`. `pg_cron` marca `succeeded` nos dois.
- **Fila do Fábio**: 5 áudios presos em `transcrevendo` (3 tentativas) — o retry só pega
  `pendente`/`erro`. Modo de falha já documentado, ativo agora.
- **Relatórios do WhatsApp**: sábado **23/08 zerado** (repete 16/08); faltas parciais por unidade
  em 18, 19, 20 e 21/08.
- **`lead_arquivado`**: zero eventos em 20 dias — o caminho com escrita ligada nunca foi
  exercitado. Precisa de decisão, não de mais espera.
- **Redirecionamento da campanha**: as mesmas 2 execuções de 20-21/08. Prazo de 2 semanas vence
  03/09.
- **Renovação órfã**: 17 lançamentos manuais em 21/08 sem 1ª aula e sem `emusys_matricula_id`.
- **Divergência de ativos mudou de sinal**: Barra 279×277, mas **CG 477×478** e **Recreio 423×425**
  — canônico agora MAIOR que a lista nas duas. Causa diferente da do limbo de conclusão.

Sem alteração em produção nesta varredura — só leitura. Memórias atualizadas.

## Dá pra automatizar? — investigação do sinal de exclusão (~14h BRT)

**Webhook: não vem, e não é falha nossa.** A API v1.4.1 não tem evento de exclusão de matrícula (só `matricula_nova`, `_renovacao`, `_trancamento`, `_finalizacao`, `_alterada`, `_aviso_previo_*`). Confirmado no `automacao_log` da 851: `inserido` em 20/08 18:50, `jornada_matricula_alterada_recebida` em 21/08 18:23, **e nada depois**. Exclusão é não-evento, mesma natureza da não-renovação.

**Pull: já vem, e o sinal é bom.** Desde 21/08 o `sync-matriculas-emusys` não infere mais pela ausência — ele **pergunta**. `reconciliarEstadosOperacionaisAusentes` pega quem sumiu do snapshot operacional, chama `GET /matriculas?aluno_id=N&status=todas` e decide em [`_shared/reconciliacao-ausentes-operacional.ts`](supabase/functions/_shared/reconciliacao-ausentes-operacional.ts): achou → reidrata (contrato concluído vira não-renovação); não achou → `motivo_auditoria='fora_do_snapshot_operacional'`. Foi o que marcou a Giulia em **21/08 21:43 — 4 dias antes de a Kailane avisar**.

**A última milha é que falta:** o rótulo morre no espelho. Nenhuma tela lê, nenhum alerta dispara, nada acontece em `alunos`.

### ⚠️ O rótulo NÃO significa "o aluno saiu" — medido nas 5 linhas que existem hoje

| Unidade | Matrícula | Aluno | O que é de verdade |
|---|---|---|---|
| Barra | 851 | Giulia Magno | **saiu**: pessoa 1263 sem NENHUMA matrícula na API |
| Barra | 843 | Davi Isolani | **apagada e REFEITA como 849** no mesmo dia (12/08). Continua ativo, `alunos` já religado à 849 |
| CG | 1799 | Matheus Keyne | já `inativo` local |
| Barra | 838 / 835 | — | nunca viraram aluno local (`aluno_id` null) |

Arquivar em cima do rótulo cru teria removido o **Davi, que é aluno ativo**. O discriminante certo é a **pessoa**, não a matrícula: "a pessoa tem alguma matrícula viva?" — e essa resposta **já está na mão dentro do run**, porque `fetchMatriculasDoAluno` devolve todas as matrículas da pessoa e o `decidirDestinos` só olha se aquele id específico está na lista, descartando o resto.

⚠️ Furo latente: `semChaveDeConsulta` (sem `emusys_aluno_id`) entra em `idsParaConfirmar` **sem consulta nenhuma** e recebe o mesmo rótulo dos confirmados. Hoje são 0 linhas, mas se automatizar em cima do rótulo, um "não deu pra perguntar" vira "confirmado apagado". Precisa de rótulo próprio antes.

Nada implementado — proposta (view de auditoria → alerta → ação) levada ao Hugo.

## Quando a Giulia saiu — linha do tempo (BRT)

Decisão do Hugo: **tratar manualmente por enquanto**, sem automatizar o sinal.

| Quando (BRT) | O quê | Fonte |
|---|---|---|
| 20/08 15:44 | lead criado no Emusys, agente **Kailane Marcos Barbosa** | `automacao_log` `upsert_lead` → lead 13370 |
| 20/08 15:50 | webhook `matricula_nova` → aluno **2331**, matrícula 851, Teclado / Isaque / sáb 12h / R$ 365 | `automacao_log` `inserido` |
| 20/08 23:40 | último sync que **ainda a via** (271 ativas na Barra) | `emusys_matriculas_sync_execucoes` |
| 21/08 15:23 | webhook `matricula_alterada` — matrícula **ainda existia**; 1ª aula empurrada de 22/08 para 05/09; nosso cadastro não mudou (`nada_a_alterar`) | `automacao_log` |
| **21/08 entre 15:23 e 18:43** | **apagada no Emusys** | janela entre os dois carimbos |
| 21/08 18:43 | sync não achou mais → `linhas_inativadas: 1` + `fora_do_snapshot_operacional` | `emusys_matriculas_sync_execucoes` |

**Durou ~1 dia como matrícula e nunca teve aula.** `nr_aulas_passadas = 0`, zero presenças, 1ª aula agendada para 05/09 (nossa grade tem 3 vivas — 05, 12 e 19/09 — e 3 canceladas nas mesmas datas).

⚠️ Consequência para o registro: **não é evasão nem não-renovação** — é desistência de matrícula nova, antes da primeira aula. Não deve entrar em `movimentacoes_admin`; o caminho certo é só arquivar (`alunos_arquivados`) e limpar os vínculos de aula.

## Arquivamento executado (~15h BRT) — aprovado pelo Hugo

Aluno **2331** movido para `alunos_arquivados` (id original preservado, como nos precedentes), `arquivado_por='claude (aprovado por Hugo, 2026-08-25)'`. Barra: 280 → **279 ativos**.

Feito em **um único `DO` block** (atômico — a 1ª tentativa falhou em `projecao_aulas` e reverteu inteira, sem estado parcial). O INSERT monta a lista de colunas **por `information_schema`, cruzando as duas tabelas**: `alunos` tem 12 colunas que `alunos_arquivados` não tem (`emusys_lead_id`, `lead_origem_id`, `valor_cheio`, `desconto_fixo`, `desconto_condicional`, `aguardando_renovacao`, `instagram_*`, `arquivado_*`) e transcrever à mão erraria.

| Passo | Por quê |
|---|---|
| `aula_alunos_emusys` — 6 apagados | FK é **SET NULL**: sem DELETE explícito viram órfãos com `aluno_id` nulo e a aula continua na grade |
| `aluno_jornada_matricula_disciplina` — 1 apagada | idem, e é o que alimenta `vw_contratos_vencendo` / `vw_renovacao_ciclos` |
| `projecao_aulas` — 40 apagadas | **NO ACTION, bloqueava o DELETE**; são as 40 aulas projetadas do contrato morto |
| `alunos_emusys_atributos_divergencias` — 1 apagada | NO ACTION; anamnese pendente, já `resolvido=true` |
| `leads` 13370 → `aluno_id = null` | NO ACTION. **`converteu=true` mantido** — precedente das matrículas 835/838 (10/08) e do Ravi (17/06) |
| `alunos` 2331 apagado | cascade levou os 2 `aluno_contatos` |
| `emusys_matriculas_estado_atual` 851 | **preservada** com `aluno_id` nulo — é o rastro de auditoria, igual às 835/838 |

⚠️ **Decisão que mexe em KPI comercial:** o lead segue `converteu=true` com conversão em 20/08 e agente Kailane, então a matrícula **continua contando na taxa de conversão de agosto** mesmo tendo durado 1 dia. Foi o precedente que o projeto já usava; se a regra correta for outra, é reversível (`update leads set converteu=false, status=... where id=13370`) — vale confirmar com o Alf.

⚠️ **Resíduo pré-existente medido:** o banco já tem **598 vínculos de aula** e **2.963 linhas de jornada** com `aluno_id` nulo — herança de arquivamentos anteriores que deixaram o SET NULL agir sozinho. Não tocado aqui; é frente própria.
