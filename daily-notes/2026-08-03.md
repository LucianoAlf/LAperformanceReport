# 2026-08-03

## Upgrade Gemini: `gemini-3-flash-preview` → `gemini-3.6-flash`

- **Modelo**: `gemini-3.6-flash` (estável, lançado jul/2026 — suporta function calling, structured outputs, thinking; 1M input / 64k output tokens). Substitui `gemini-3-flash-preview` (preview de dez/2025).
- **Chave**: `GEMINI_API_KEY` rotacionada e funcionando no secret do Supabase (o bloqueio de 16/07 foi resolvido). Confirmado com teste direto na API (HTTP 200) e end-to-end nas edges.
- **9 edge functions atualizadas e deployadas**:
  - `gemini-insights` (const `GEMINI_MODEL`)
  - `gemini-insights-turma` (const `GEMINI_MODEL`)
  - `gemini-fabio-chat` (const `GEMINI_MODEL`)
  - `gerar-relatorio-pedagogico` (const `MODELO_IA`)
  - `gemini-relatorio-professor-individual` (URL inline)
  - `gerar-relatorio-aluno` (URL inline)
  - `gerar-plano-aluno` (URL inline)
  - `gemini-insights-retencao` (URL inline)
  - `gemini-insights-comercial` (URL inline)
- **Frontend**: dropdown de modelos em `ModalEditarAgente.tsx` — `gemini-2.0-flash` e `gemini-1.5-pro` substituídos por `gemini-3.6-flash` (nenhum agente no banco usava Gemini; todos usam OpenAI).
- **Edges `gemini-*` NÃO alteradas** (já migradas pra OpenAI anteriormente): `gemini-insights-professor` (`gpt-5.4-mini`), `gemini-relatorio-gerencial` (OpenAI), `gemini-relatorio-coordenacao`, `gemini-relatorio-individual`, `gemini-relatorio-professor`, `gemini-relatorio-turma`.
- **Testes end-to-end**:
  - `gerar-relatorio-pedagogico` (aluno_id=3): **success: true**, relatório gerado e salvo com `modelo_ia='gemini-3.6-flash'` no banco.
  - `gemini-insights-comercial`: **HTTP 200**.
  - `gemini-fabio-chat`: HTTP 200 (erro `projetosAtivos` undefined é bug de dados pré-existente — a edge não chega a chamar o Gemini sem `contexto`; não relacionado ao modelo).
  - `gerar-plano-aluno`: HTTP 500 (erro `health_score_numerico` undefined — bug de dados pré-existente, não relacionado ao modelo).
  - Logs de runtime: **nenhum erro da API Gemini** (sem 403/404).
- **Docs atualizadas**: `CLAUDE.md` (linha da chave bloqueada → agora funcionando), `.claude/memory/integracao-infra.md` (edge `classificar-desinteresse`).
