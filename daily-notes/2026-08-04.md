# 2026-08-04

## Varredura Meta Ads — canal quando o WhatsApp manda o payload incompleto (~13h30 BRT)

### O que apareceu na verificação

Rodando a checagem de rotina ("o lead está recebendo a origem?"), 2 leads de hoje apareceram
**com anúncio e sem origem** — Lena (#11746, 11:57) e Juliana (#11751, 14:17). Nos dias 01, 02 e
03/08 esse número estava em 0.

### Causa: payload incompleto do WhatsApp, não bug nosso

Lendo a conversa 18973 direto no Chatwoot:

```json
"additional_attributes": {
  "source_id": "120251062759270422",
  "source_type": "ad",
  "entry_point_conversion_source": "ctwa_ad"
}
```

**3 chaves, em vez das 5 habituais** — faltam `ctwa_clid` e `source_app`. Consequência:

- o lead **ganha** `meta_ad_source_id` (o `source_id` veio);
- **não ganha** `canal_origem_id`, porque a edge deriva o canal do `source_app`;
- e o fluxo n8n em tempo real também não pega, porque sem `ctwa_clid` a mensagem não carrega
  `external_ad_reply`.

A edge se recusou a gravar o canal — comportamento correto pelo desenho ("não chutar origem").

### Correção: inferir o app pelo MESMO anúncio

O mesmo `source_id` aparece em outras conversas **com** `source_app` preenchido. A edge passou a
montar um mapa `source_id → source_app` a partir das conversas da varredura que têm o campo, e
usa como fallback quando falta.

⚠️ **A origem vem da própria Meta**, para o mesmo anúncio — não é chute nem inferência a partir
dos nossos dados. **Não usar `leads` como fonte**: o anúncio `120251062759270422` tem 135 leads,
sendo 131 Instagram e **2 com canal diferente** (preenchido por outra via). Deduzir dali
propagaria o erro.

Novo campo no resumo: `canais_por_app_inferido`. No log, `source_app_inferido` registra de onde
veio.

### Resultado

```
conversas_lidas: 179 | conversas_anuncio: 45
vinculados: 0 | canais_preenchidos: 2 | canais_por_app_inferido: 2
ja_completos: 40 | ambiguos: 2 | nao_encontrados: 1
```

Pegou exatamente os 2 casos e nada além. **"Com anúncio e sem origem" = 0** em 01, 02, 03 e
04/08.

### Verificações de saúde feitas junto

- **Cron vivo e escrevendo** — não só `succeeded`: log com escrita real hoje às 06h, 07h, 09h,
  11h e 13h BRT. Isso também prova que o deploy não resetou o `verify_jwt` (senão o gateway
  devolveria 401 e não haveria escrita).
- **Mapeamento de canal 100% coerente:** 42 gravações, todas `instagram` → canal 1. Zero erro.
- ⚠️ O cron está em **`40 * * * *` (de hora em hora)**, não no `40 7 * * *` (diário) com que
  nasceu. Mudança de 03/08, já documentada no CLAUDE.md, e **confirmada pelo Hugo** como
  intencional.

### Git

⚠️ **O commit `12d4134` (fix do canal) sumiu da branch local** — a `main` foi reescrita por
outra sessão. O conteúdo equivalente voltou como `c5a54251`, então nada foi perdido, mas foi
sorte: as duas sessões mexeram na mesma coisa de forma equivalente. Se tivessem divergido, uma
teria apagado a outra em silêncio.

**Decisão do Hugo: continuar trabalhando direto na `main`** (sem branch por frente de trabalho).
Regra prática para não repetir: **`git pull` antes de commitar, sempre**.

⚠️ O daily-note de 01/08 foi escrito com data errada em parte da sessão (relógio local estava
3 dias atrás). Este arquivo é o de hoje.

## Auditoria das pendências em observação (~14h BRT)

Varredura das pendências abertas contra banco e código. Resultado por item:

**Saudáveis:** item 11 (varredura Meta Ads) — `anuncio_SEM_origem = 0` em 14 dias seguidos; a
correção da v2 segurou. Item 1 (duplicatas) — nenhuma nova desde 25/06, a trava do frontend
está funcionando; restam 8 grupos / 17 linhas, todas antigas. Item 10 — as 5 migrations já
têm arquivo no repo.

**Continuam abertas:** item 7 (usuário novo nasce fora do RBAC — código inalterado, mas não
mordeu porque ninguém foi criado desde 31/07), item 6 etapa 1 (seletor multi-unidade não
iniciado), item 2 (delete 409), item 8 (Matheus Felipe é o único fora do RBAC), item 12
(3 migrations do Alf com arquivo no repo mas **sem registro** em `schema_migrations` —
agora um `db push` tentaria reaplicar).

**Item 0b:** de 4 "arquivadas mas vivas" caiu para 3 (João Pedro #1591 saiu).

⚠️ **Sinal novo no item 11:** `vinculado` subiu (12→22/dia) porque o cron virou horário em
03/08. Separando por origem, a varredura pega 4-7× mais que o n8n em tempo real (04/08: 12
contra 1). Ela **virou a fonte principal**, não a rede de segurança.

🔴 **Achado fora da lista:** `sync_presenca` acumulou **257 `nao_encontrado` em 8 dias**
(41 só em 03/08). Não está em nenhuma pendência. Não investigado ainda.

## Observador Emusys — está melhor que o n8n, mas falta 1 evento (~15h BRT)

**Vivo e em paridade.** Último evento 13 min antes da consulta, 436 nas últimas 24h.
**Paridade 100% há 6 dias** (30/07 a 04/08) nos eventos do escopo dele. Antes disso: 28/07
(-12) e 29/07 (-3), anteriores ao fix v16. Qualidade em 48h: `lead_not_found` 0, erro 0,
curso NULL só em 1 cancelamento (payload de cancelamento não traz curso — esperado).

⚠️ **`lead_arquivado` NUNCA chegou** — zero ocorrências em todo o `automacao_log`. É o ramo
que a v4 (31/07) construiu e que estrearia direto em produção. Só sai do escuro forçando um
arquivamento de teste no Emusys.

**Comparação observador × dado gravado** (30/07 em diante, casando pela chave da RPC —
lead + data + horário + curso): 32 casos em sombra, 27 casaram com linha existente.
**Lead: 0 divergências. Curso: 0 divergências. Professor: 24 iguais, 3 diferentes** — e nos
3 o observador acerta: o gravado aponta para cadastro **mesclado inativo**
(`Erick Osmy (mesclado 54)` em vez de `Erick Cosme` 52; `Matheus Reis ... (mesclado 57)` em
vez de `Matheus Reis` 46). Ou seja, trocar melhora ~11% dos professores de experimental e
não piora nada medido.

⚠️ **Primeira comparação estava errada** — juntei sem o `curso` e deu 6 divergências. Vários
desses alunos têm 2-3 experimentais no mesmo horário (instrumentos diferentes), então o join
casava a linha errada. Com a chave completa, 3.

⚠️ **Ressalva:** o professor gravado pode ter sido sobrescrito pela reconciliação do
`sync-presenca-emusys`, não pelo n8n. O valor é errado de qualquer forma, mas a culpa não é
atribuível.

⚠️ **Cutover não duplica:** li a `registrar_experimental` no banco — o dedup é por
`(lead, data, horário, curso)` e `emusys_aula_id = COALESCE(existente, novo)`. Rodar
observador e n8n juntos faz o segundo virar UPDATE. Dá para virar gradual.

⚠️ **Não verificado:** o MCP do n8n não está conectado nesta sessão, então **não abri os
workflows** `EB0LibpOJCLhKp7M` / `j41tPbyjGXUQUxrN`. A afirmação "o n8n só chama as 2 RPCs"
vem do cabeçalho do observador (escrito em 29/07 após leitura), não de verificação minha.
Antes de desligar o n8n isso tem que ser conferido na fonte.

### 🔴 `lead_experimentais.emusys_aula_id` NÃO é o id da aula

O `body.id` do webhook é o id da **entrega da notificação** — cada assinante recebe um número
diferente para o mesmo evento (n8n 73172 × observador 73173; 72641 × 72643; 72469 × 72471).
O payload **não tem** `aula.id` (conferidas todas as chaves de `body.aula`).

Prova: ids reais de aula em `aulas_emusys` estão na faixa **254.000–827.000**. Dos 12 ids
acima, **nenhum** existe lá; no geral só 44 de 311 casam, e isso é coincidência com registros
antigos. Consequência: qualquer join experimental↔aula por esse campo não funciona — e o
CLAUDE.md descreve a reconciliação do `sync-presenca-emusys` como sendo por `emusys_aula_id`.
Independe da troca n8n↔observador (o campo já está assim), mas na troca o número muda de faixa.

## Sombra deixa de se passar por operação real (~16h BRT)

**Problema:** o dry-run não escreve em tabela de negócio (confirmado no código), mas grava em
`automacao_log` — **2.887 linhas em 30 dias, 47% da tabela** — e essas linhas apareciam nas
telas sem nada indicando que eram teste. Pior: o badge é
`acaoStyles[acao] || acaoStyles.atualizado`, e como `processado_sombra` /
`webhook_observado_direto` não estavam no mapa, **caíam no fallback e apareciam como
"Atualizado"**, como se o aluno tivesse sido alterado. O `workflow_id` só aparecia na linha
expandida — e as 2.018 linhas de payload bruto **não têm `detalhes`, logo não expandem**:
70% eram indistinguíveis por qualquer meio.

**Não afeta métrica:** as 4 funções que citam `automacao_log` só escrevem nela, nenhuma lê.
Das 227 invariantes em 30 dias, 0 vieram de linha do observador. O único trigger da tabela só
acrescenta uma chave no `detalhes` da própria linha.

**Feito:**
- `src/lib/automacaoSombra.ts` (novo): `WORKFLOWS_SOMBRA`, `FILTRO_SEM_SOMBRA`, `ehLogDeSombra`.
- `useAutomacoesData.ts`: filtro `incluirSombra` (default `false`) + entrada no `fkey`.
- `AutomacoesPage.tsx`: toggle "Mostrar sombra (teste)".
- `TabAutomacao.tsx`: toggle próprio + `acaoStylesSombra` (badge cinza "Sombra (teste)" /
  "Sombra (payload)"). Ficou **fora** de `acaoStyles` de propósito: aquele mapa também gera os
  cartões de contagem do topo, e sombra não é métrica de operação.
- `LinhaEvento.tsx`: badge `SOMBRA` quando o toggle está ligado.

⚠️ **`NOT IN` puro descartaria linha com `workflow_id` NULL** (NULL NOT IN (...) = NULL).
Por isso o filtro é `workflow_id.is.null,workflow_id.not.in.(...)`. Medido: com a guarda,
3.244 linhas; sem ela, 3.068 — **176 sumiriam em silêncio**.

**Validação:** `tsc --noEmit` sem erro em `src/` (os 5 erros restantes são pré-existentes em
`scripts/importar_historico_ltv.js`). Sintaxe do filtro provada contra a API real — um filtro
malformado devolve `PGRST100` de parse, o meu passa e chega no Postgres (`42501`, porque anon
não lê a tabela).

## Observador v5 — liberação por evento + 2 campos novos (~17h BRT)

Decisão do Hugo: migrar os eventos do n8n para o observador **gradualmente, começando
pela experimental**. Código preparado (NÃO deployado, aguardando autorização).

**1. Gate por evento.** O interruptor era tudo-ou-nada — `OBSERVADOR_DRY_RUN=false` ligava
lead + experimental + arquivamento juntos. Agora: `OBSERVADOR_ESCREVE` aceita lista de
eventos (`*`/`todos` = tudo). Sem a variável, honra o `OBSERVADOR_DRY_RUN` antigo, cujo
default segue sendo NÃO escrever — comportamento atual preservado.

**2. Token opcional.** `OBSERVADOR_TOKEN`: quando definido, exige `x-observador-token` ou
`?token=`. **Enquanto não estiver definido, a verificação fica desligada de propósito** —
o deploy não pode derrubar o webhook antes de sabermos se o Emusys manda header custom.
⚠️ Hoje o endereço é público e, escrevendo, gravaria em `leads`.

**3. `status` no log.** O observador não preenchia (ficava no default 'ok'). Agora grava
`erro` (falhou) / `warn` (estava escrevendo e não achou em quem escrever) / `ok`, e
acrescenta `detalhes.modo` = `escrita`|`sombra`. O `status` é o que a tela de Automações
já filtra.

**4. Professor por `emusys_id`** (camada 1, antes de telefone e nome). ⚠️ **Filtra unidade
obrigatoriamente**: `professores_unidades.emusys_id` NÃO é único — o 1002 aparece em 2
unidades apontando para professores diferentes (7 e 8). Sem o filtro, erraria a pessoa.

**5. `observacoes` da experimental** gravada em `lead_experimentais` como delta pós-RPC
(a `registrar_experimental` não tem parâmetro; mudar a RPC mexeria em objeto com consumidor
ativo). Só preenche quando vazia. A coluna estava **0 de 311** — ganho puro.

### Achados que motivaram 4 e 5

- **`aula.professor_id` começou a chegar em 04/08** — 1 payload em 119. Valor 863 mapeia
  para professor 62 (Adriana, ativa), exatamente quem o resolvedor por nome acertou. Não dá
  para saber ainda se veio para ficar; a camada fica dormente enquanto não vier.
- **`observacoes` chega em 100% dos payloads**, preenchida em ~70%, e era descartada pelos
  dois lados. Exemplo real: *"Aluna tem 2 anos, a mãe informou que ela gosta muito de brilha
  brilha estrelinha"*.

### O observador manda mensagem? NÃO

Verificado no código: **zero chamadas externas** (só os 2 imports do topo). Não tem `fetch`
para UAZAPI/Chatwoot/n8n — fisicamente não consegue mandar mensagem.

E o n8n manda? Sem evidência de que mande. ⚠️ **O 1º teste que fiz foi furado** — procurei
correlação em `crm_mensagens` e deu zero, mas **existem 0 mensagens de saída nessa tabela**
nos 20 dias; zero de zero não prova nada. Refeito em `admin_mensagens` com placebo:

| Janela | Experimentais com msg de saída perto |
|---|---|
| Real (0 a +5min) | 11 de 106 (10,4%) |
| Placebo (mesma hora, 1 dia antes) | 6 de 106 (5,7%) |
| Placebo (+3h) | 4 de 106 (3,8%) |

Automação disparando por experimental daria ~100%, não 10%. Os 11 são da ordem do acaso
(gente conversando no mesmo horário). **Não existe mensagem automática por experimental.**
Continua sem verificação absoluta só o que não deixe rastro nenhum no banco.

### Correção do filtro de sombra que eu tinha feito errado

O filtro da tela escondia por `workflow_id`. Funcionaria hoje mas **quebraria na virada**:
as escritas reais saem da MESMA função, então sumiriam da tela justamente quando virassem
operação de verdade. Trocado para filtrar por **`acao`** (`processado_sombra` e
`webhook_observado_direto` escondidos; `processado` aparece, com badge próprio).
`automacao_log.acao` é NOT NULL, então `NOT IN` aqui não descarta linha em silêncio.

### Sequência combinada para virar a experimental

1. Deploy (⚠️ `verify_jwt: false` explícito — o MCP reseta para `true` e mata o webhook)
2. `OBSERVADOR_ESCREVE=aula_experimental_criada,aula_experimental_reagendada,aula_experimental_cancelada`
3. Conferir no log: `acao='processado'` + `detalhes.modo='escrita'`
4. Dias de escrita dupla (o dedup da RPC protege), comparando
5. Desabilitar o **ramo** de experimental no n8n — ⚠️ é sub-workflow chamado por um pai
   ativo, desativar o sub sozinho quebra o pai (fonte: docs, não verificado)
6. Rollback: tirar os eventos da variável; o n8n reassume na hora

⚠️ **Pendente de decisão:** o passo final do handler responde `200 ok` mesmo com erro. Com
o n8n ligado tudo bem; depois de desligado, falha vira evento perdido em silêncio. Não
mexi porque devolver 5xx pode ter efeito colateral desconhecido no Emusys (corte automático
de webhook após N falhas). Por ora a falha fica visível como `status='erro'`.

**Validação:** `deno check` limpo na edge; `tsc --noEmit` sem erro em `src/`.

## Observador v5 EM PRODUÇÃO para experimental (~15h50 BRT)

Autorizado pelo Hugo. Deploy feito e a experimental **liberada para escrita**.

**Deploy pelo CLI, não pelo MCP — desvio consciente da regra do projeto.** O MCP exige colar
as 643 linhas dentro da chamada, que é exatamente a "transcrição lossy" que o CLAUDE.md
aponta como causa de git ≠ produção. O CLI envia do disco: zero risco de erro de cópia num
webhook de produção, e o `--no-verify-jwt` é explícito (evita o reset que já derrubou 2 edges).
Antes de subir, baixei o código deployado (v18) e conferi que era idêntico ao repo — não
sobrescrevi trabalho de outra sessão.

**Validação do deploy:**
- v18 → v19 → v20, `verify_jwt: false` preservado ✅
- POST **sem nenhum header de auth** (igual ao Emusys) → HTTP 200 ✅
- Smoke test com dados que não casam com lead: `acao='processado'`, `modo='escrita'`,
  RPC chamada de verdade e devolvendo `lead_not_found` → **0 linhas criadas** em
  `lead_experimentais` ✅

**🐛 Bug meu pego pelo smoke test.** O `status` ficou `ok` num caso que devia ser `warn`.
Minha regra procurava `lead_not_found` no campo `acao`, mas na **escrita real** isso vem
dentro do retorno da RPC (`{success:false, reason:'lead_not_found'}`) — só funcionava no
formato do modo sombra. Ou seja: experimental sem lead passaria despercebida, justamente o
que o campo existe para denunciar. Corrigido (`rpcRecusou`) e reprovado lado a lado: mesmo
cenário, 18:49 = `ok` (errado) / 18:50 = `warn` (certo).

**Estado:**
```
Emusys → n8n .......... ESCREVE (intacto)
       → observador ... ESCREVE experimental (3 eventos)
                        assiste lead e arquivamento
```
`OBSERVADOR_ESCREVE=aula_experimental_criada,aula_experimental_reagendada,aula_experimental_cancelada`
Rollback: `supabase secrets unset OBSERVADOR_ESCREVE` (n8n nunca parou).

**⚠️ PENDENTE antes de desligar o n8n:** ver a 1ª experimental REAL passar. O smoke test usou
dados falsos — prova que o caminho está ativo, não que grava certo com lead existente.
Conferir: `acao='processado'`+`modo='escrita'`, professor correto, **`observacoes` preenchida**
(hoje 0 de 311), lead com `experimental_agendada`/data/horário/etapa 5, e que não duplicou.

**Sobre backfill como rede (discussão com o Hugo).** Ele está certo que dá para reprocessar
pelo payload cru — a `registrar_experimental` recebe tudo que está lá. Duas ressalvas: (a) não
existe rotina de backfill, seria script na mão; (b) **só cobre o que CHEGOU na função** — se
ela parar de receber (401 no gateway, já aconteceu 2× neste projeto), não há payload guardado
e não há o que reprocessar. Por isso a ordem continua: só desligar o n8n depois de ver uma
real passar.

⚠️ **Ao desligar, desligar SÓ o ramo de experimental.** Lead (`lead_criado`/`lead_editado`,
104 eventos/dia contra 8,6 da experimental) continua 100% no n8n — o observador segue em
sombra para eles. Derrubar o workflow inteiro pararia os leads.

## n8n desligado para experimental — migração concluída (~16h07 BRT)

**MCP do n8n conectado** (o Hugo habilitou), o que finalmente permitiu ler os workflows na
fonte. Isso derrubou duas afirmações minhas anteriores — registradas abaixo porque as duas
podiam ter causado estrago.

### ❌ Correção 1: o n8n MANDA mensagem por experimental

Eu havia concluído, com teste placebo em `admin_mensagens`/`crm_mensagens`, que "não existe
mensagem automática por experimental". **Errado.** As mensagens saem por **WAHA**, que não
passa por nenhuma dessas tabelas. O `docs/MAPA-INTEGRACAO-EMUSYS.md` (linha 62) já dizia isso.

O `Fucq0bQwF4oeuWnv` tem **32 nós** e dispara 5 ramos paralelos do webhook. Só um deles é o que
o observador replica. As 6 mensagens WAHA: `waha_msg_consultor`, `waha_msg_professor`,
`Confirma Agendamento (Criada)` (essa vai **pro lead**, após espera de 15min + dedup), e as
equivalentes de Reagendada/Cancelada.

**Lição:** ausência de rastro numa tabela não prova ausência do efeito — o canal pode ser outro.

### ❌ Correção 2: o n8n JÁ filtra professor ativo

O comentário no código do observador (escrito 29/07) diz que o n8n usa `nome ILIKE` sem filtrar
ativo. O SQL real do nó `Buscar Professor` hoje faz `WHERE p.ativo = true` e casa por
`right(telefone,11)` OU `emusys_nome` da unidade, preferindo telefone. É equivalente às camadas
2 e 3 do observador — e o `right(...,11)` é até mais tolerante que o match exato que implementei.

O workflow foi atualizado em **03/08 23:07** (versão 34). As 3 divergências de professor que
medi são de 31/07 ou antes → **o "ganho de 11%" provavelmente não existe mais**. O que sobra de
vantagem real: a camada por `emusys_id` e o `observacoes`. A troca passou a ser decisão de
arquitetura, não correção de bug. ⚠️ Remedir daqui a alguns dias.

### O que foi desligado (pelo Hugo, 16h07)

No **`j41tPbyjGXUQUxrN`**, dois nós Postgres desativados:
- ⛔ `Agendar Experimental`
- ⛔ `Cancelar Experimental`

Escolha cirúrgica (opção B). Continuam ativos: `Buscar Professor`, `Log Agendamento`,
`Log Cancelamento`, `Encaminhar para Supabase do rayan`, `busca_lead`+`updateLead` (NocoDB).
No workflow pai **nada foi tocado**.

⚠️ **NÃO desligar `Espelhar Experimental p/ Mila (Supabase)`** — aponta para
`wxairgavoteauysgruyv` (Supabase da **Mila**), não tem relação com a persistência do LA Report.
Desligar pararia o bot SDR de saber das experimentais. O Hugo chegou a cogitar; foi evitado.

⚠️ Também **não** desligar `Call '[ Function ] - atualizar lead para experimental'5` no pai:
levaria junto o log em `leads_automacao_log`, o encaminhamento pro rayan
(`aexacbmirdlcssmjjbzx` — projeto diferente do da Mila) e o NocoDB do lead.

**Rollback:** reativar os 2 nós. O dedup da RPC impede duplicata se os dois voltarem a escrever.

### 🐛 Dois bugs de produção achados na execução 603616

**1. `Gravar Raw Experimental` nunca funcionou.**
```
null value in column "lead_nome" of relation "leads_automacao_log" violates not-null constraint
```
O INSERT não passa `lead_nome`, que é NOT NULL. Confirmado no banco: **`webhook_experimental_raw`
= 0 linhas, desde sempre**. O nó tem `continueOnFail`, então a execução aparece verde.
Consequência: o backup bruto que ele deveria manter não existe — o `payload_bruto` do observador
é hoje o único registro cru que funciona.

**2. Professor não é avisado em ~5% das experimentais.**
```
Invalid Brazilian phone number '55@c.us'  (HTTP 422)
```
`telefone_professor` vem vazio no payload → o WAHA monta `55@c.us` e recusa. Medido: **7 de 131
payloads (5,3%)** em 30 dias. O nó tem `onError: continueRegularOutput`, então some no silêncio.
Caso concreto: Amelie, aula 73172, 08/08 11h com a Adriana — a professora não foi avisada.
⚠️ Conserto óbvio: fallback para `professores.telefone_whatsapp` (preenchido em 44 de 44).

### Pendente de verificação

Afirmei que nó desabilitado no n8n repassa os dados adiante, e que por isso `Log Agendamento`
continuaria gravando. É o comportamento padrão do n8n, **mas não testei**. Conferir na próxima
experimental real: se `leads_automacao_log` parar de receber `aula_experimental_criada`, a
cadeia quebrou e é para reativar.
