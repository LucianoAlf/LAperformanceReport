# 2026-09-21

## Fase 3 do Google configurada + a Barra investigada e absolvida (~BRT)

### Checagem de 4 dias da atribuição (LAPE-38)

| Sinal | 17/09 | 21/09 |
|---|---|---|
| Leads com `gclid` | 3 | **21** |
| Cliques registrados | 3 | **22** |
| Cliques **sem** campanha resolvida | — | **0** |
| Leads com `canal_origem_id = 3` desde 17/09 | — | **18** |

O resolvedor de campanha (`click_view`, cron `25 */6`) está em **100%**. Era o ponto de maior
dúvida, porque depende da latência de ingestão do lado do Google.

A fila da Fase 3 continua **0**, e testei que é falta de dado e não bug: rodei os 21 leads com
gclid contra `alunos` pelas duas portas — **nenhum virou aluno**, nem os mais antigos (Vera
Mattos de 07/05, Daniel Silva de 27/06).

### O funil por campanha fecha 100% com dado NOSSO

Pergunta do Hugo: precisamos mandar pro Google para ter a métrica? **Não.** Custo por campanha
(`google_ads_metricas_diarias`) × clique→campanha (`google_ads_cliques.campanha_id`) ×
clique→lead × lead→matrícula × valor (`alunos`) — a cadeia fecha. **O CAC real por campanha é
nosso.** A Fase 3 só existe para ensinar o leiloeiro, não para medir.

Medido com janelas alinhadas (desde 17/09):

| Campanha | Gasto | Cliques | Eventos | Custo/lead | % clique capturado |
|---|---|---|---|---|---|
| CG P.MAX | R$ 103,94 | 873 | 14 | R$ 8,00 | 1,60% |
| RECREIO P.MAX | R$ 151,45 | 568 | 7 | R$ 21,64 | 1,23% |
| BARRA P.MAX | R$ 151,41 | 376 | **0** | — | **0%** |

⚠️ **Meu primeiro cálculo deu R$ 494/lead no Recreio — estava errado.** Eu tinha cruzado
**90 dias de gasto com 4 dias de atribuição**. Janela assimétrica produz número que parece
medida e é artefato.

### 🔴 → ✅ A Barra: eu reportei como falha e NÃO é

Reportei "0 de 376 cliques" sugerindo que o rastreio não chegava na landing da Barra. O Hugo
cobrou a distinção certa: *"preciso saber se não tá funcionando ou ainda não aconteceu evento,
são coisas bem diferentes"*. Testei elo por elo:

| Elo | Barra | CG | Recreio |
|---|---|---|---|
| Landing tem o link CQC | ✅ `/t/4A3A1A30` | ✅ `/t/12A35C26` | ✅ `/t/29B4F5AD` |
| Link responde | 302 | 302 | 302 |
| Rastreio invisível (zero-width) | ✅ | ✅ | ✅ |
| Número de destino | `552139550932` | `5521982956809` | `552139552420` |
| Inbox Chatwoot | 147 Mila_Barra | 155 Mila_CG | 148 Mila_Recreio |
| Conexão WAHA | **working** | working | working |

**Nenhum elo quebrado — é "ainda não aconteceu evento".** Clique no anúncio ≠ clique no botão
de WhatsApp.

⚠️ **O teste foi feito SEM seguir o redirect** (`-o /dev/null -w "%{redirect_url}"`). Seguir
levaria ao `api.whatsapp.com/send`, e completar o fluxo criaria conversa real e notificaria o
consultor da Barra — a regra de "teste não notifica consultor real" vale aqui.

👁️ **O que fica em observação:** com a taxa das outras duas (1,2–1,6%), 376 cliques deveriam
gerar ~5 eventos. Zero tem ~0,5% de chance por azar. **Corte definido: passando de ~700
cliques ainda com zero, há algo que estes testes não alcançam** — provavelmente o link da
Barra não estar cadastrado no CQC para disparar webhook, o que só a onpromedia resolve.

### A conversion action foi criada

`customers/7179097170/conversionActions/7785226299` — "Matricula (LA Report)", **PURCHASE**,
`primaryForGoal: false`, lookback 90d. Secret `GOOGLE_ADS_CONVERSION_ACTION_ID` configurado.

Confirmado **pelo efeito**, não pela mensagem do CLI: o diagnóstico devolve
`conversion_action_configurada: 7785226299`, e o modo `enviar` passou a ser aceito (antes
recusava com "não configurado"), respondendo `na_fila: 0`.

⚠️ Fica **separada** da action da onpromedia (`7640911878`, CONTACT) de propósito — misturar
os dois sinais deixaria nossa matrícula e o contato deles no mesmo balde, e eles podem
sobrescrever o que subirmos.

### ⚠️ Cron NÃO criado — decisão pendente

Com o cron ligado, a **primeira matrícula da história do sistema** iria ao Google sem ninguém
olhar, e conversão enviada não se apaga. Proposto: primeiro envio manual e conferido, cron
`40 6 * * *` só depois. Aguardando decisão do Hugo.

## A nota de divergência na tela do Google (~BRT)

Quando a Fase 3 começar a enviar, o painel "Conversões por ação configurada" vai ganhar uma
linha `Matricula (LA Report)`. Ela **não vai bater** com a tabela "Leads atribuídos a anúncios"
da mesma tela, e quem comparar vai achar que há bug. A nota existe para essa pessoa.

**O que muda na tela, verificado no código (não suposto):**

| Elemento | Fonte | Muda? |
|---|---|---|
| KPI "Conversões" (854,1 em 30d) | `metrics.conversions` — metas primárias | ❌ Não |
| "Custo por conversão" | gasto ÷ metas primárias | ❌ Não |
| Painel "por ação" (1.584,7) | `metrics.all_conversions` | ⚠️ **Sim** |
| Gasto, cliques, CTR, CPC | API | ❌ Não |
| Tabela "Leads atribuídos" | nosso banco | ❌ Não |

O KPI não muda **porque a action nasceu com `primaryForGoal: false`** — foi exatamente para
isso. Só o painel de "todas as ações" reflete.

**As 4 razões da divergência**, que a nota resume para o leitor:

1. **A data é outra** — o Google lança no dia do *clique*, nós no dia da *matrícula*. O mesmo
   aluno cai em meses diferentes nos dois relatórios. É a maior fonte de diferença.
2. **Crédito fracionado** — o modelo dele reparte uma conversão entre pontos de contato.
   ⚠️ **Os decimais do painel são a prova disso**: 854,1 e 1.584,7 não são arredondamento.
   Para nós, uma matrícula é sempre 1 inteira.
3. **Janela de 90 dias** — clique mais velho é recusado. Tem caso real na fila: Vera Mattos
   clicou em 07/05 (**137 dias**, já fora) e Daniel Silva em 27/06 (86 dias, **sai em ~4
   dias**). A edge reporta como `fora_da_janela` em vez de mandar e falhar calada.
4. **Dedup por timestamp** — irmãos no mesmo instante: ele conta 1, nós 2. Mitigado por usar
   `created_at` em vez de `data_matricula`, não eliminado.

**Implementação** (`SecaoGoogleAds.tsx`): a nossa linha ganha ★, cor índigo e o título
"enviada por nós (Fase 3)"; as outras seguem verdes.

⚠️ **A action é reconhecida pelo NOME** (`MARCA_ACAO_NOSSA = 'LA Report'`), porque o recorte
por ação da API devolve o nome e nunca o id. Renomear a action no painel do Google **quebra o
destaque em silêncio** — o número continua certo, só o realce se perde. Está comentado no
código.

⚠️ **A nota só renderiza quando a nossa ação já tem número** (`.some(...)`). Antes do primeiro
envio ela não teria a que se referir, e explicar uma linha inexistente confunde mais do que
esclarece.

`npx tsc --noEmit` sem erro no arquivo e `npm run build` ✓ em 34s.

### Cron: segue NÃO criado

Mantida a recomendação: o primeiro envio da história do sistema é manual e conferido a olho,
porque conversão enviada ao Google não se apaga. O cron `40 6 * * *` entra depois disso.

## Agenda mobile (LAPE-32, etapa 3): eixo decidido e plano escrito (~17h BRT)

Pesquisa de padrões mobile 2026 + mockup comparativo + plano em TDD. Nada de código de app ainda.

**Pesquisa (o que mudou e o que foi descartado)**
- Confirmado: no celular a agenda (lista cronológica) vence a grade — grid fica inacionável abaixo de 360px; bottom sheet vence modal para data; Material 3 confirma 3-5 destinos na barra inferior; "priority+"/row→card para tabela densa (é o que ListaAlunosMobile já faz).
- Descartados por não servirem ao caso: glassmorphism e layout adaptado por IA.

**Decisão do eixo (Hugo escolheu a opção C sobre mockup navegável)**
- Lista cronológica (hora = ordem) + trilho horizontal de chips de professor. NÃO é o eixo do desktop, que é matriz pessoa × hora.
- Aritmética que fechou a questão: 375px − 172px de rótulo = 203px para o dia inteiro; numa janela 08h-21h dá ~15px por hora, e uma aula de 50 min vira um retângulo de 13px.
- Mockup: https://claude.ai/artifact/TDQn2F5nHU9Ec2d7jXzFuQ (privado) — 4 artboards com o mesmo dia e as mesmas 10 aulas: desktop de hoje + as 3 opções. O artboard C é interativo, com o swipe funcionando em 3 dias.

**Dois achados sobre o DESKTOP que o mockup revelou**
1. A colisão de SALA entre professores diferentes é invisível no agrupamento por professor — as duas aulas ficam em trilhos separados, cada uma sozinha no seu, e `resumoSobreposicao` (que opera dentro de um trilho) não vê nada. Só aparece trocando o toggle para Sala, ou seja, depende do modo em que a pessoa está.
2. `estadoDaAula` é cascata com `return`: um estado só pode vencer. Aula experimental acontecendo agora sai violeta e o "agora" se perde, porque um cartão de 88px só tem a borda esquerda para codificar. A linha da lista tem 350px e cabem os dois.

**Swipe de dia (pedido do Hugo)** — 6 regras registradas no plano: área só no corpo da lista (o trilho de chips rola no próprio eixo); travamento de eixo nos primeiros 10px, decidido uma vez e congelado; confirmação em 25% da largura; resistência de 25% nas pontas; 260ms cubic-bezier(0.2,0,0,1) com transição desligada durante o arrasto; prefers-reduced-motion troca sem deslizar; as setas permanecem.

**Plano:** `docs/superpowers/plans/2026-09-21-mobile-etapa3-agenda.md`, commit `8dddd8a0`. 6 tarefas em TDD.
- A bifurcação fica DENTRO da AgendaPage (padrão do AlunosPage), não na rota (padrão do DashboardResponsivo): a Agenda tem 4 visões e esta etapa porta 2.
- `/app/agenda` NÃO entra em ROTAS_PORTADAS — vai para `abasPortadas.ts`, o mecanismo criado após a reversão de Alunos em 14/09. Marcar a rota apagaria a faixa de Chamada e Calendário de uma vez.
- `colisoesDeSala` é função nova; NÃO reusar `resumoSobreposicao`, que responde outra pergunta (sobre o dia inteiro ela devolveria "4 aulas às 11:00", que é a escola funcionando).
- `useAgendaDia` ganha `lerDoCache` (aditivo): o palco monta 3 dias e o vizinho precisa aparecer DURANTE o arrasto — o prefetch já o trazia, só não havia porta para lê-lo de fora.

**Autorrevisão do plano pegou 5 defeitos meus antes de commitar:** `aulaEmAndamento(aula, minutos)` não recebe data (eu tinha escrito `(aula, data, agora)`); `void largura` de gambiarra no `aoMover`; a Task 5 sem código, só bullets, que é exatamente o que o formato proíbe; `React.PointerEvent` sem import; e dois regex de teste que reprovariam a implementação correta.

**Pendências:** nenhuma linha de código de app foi escrita — a execução das 6 tarefas está por começar.
