# 2026-07-06

## Relatório diário WhatsApp — forma de pagamento inline
- Pedido da Vitória: mostrar a forma de pagamento na mesma linha do valor (`Pass: R$ 425,00 Crédito` / `Parc: R$ 387,00 Pix`) em vez da linha separada `💳 Pag: Não informado`. Implementado no relatório diário (`ComercialPage.tsx`, `gerarRelatorioDiario`), no mesmo padrão que já existia no relatório mensal.
- Descoberta ao investigar: o `(forma)` ao lado do **Pass** nunca aparece de fato — `alunos` não tem coluna de forma de pagamento do passaporte (só existe em `leads`, fase de negociação/lead). Só o **Parc** funciona de verdade (vem de `alunos.forma_pagamento_id`). Mantido assim por decisão do Hugo ("por enquanto deixe assim").
- Casos reais conferidos sem forma de pagamento: Sergio Gabriel Soares Costa (id 1746), Arthur Pontes Rodriguez (1813), Carlos Tayrone Mendes Namorato (1812), Beatriz Gonçalves Pereira (1811) — todos com `forma_pagamento_id` NULL no banco, confirmado via SQL.

## processar-matricula-emusys v28 — captura tipo_pagamento na matrícula nova
- Investigação da causa raiz do "Não informado": o webhook Emusys (`matricula_nova`) traz `matricula.tipo_pagamento` preenchido em **~36% dos casos reais** (amostra de 127 webhooks: 81 nulo / 40 "Pgto Recorrente" / 5 "Pix" / 1 "Cartão de Crédito"), mas `handleMatriculaNova` nunca usava esse dado — só `handleRenovacao` usava o mapeamento `mapTipoPagamentoToFormaId` (já existente).
- **Fix (commit `78ec640`, deploy via MCP, `verify_jwt:false`, versão 44 ACTIVE):** `handleMatriculaNova` agora grava `alunos.forma_pagamento_id` nos 3 caminhos de escrita (segundo curso, update de matrícula existente, aluno novo).
- Corrigido também um drift de versionamento que já existia no arquivo: o cabeçalho dizia v27 mas a constante `VERSAO` estava travada em v25 — bump pra v28, changelog documentado no topo do arquivo.
- **Limite conhecido (não é bug):** passaporte nunca tem forma de pagamento em nenhuma fonte do Emusys (nem webhook, nem `GET /matriculas`). Os ~64% de matrículas sem `tipo_pagamento` no webhook continuam dependendo do sync noturno (`sync-matriculas-emusys`, quando a API já tiver o dado) ou de preenchimento manual — não há como automatizar 100%.

## Pendência aberta — experimentais de aluno já matriculado inflando conversão
- Vitória relatou (mensagem repassada pelo Hugo): experimentais de troca de professor/horário ou 2º curso de alunos **já matriculados** contam como se fossem leads novos no funil comercial, inflando a taxa exp→matrícula.
- Confirmado: é a mesma dúvida em aberto já documentada em memória (Alf × Hugo, decisões conflitantes de 20/06 e 23/06). A régua do **professor** já foi corrigida em 23/06 (conta só lead puro); o **funil comercial geral** (KPIs de conversão) ainda não tem esse filtro.
- Proposta da Vitória: separar por **quem agendou** a experimental (Comercial vs ADM) em vez de tentar diferenciar "lead novo" de "aluno existente" nos dados. Achado técnico: o payload do webhook do Emusys já traz `nome_atendente` (quem agendou), mas o n8n (`j41tPbyjGXUQUxrN`) não captura esse campo hoje — só `nome_professor`, `curso`, `lead_id`, `emusys_aula_id`.
- Nada implementado ainda — só diagnóstico. Próximo passo: mapear em qual tela/KPI exato a Vitória está vendo a inflação, e decidir a regra (capturar `nome_atendente` + lista comercial×ADM por unidade).
