# 2026-09-25

## Fideliza+ no celular — a dupla primeiro (LAPE-32) (~09h BRT)

**O que foi feito.** Aba Fideliza+ do Administrativo ganhou versão mobile
(`FidelizaMobile` + `src/lib/fidelizaMobile.ts`), e `'fideliza'` entrou em
`ABAS_PORTADAS`. Commit `5925f449`.

**O achado que corrigiu a proposta.** O desktop tem DOIS ramos e eu só havia
medido um. `isSuperAdmin = isAdmin && (!unidadeSelecionada || === 'todos')`:

| ramo | rolagem a 390px | lateral | truncados |
|---|---|---|---|
| Consolidado (matriz métrica × dupla) | 4.198px (5,0 telas) | não | 0 |
| unidade escolhida (a própria dupla) | 2.751px (3,3 telas) | não | 0 |

Ou seja: **a inversão de eixo que eu havia proposto no artifact já existia** —
para quem escolhe uma unidade. O que faltava era valer para os dois ramos. Por
isso a bifurcação mobile vem ANTES do `isSuperAdmin`, e é uma tela só (há
assert cobrando essa ordem).

**Depois:** Consolidado 919px (1,1 tela), unidade escolhida 740px (0,9), 0
tabelas, 0 truncados, 0 rolagem lateral. O único alvo <44px é a pílula de
unidade do shell (27px), que não é desta frente.

**O que a tela pequena diz e a grande não:** quanto falta. O desktop escreve
"Atencao: inadimplencia acima da meta"; o celular diz "baixar 2,0 ponto" e
"faltam R$ 3.000". O verbo vai junto do número — "0,8%" sozinho não distingue
estar acima de estar abaixo.

**🔴 Defeito pego medindo, não lendo.** Com unidade escolhida a RPC devolve só
aquela dupla, e a medalha do pódio sai do índice no array: a Barra aparecia com
🥇 tendo 52 pontos contra 66 do Recreio. Pódio agora exige 2+ duplas, e a tela
diz por que a comparação sumiu. Travado por teste.

**Decisões medidas no banco (as três que estavam pendentes no artifact):**
- A dupla é atributo da **unidade** (`unidades.farmers_apelidos`): Barra
  `Duda & Arthur`, CG `Mayra & Jhon`, Recreio `Fefe & Vi`. Não existe vínculo
  usuário→dupla, e não precisa — é usuário→unidade, que o shell já resolve.
- `programa_fideliza_historico` tem **0 linhas** nas três unidades. O bloco
  Histórico Trimestral do desktop exibe 4 linhas de traço mais uma "média
  anual" que repete o único trimestre vivo. Fica no computador por não ter dado.
- `programa_fideliza_penalidades` também está vazia (0 linhas).

**⚠️ Armadilha preservada de propósito.** O hook do desktop fazia
`metas[chave] || 3000`, e `||` trata meta ZERO como ausente. Ao mover o mapa
para fonte única (`metaLojinhaDaUnidade`), troquei por `??` e isso **mudaria o
placar** de quem zerasse a meta na tela de Configurações. Voltei ao `||`, com a
nota e um teste. Hoje é teórico (CG 5.000, Barra e Recreio 3.000).

**Desktop provado intocado a 1440px:** 3 sub-abas, matriz com larguras
227/288/109/173/167/114, mesmas 7 linhas, mesmos 66/52/30 pontos — que são os
mesmos números que o celular exibe.

**Testes.** 17 novos, **7/7 mutações pegadas** (uma 8ª era equivalente por
construção: com `bateu=false` o sinal de `valor - meta` já é o certo, então
`Math.abs` não é mutante). Suíte `test`: **1.313/1.313**.

⚠️ **`npm test` está falhando no `pretest`, e não é desta frente:**
`tests/presencaRosterV2PublicacaoGatadaPostgres.test.mjs` sai com **exit 127**
(binário `psql` não alcançado pelo `spawnSync` do Node no Windows) e derruba a
cadeia antes da suíte principal rodar. Nada do meu diff toca presença. Rodar
com `npm test --ignore-scripts` enquanto isso.

**Pendências.** Lojinha, Farmer, Caixa e Entrada seguem com a faixa âmbar. Os
commits `819d839c`, `abcdceff` e `5925f449` estão **locais, não pushados**.

## Lojinha no celular — produto vira linha (LAPE-32) (~10h BRT)

**Escolha por medição.** Medi as quatro abas restantes a 390px com unidade
escolhida (no Consolidado o Caixa nem abre — recusa por desenho):

| aba | rolagem | alvos <44px | tabela mais larga |
|---|---|---|---|
| **Lojinha** | 19.509px · 23 telas (5 sub-abas) | 1 | 354px |
| Caixa | 2.821px · 3,3 | 8 | **900px** |
| Farmer | 2.628px · 3,1 | 6 | — |
| Entrada | 796px · 0,9 | 5 | — |

**O que foi feito.** Sub-aba Produtos ganhou versão mobile (`ProdutosMobile` +
`src/lib/lojinhaMobile.ts`). Commit `498711eb`.

**Antes → depois (Barra, 390px):** 7.496px (8,9 telas) → **1.706px (2,0)**;
326px por produto → ~62px por linha; 1 tabela → 0; 0 rolagem lateral, 0
truncados. A ficha abre em folha de 465px, cabe sem rolagem interna, e o botão
Editar abre o **mesmo `ModalProduto`** do desktop — exercitado ponta a ponta.

**⚠️ Dois defeitos MEUS, pegos medindo e corrigidos antes do commit:** os chips
nasceram com 36px (dez alvos abaixo do mínimo) e o campo de busca com 20px. A
altura mínima tem de ir no elemento que **recebe** o toque, não só no contêiner.

**⚠️ `'lojinha'` NÃO entrou em `ABAS_PORTADAS`.** Só Produtos foi portada e a
aba tem cinco sub-abas; marcar a rota apagaria a faixa âmbar de Vendas,
Estoque, Comissões e Configurações de uma vez — o erro de 14/09. A faixa sai
quando Estoque também estiver pronta.

### 🔴 Dois defeitos do DESKTOP achados de passagem (não corrigidos)

Os dois **mentem em silêncio** — não há mensagem de erro, há um número
tranquilizador. Decisão do Hugo pendente, porque consertar muda o que o
computador mostra.

1. **[TabProdutos.tsx:72](src/components/App/Lojinha/TabProdutos.tsx#L72)** —
   `unidadeId === 'todos' ? unidadeId : unidadeId`: **os dois ramos do ternário
   são idênticos**. No Consolidado manda `unidade_id=eq.todos` ao PostgREST →
   **40 requisições com HTTP 400**, e o `|| 0` da linha seguinte transforma a
   falha em "estoque 0". Todo produto parece esgotado. Mesma família do card
   SOZINHOS de 22/09 (`?? 0` sobre resposta que falhou).
   ⚠️ De quebra, é **N+1**: uma consulta por produto (20 requisições).
2. **`professores` não tem FK para `unidades`** (medido: 0 FKs), e
   `useFeedbackPendente.ts:40` pede o embed `unidades(nome)` — o PostgREST
   recusa com 400 (PGRST200). A lista de professores do Painel Farmer **nunca
   carrega**.

**Fonte única.** O predicado de filtro saiu do corpo da `TabProdutos` para
`produtoPassaNoFiltro`, sem mudar uma comparação — as duas telas filtram a
mesma lista e o celular exibe a **contagem** nos chips, o que tornaria a
divergência visível na hora. Há assert proibindo o `filter` inline de voltar.

⚠️ **`estoque_baixo` tem duas leituras de propósito:** no FILTRO inclui o
zerado (regra do desktop, intocada); no CHIP não soma, porque os dois chips
ficam lado a lado e contagens sobrepostas fazem quem lê somar errado.

**Testes.** 14 novos, **7/7 mutações pegadas**. Suíte: **1.327/1.327**.
⚠️ Um assert meu reprovou valor certo: `toLocaleString('pt-BR')` separa `R$` do
número com **espaço não separável** (U+00A0), e a mensagem de erro mostrava as
duas strings idênticas. Assert de moeda vai por regex.

**Desktop provado intocado a 1440px:** 9 colunas, larguras
70/299/157/105/102/130/105/83/73, 20 linhas, mesma primeira linha.

**Artifact.** Ganhou **índice** com o estado de cada seção (versão 5) e a seção
da Lojinha com inventário medido (versão 4).

## PDV da Lojinha: o carrinho nascia abaixo da dobra (LAPE-32) (~10h30 BRT)

**Relato do Hugo:** *"eu clico no item e eu não vejo o modal, tenho que rolar para
baixo para ver"*, com print da aba Vendas → Novo Pedido.

**O "modal" não era modal.** É o painel do **Carrinho**: no celular ele é o segundo
item de um `grid grid-cols-1 lg:grid-cols-3`, ou seja vem logo **depois** da grade de
12 produtos — que mede **~1.200px a 390px**. Tocar num produto mandava o que a pessoa
acabou de escolher para fora da vista, e só a rolagem dizia se o toque funcionou.

**Antes de mexer, medi os candidatos errados** (banco isolado com o shell mobile real,
rota temporária, removida depois):

| modal | resultado |
|---|---|
| `ModalVendaDetalhes` | centraliza em **138→707px**, viewport 844 |
| `ModalProduto` | `max-h` 759,6px, `overflow-y: auto`, rola por dentro |

Os dois Dialog estavam **certos**. Se eu tivesse aceitado a primeira leitura
("modal abre no lugar errado"), teria mexido no `dialog.tsx` — que é compartilhado
pelo app inteiro — para consertar um defeito que não estava ali.

**O conserto:** no celular o carrinho vira (a) uma **barra grudada no rodapé**, que
aparece com o primeiro item e diz **qual** entrou e quanto já deu, e (b) a
`FolhaMobile` padrão com o formulário inteiro, a um toque.

⚠️ **`sticky bottom-0`, nunca `fixed`.** `fixed bottom-0` ficaria por cima da barra de
navegação do shell, e acertar isso exigiria conhecer a altura dela dentro da tela de
vendas — uma segunda verdade sobre o layout do shell, que apodrece sozinha quando o
shell mudar. Medido a 390px: barra em **712→781px**, navegação começando em **793px**,
idêntico em quatro posições de rolagem (topo, 300, 600, fim).

🔴 **O desktop não muda, e a prova é mecânica:** o painel lateral é o **mesmo JSX**
(comparado com o de `HEAD` normalizando espaços — idêntico). O corpo do carrinho virou
uma constante lida pelos dois ramos; duas cópias do formulário dariam **duas regras
para o mesmo dinheiro**, e desconto, vendedor e forma de pagamento poderiam divergir
entre computador e celular sem nada acusar.

⚠️ **`'vendas'` NÃO entrou em `ABAS_PORTADAS`** — só o PDV foi tratado; o Histórico
segue sendo a tabela do computador virando cards pelo CSS do shell.

**Estado:** `tests/lojinhaVendasMobile.test.mjs`, 6 casos, **8/8 mutações pegadas**;
suíte **1.333/1.333**; commit `0cea67c9`. `git fetch` antes: 0 atrás.

**Pendências que seguem abertas** (herdadas da seção anterior): os 2 defeitos do
desktop na Lojinha, `'lojinha'` fora de `ABAS_PORTADAS`, e os commits locais ainda
não pushados.

## Queda do banco (17h10–17h27) e auditoria de desempenho (~17h30 BRT)

**Incidente:** às 17h10 todos os crons do minuto falharam com `job startup timeout`; o
`dashboard-aquecer-caches` das 17h12 nunca terminou (`server restarted`); o Postgres
reiniciou às 17h26:43 e a API voltou às 17h27. Login indisponível ~16 min. Não era o
incidente público do Supabase (401 por JWT) — o nosso era 522/timeout, banco inacessível.
Causa exata do reinício (OOM x manual) não confirmada: `get_logs` do MCP quebrou (endpoint
`logs.all` removido pelo Supabase); ver Dashboard → Logs.

**Máquina:** `t4g.micro` (1 GB RAM, CPU burstable), banco 21,9 GB. Gráfico semanal do
painel: Disk IO 5–15% até ~22/09 → **100% a partir de 23/09**; CPU → 98%.

**Auditoria (medido):**
- `dashboard-aquecer-caches` (jobid 305, `*/4`, criado 23/09 21h): chama ~70 RPCs pesadas
  (faturas, KPIs, professores, comercial 12 meses × 2, agenda, inadimplência…). 24h:
  357 execuções, **9.420 s de banco (~2h37)**, média 26 s subindo (17 s em 24/09 → 34 s
  hoje), pico 114 s. É ~2/3 de todo o tempo de cron.
- **O cache por versão nunca acerta para o aquecedor:** `cache_versao_log` registra
  **~500 versões/hora** (leads 347/h, aulas_emusys 124/h, aula_alunos_emusys 84/h). A cada
  4 min tudo já foi invalidado → o aquecimento é um recálculo completo 360×/dia, com ou sem
  usuário olhando.
- `la-os-coletar-pg-cron` (*/15) varre o `cron.job_run_details` inteiro: 680 MB, 547 mil
  linhas desde 14/07, **sem retenção** → ~660 MB lidos do disco por execução.
- `sync_run_items` 13 GB (heap 8,4 GB + índices 5,3 GB) de 21,9 GB. Expurgo existe
  (jobid 304, 06h40, teto 500 mil itens/dia): 947 de 7.954 runs expurgados, 7.007
  pendentes; entram ~223 runs/dia. DELETE não devolve espaço sem repack.
- `emusys_fatura_source_events` 3 GB / 8,4 mi linhas — mesmo padrão, não investigado.
- Snapshots de situação (3 unidades, 4×/h) somam ~1.770 s/dia.

**Pendências:** decisão do Hugo sobre (1) espaçar/desligar o aquecedor, (2) retenção do
`cron.job_run_details`, (3) acelerar expurgo + repack do `sync_run_items`, (4) upgrade de
compute. Nada aplicado ainda.

**Aplicado (~17h50 BRT, aprovado pelo Hugo)** — migration
`20260926130000_desligar_aquecedor_e_acelerar_expurgo_sync_run_items`:
- `dashboard-aquecer-caches` (305) **desligado** (`active=false`, não deletado).
- `sync-run-items-expurgo-diario` (304) de `40 6 * * *` para `*/5 9 * * *` (06h00–06h55
  BRT, 12 × 1 mi) — na lacuna do `sync-faturas-fila-worker`. Todos os 7.663.143 itens já
  estavam consolidados; 1 rodada/dia levaria ~8 dias.

👁️ OBSERVAR: (a) amanhã após 07h BRT, `select count(*) from sync_run_retencao where
expurgado_em is null` deve cair de 7.007 para perto de zero; (b) Disk IO do painel caindo
nas próximas horas. ⚠️ DELETE não devolve disco — liberar os ~13 GB exige VACUUM FULL/
pg_repack do `sync_run_items` (trava a tabela; decisão pendente). Retenção do
`cron.job_run_details` (680 MB) ainda não aprovada.

## Monitor de desempenho do banco (LAPE-46) + regra de desempenho (~18h30 BRT)

- **Regra nova** `rules/desempenho_banco.md` + alerta no topo de "Regras Importantes" do
  `CLAUDE.md` (commit `0c546d27`): custo/dia = duração × frequência declarado na migration,
  medir a frio com BUFFERS, escalonar, ler incremental, retenção desde o início, reconferir
  em 24h/7d. Pedido do Hugo: "criar funções é fácil; o difícil é não explodir o banco".
- **Monitor no ar:** schema `monitoramento.banco_*` (migration `20260926140000`, commit
  `f27f5d79`) + coletor `check-banco-desempenho.py` na la-hq a cada minuto (fiscal mila
  `4abf398`), lendo o endpoint Prometheus do Supabase. Buffer local se o banco cair, alerta
  no Telegram (LAHQ Agents / API Logs), vigia do próprio coletor no `check-lareport-db.py`.
  Coleta de consumo medida em 40 ms.
- **Achados das primeiras leituras:** a MÁQUINA reiniciou às 17h26 (não só o Postgres);
  swap 30% → 50% em 1h depois do reinício; cota de IO a 121–166% do baseline por alguns
  minutos mesmo sem o aquecedor. Docs do Supabase: Micro recomenda banco até **10 GB** — o
  nosso tem 22 GB.
- **Falta:** aba "Banco" no LA-OS (gráficos: linha do tempo, mapa de calor hora×dia,
  ranking de consumidores, eventos) + deploy.

## Recital: 5 min de troca entre apresentações (LAPE-39) (~19h BRT)

- **O quê:** `calcularHorariosDaGrade` encadeava as apresentações do bloco sem folga — a programação
  prometia a seguinte no segundo em que a anterior acabava. Agora há 5 min entre apresentações do
  mesmo bloco (`INTERVALO_ENTRE_APRESENTACOES_PADRAO_SEGUNDOS = 300`); nada depois da última, e o
  intervalo entre blocos (45 min) segue igual. Topo da Grade passou a dizer os dois intervalos.
- **Efeito:** como o horário é derivado, Grade, Palco, Revisão, Check-in e impressões mudam juntos.
  Bloco de 3 apresentações de 5 min: 09:00–09:15 → 09:00–09:25.
- **Decisão:** constante no código, sem coluna em `evento` (sem migration). O cálculo já aceita
  `intervalo_entre_apresentacoes_segundos` opcional para quando virar configurável por evento.
- **Testes:** 4 testes atualizados (horários esperados) + 2 novos (configurável/zero; não soma ao
  intervalo entre blocos). Suítes `eventos*` 158/158.
