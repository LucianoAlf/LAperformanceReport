# 2026-07-29

## Observador v16: em vez de reimplementar o n8n, chamar as mesmas RPCs (~22h BRT)

Correção das três regressões achadas na sombra. `OBSERVADOR_DRY_RUN` continua `true` e o n8n
**não foi tocado** — segue sendo o único que grava.

**A descoberta que mudou o desenho:** lidos os dois workflows na fonte, o n8n **não tem lógica
própria**. Ele só chama duas RPCs canônicas:

| evento | workflow n8n | RPC |
|---|---|---|
| `lead_criado` / `lead_editado` | `EB0LibpOJCLhKp7M` | `upsert_lead(..., 'emusys', ...)` |
| `aula_experimental_*` | `j41tPbyjGXUQUxrN` | `registrar_experimental(...)` |

A v15 transcrevia essas RPCs para TypeScript — e era na transcrição que as três regressões
nasciam. A v16 **chama as mesmas RPCs, com os mesmos argumentos**. A única diferença passa a ser
o resolvedor de professor. Paridade por construção, não por cópia.

O que o n8n perde e a RPC já resolvia sozinha: match de lead em 3 camadas
(`emusys_lead_id` → telefone+unidade → nome+unidade), de-para de curso com `\s+(t|ind)$` e
`unaccent`, upsert por lead+data+horário+curso com `ON CONFLICT` no índice parcial
`uq_lead_exp_negocio_novo` (garantia atômica contra os reenvios simultâneos do Emusys),
`emusys_aula_id`, e a atualização das colunas legadas em `leads`.

**O que o observador ADICIONA** (regra "pode adicionar, remover não"):
1. Professor por `telefone_whatsapp` + `ativo=true`; fallback por `professores_unidades.emusys_nome`
   **da unidade** e também só entre ativos. O n8n usa `nome ILIKE '%'||$1||'%'` sem filtrar ativo.
2. `agente_comercial`, `motivo_arquivamento` e `etapa_pipeline_id` (do estágio do funil), aplicados
   como **delta depois** da `upsert_lead`, sem tocar no que ela gravou. Etapa só avança.
3. O de-para de canal caseiro foi **removido**: agora vai o texto cru para a RPC, que já tem a
   tabela completa (inclusive com acento — `LOJA DE MÚSICA`, `RÁDIO`). Fecha a dúvida do
   `ALUNO DA ESCOLA → Indicação` que estava em aberto desde 27/07.

**Paridade deliberada:** lead sem telefone continua ignorado, porque o nó `tem numero?2` do n8n
descarta antes do upsert. Criar esses leads seria adicionar linha que hoje não existe — decisão
para o Hugo, não default meu.

**Validado com replay de payload real** (5 casos, `id` do evento trocado para não bater na unique
`automacao_log_idempotency_key_uq`):

| caso | antes (v15) | agora (v16) |
|---|---|---|
| Giulia / Erick Osmy | prof 52 ✔ (n8n: 54) | prof **52** via telefone ✔ |
| Isadora / "Musicalização Infantil T" (CG) | curso **NULL** | curso **4** ✔, prof 62 via `emusys_nome_da_unidade` |
| Alexandre Neves (`lead_editado`) | `update(dry)` | `upsert_lead(dry) → update`, lead 11571 ✔ |
| experimental com `lead_id` ainda não vinculado | `ignorado: lead nao encontrado` | casa por **telefone** → lead 11021 ✔ |
| idem, e sem telefone do aluno | `ignorado` | casa por **nome** → lead 11021 ✔ |

⚠️ **Achado do teste:** o primeiro replay não gravou o resultado e o motivo não era o código — é a
unique parcial `automacao_log_idempotency_key_uq`. Reenviar um `body.id` já visto faz o INSERT do
resultado falhar, e o `catch` só escreve em `console.error`. Vale para qualquer replay futuro.

**Pendências:**
- Rodar 1 dia de sombra na v16 e conferir que `lead_not_found` zerou e que `professor_via` é
  majoritariamente `telefone`.
- Só então `DRY_RUN=false` + desligar os branches de lead/experimental do n8n no mesmo instante.
- **Corrigir a camada 1 da `registrar_experimental`**, que busca `emusys_lead_id` sem filtrar
  unidade. É bug independente do observador e afeta o n8n hoje (caso Vanice/Barra × Joaquim/Recreio).
- `lead_arquivado` continua fora do observador (o n8n trata com UPDATE direto).

## Observador em sombra: 2 dias de dado real, diff campo a campo (~21h BRT)

Verificação combinada em 27/07 (rodar 1–2 dias em sombra e comparar). **Nada foi alterado** — só leitura.
`OBSERVADOR_DRY_RUN` segue `true`, v15 é a versão deployada, e o n8n continua sendo o único que grava.

**Amostra (28–29/07, `automacao_log` / `workflow_id='observador-worker'`):** 229 lead (164 editado + 65 criado)
e 30 experimental. O caminho `resolverProfessor` finalmente rodou.

### ✅ O que o observador acerta e o n8n erra (motivo da migração)

**Giulia Spala Teixeira (Recreio), 2 aulas hoje.** Payload traz `Erick Osmy` + `(21) 97189-9022`.
n8n gravou `professor_experimental_id=54` (o cadáver); observador resolveu **52** por telefone+ativo.
É exatamente o bug de 27/07, reproduzido com dado de produção. O 54 voltou a receber 4 experimentais
em 2 dias (Giulia ×2, Gabriel Gerard, Lucas Barbosa) — a torneira segue aberta.

### 🚫 Três regressões que impedem ligar `DRY_RUN=false` hoje

**1. Match do lead — o observador tem 1 camada, a RPC tem 3.** Lido `registrar_experimental` no banco:
casa por `emusys_lead_id` → **telefone+unidade+não arquivado** → **nome+unidade**. O observador só faz a
primeira. Resultado medido: **13 das 26 experimentais (50%) deram `ignorado: lead nao encontrado na unidade`**.
Causa: `leads.emusys_lead_id` é preenchido *pelo n8n*, segundos depois — o lead existe, mas ainda sem a chave.
Confirmado no relógio: João Henrique (lead 1777, de março) consultado 14:50:24, `updated_at` 14:50:30;
Gabriel Gerard 11:39:05 × 11:39:11; Benjamin 17:47:15 × 17:47:21.
Efeito colateral do mesmo buraco: 5 `lead_editado` viraram `insert(dry)` de lead que **já existe**
(Giulia 11021, Gabriel Gerard 11555) — em modo escrita seriam duplicatas.

**2. Curso perdido em toda Campo Grande.** CG manda `"Piano T"`, `"Violão T"`, `"Canto T"`,
`"Musicalização Infantil T"`. O `resolverCurso` da edge só remove sufixo `" IND"`; a RPC remove
`\s+(t|ind)$` e `\s+para\s+instrumento$`, com `unaccent` e desempate `IND` por último.
**5 dos 13 upserts saíram com `curso_interesse_id` NULL** onde o n8n gravou curso (4, 10, 2, 6, 10).

**3. Upsert de experimental com chave errada.** A edge busca por `(lead_id, data_experimental)` com
`.maybeSingle()`; a chave de negócio canônica é `(lead + data + horário + curso)` e existe
`emusys_aula_id` (desde 23/06) justamente para multi-instrumento. Aconteceu hoje: Alice e Helena
Iribarne, mesmo lead, mesma data, **duas aulas** (15:00 e 15:30) — `maybeSingle()` com 2 linhas quebra.
A edge também não grava `emusys_aula_id`, e não replica os efeitos da RPC em `leads`
(`experimental_agendada`, `data_experimental`, etapa) nem o tratamento de cancelamento.

### Colisão de `emusys_lead_id` — a RPC é que está errada aqui

A camada 1 da `registrar_experimental` busca `WHERE emusys_lead_id = p_emusys_lead_id` **sem filtrar unidade**.
Caso vivo hoje: Vanice Vianna (Barra, emusys 7090) — o único lead com esse id está no **Recreio** (8248, "Joaquim").
O observador está certo em exigir `(unidade_id, emusys_lead_id)`; a correção da RPC é ganho independente.

### Recomendação

Não virar a chave ainda. Ordem: (a) fallback telefone→nome no `processarLead`/`processarExperimental`,
(b) normalização de curso igual à da RPC, (c) chave de upsert por data+horário+curso e gravar `emusys_aula_id`.
Depois disso, mais 1 dia de sombra para confirmar 0 `ignorado`, e aí `DRY_RUN=false` + desligar os branches
de lead/experimental do n8n no mesmo instante.

## Dashboard pedia a rede inteira em nome de usuário de unidade (~20h BRT)

Depois do guard de leitura entrar, o Arthur (e o `hugobarra`) ainda viam o card de Health Score em `0/0`. O console entregou a causa — **dois** 403 com a mesma natureza:

```
rpc/get_health_score_professor_v3_performance: 403   ← guard criado hoje
rpc/get_conciliacao_experimentais_v2: 403            ← guard de 15/07, pré-existente
"Acesso negado: unidade fora do escopo do usuario"
```

O segundo é anterior a mim e falha igual: **o Dashboard já mandava a unidade errada antes**, o guard novo só somou uma tela ao sintoma.

**Descartado, com verificação:** cadastro do `hugobarra` (id 27, perfil `unidade`, `unidade_id` = Barra, ativo, com vínculo de auth) e RLS em `usuarios` (policy de SELECT é `true` para `authenticated`).

**Causa real** — [`DashboardPage.tsx:171`](../src/components/App/Dashboard/DashboardPage.tsx):

```js
const unidade = filtroAtivo || 'todos';
```

`filtroAtivo` é null em **duas** situações que esse `||` funde numa só: "admin escolheu consolidado" e "o auth ainda não carregou quem é o usuário". Para admin, consolidado é resposta legítima. Para perfil `unidade` é uma afirmação falsa — pede a rede inteira em nome de quem só pode ver a Barra — e os dois guards recusam, corretamente.

O `AppLayout` usa `useUnidadeFiltro`, cujo `filtroAtivo = isAdmin ? unidadeSelecionada : unidadeId`; o `unidadeId` sai de `usuario?.unidade_id`, que é null até o `fetchUsuario` resolver.

**Conserto:** o Dashboard passou a usar `isAdmin ? (filtroAtivo ?? 'todos') : unidadeId` — mesmo padrão já documentado em [`AdministrativoPage.tsx:242`](../src/components/App/Administrativo/AdministrativoPage.tsx) — mais um gate `unidadePronta` que segura o efeito principal, o `useComercialOperacionalResumoV2` e o hook do Health Score enquanto a unidade não é conhecida. Antes, esses três disparavam com `null`.

Corrige de quebra a **Média Alunos/Professor da Barra**, que aparecia 6 por dividir pelos 44 professores da rede em vez dos 19 da unidade.

O **GestaoMensal** recebeu o mesmo tratamento (é a outra tela que consome o Health Score V3): as três abas só montam com `unidadePronta`.

**Detalhe que quase me fez fazer trabalho inútil:** o `isAdmin ? … : unidadeId` sozinho é *no-op*. Como `filtroAtivo = isAdmin ? unidadeSelecionada : unidadeId`, para não-admin ele **já é** o `unidadeId` — reescrever não muda valor nenhum. O que a reescrita entrega é a capacidade de **distinguir os dois nulls**; quem conserta de fato é o gate que vem depois. Por isso o `PreAtendimentoPage.tsx:86` foi deixado como está: repete o padrão, mas não consome nenhuma das RPCs com guard, e a mudança ali seria puro ruído no diff.

⚠️ **O padrão segue espalhado, sem correção:** `ProfessoresPage.tsx:78`, `AlunosPage.tsx:234`, `SalasPage.tsx:91`, `SucessoClientePage.tsx:27` e `TabelaAlunos.tsx:178` usam `unidadeSelecionada || 'todos'`, com a mesma corrida (o `useEffect` de `useUnidadeFiltro` só preenche depois). Se curam sozinhas porque refazem a busca quando a unidade chega — o custo é um 403 transitório no console. `ComercialPage` já faz certo.

**Não verificado:** confirmei que compila e que os tipos fecham, não que a tela do `hugobarra` enche. Falta o teste em produção depois do deploy.

## Health Score V3 negado para todo não-admin (~18h BRT)

O Arthur (gerente da Barra) reportou a aba **Professores → Carteira** exibindo
`HEALTH_SCORE_V3_ACESSO_NEGADO: configuracao exige permissao global`, e a aba **Performance** caindo em tela branca do React Router com `Cannot read properties of null (reading 'numerador')`.

Eram **dois problemas encadeados**, não um.

### 1. O guard de escrita estava numa RPC de leitura

`get_health_score_professor_v3_performance` e `..._snapshot_modal` abriam com `perform fn_health_score_professor_v3_ator_gerenciador()` — função que exige a permissão `professores.editar`. Guard de **gerenciamento** aplicado a endpoint de **leitura**.

**Causa raiz, mais profunda do que parecia:** `fn_usuario_atual_tem_permissao` consulta o sistema granular (`usuario_perfis` + `perfil_permissoes`), e **`usuario_perfis` está vazia — 0 linhas para 29 usuários ativos**. Só passa quem cai no atalho legado dentro de `usuario_tem_permissao`:

```sql
IF (perfil = 'admin' AND ativo) THEN RETURN TRUE;  -- daqui pra baixo consulta tabela vazia
```

Ou seja, **toda checagem de permissão granular do sistema hoje significa "é admin?"**. A permissão `professores.carteira` existe e está ligada aos perfis Gerente/Farmer/Professor — mas ninguém está nesses perfis. O mesmo vale no frontend: `fetchPermissoes` carrega tudo para admin e chama a RPC `usuario_permissoes` (vazia) para o resto, então `hasPermission()` é sempre `false` para não-admin.

**Alcance real:** não era só a Carteira. A mesma RPC alimenta `DashboardPage` e `GestaoMensal/TabProfessoresNew`. Três módulos, 17 contas de perfil `unidade`.

**Conserto:** `fn_health_score_professor_v3_ator_leitura(p_unidade_id)` — admin lê tudo (inclusive consolidado); perfil `unidade` lê só a própria unidade; demais negados. Apoia-se no **escopo por unidade**, que é o modelo que de fato funciona no projeto (mesmo padrão de `get_conciliacao_experimentais_v2`), e não no sistema granular dormente.

O `ator_gerenciador` **não foi tocado** — segue protegendo 14 funções, entre elas as de escrita (`ativar_`, `criar_`, `salvar_`, `simular_`, `fechar_ciclo`).

⚠️ **A troca nas 4 sobrecargas foi feita por `pg_get_functiondef` + `replace` + `execute`**, não reescrevendo os corpos à mão: essas funções foram redefinidas em 28/07 pela migration `20260728225000` e transcrever arriscaria reverter aquele trabalho. A migration aborta sem alterar nada se o guard não for encontrado ou se não trocar exatamente 4.

**Decisão (Hugo):** perfil `professor` (1 usuário, Matheus/Recreio) segue negado. Dar acesso exigiria filtrar pela própria carteira — senão ele veria o score dos colegas de unidade. Não é regressão: ele já não via antes.

### 2. Crash de null na célula de métrica

Bug independente, que atinge **inclusive admin**:

```tsx
{metric?.numerador !== null && metric?.denominador !== null && (
  <p>Base: <strong>{metric.numerador}/{metric.denominador}</strong></p>
)}
```

Com `metric` null, `metric?.numerador` devolve `undefined`, e `undefined !== null` é **true** — a guarda aprova e o `metric.numerador` seguinte derruba a página. E `metric` é null **por desenho**: o fallback de quando não há snapshot define `metric: null` explicitamente. A linha logo acima (amostra) já testava `!== null` **e** `!== undefined`; esta ficou pela metade.

Verificadas as outras 3 ocorrências do padrão no arquivo — todas completam a checagem. O `ModalDetalhesProfessorPerformance` tem `if (!metric) return` antes do uso.

### Pendências

- **Avisar o Luciano**: ele mexeu nessas funções em 28/07 e está ativo hoje. Se redefinir a RPC de performance, o guard volta ao antigo.
- **Ligar o sistema de permissões granulares** é projeto à parte: 29 usuários a mapear entre 6 perfis, mexendo em 4 funções de uma vez. Não fazer às pressas para desbloquear tela.

## API do Emusys: duas versões novas (~17h BRT)

Skill `emusys-api` atualizada de 1.2.7 → **1.3.1**.

- **v1.3.0** (28/07): endpoint novo `GET /crm/aniversariantes` — intervalo de dia/mês ignorando o ano, com papéis (aluno, ex_aluno, responsável, professor, usuário, lead). `limite` vai até 100, diferente dos outros endpoints.
- **v1.3.1** (29/07): **mudança de semântica no `status` de `/matriculas`**. Antes, matrícula com trancamento temporário vigente vinha como `ativa`. Agora são três valores exclusivos: `ativa`, `trancada` (com `trancamento_ativo`) e `inativa` (era `finalizada`, com `motivo_inativa` = `interrompida`/`concluida`).

**Verificado ao vivo:** já está em vigor nas 3 unidades. 28 matrículas migraram de `ativa` para `trancada` (CG 18, Barra 6, Recreio 4), motivos reais "Financeiro", "Saude", "Viagem". Como o cron das 02h rodou antes da mudança entrar, 26 delas ainda constavam como `ativa` na nossa jornada no momento da checagem — o `sync-matriculas-emusys` varre `status=todas` e mapeia o campo, então corrige sozinho no próximo ciclo.

`motivo_inativa` é ganho relevante e ainda não aproveitado: separa **evasão** de **contrato concluído**, coisa que antes era indistinguível.

## Achado colateral: o Tailwind é servido por CDN em produção

O `index.html:9` carrega `https://cdn.tailwindcss.com`, com a config do tema inline. Fui verificar e **não é resquício, é a arquitetura**:

| | |
|---|---|
| `tailwindcss` no `package.json` | não existe (só `tailwind-merge`, outra lib) |
| `tailwind.config.js` / `postcss.config.js` | não existem |
| Utilitários (`.flex{`, `.grid{`, `.px-4{`) no CSS buildado | **0 ocorrências** |

Todo o estilo do sistema é gerado **no navegador, em runtime**, por script de CDN externo. Se a CDN cair ou for bloqueada, o report abre sem estilo.

⚠️ Migrar não é trivial: hoje, sem build, classe montada dinamicamente funciona; com o plugin PostCSS, é removida pelo purge se não estiver no safelist. Merece branch própria e teste visual tela a tela.

## Campo Grande com 7% de inadimplência (~16h BRT)

Investigando a diferença entre unidades no banner de inadimplência, com dados ao vivo da API:

| | Barra | Recreio | Campo Grande |
|---|---|---|---|
| Taxa de inadimplência | 1,9% | 0,5% | **7,0%** |
| Cobertura de cobrança automática | **88,8%** | 54,2% | **41,5%** |
| Inadimplentes sem cobrança automática | 1 de 5 | 0 de 2 | **21 de 36** |
| Inadimplentes com cobrança ativa mesmo assim | 4 | 2 | **15** |

Forma de pagamento **não explica**: o mesmo Crédito Recorrente falha 8,0% em CG contra 0,4% no Recreio. São dois problemas somados — cobrança automática não implantada (21 casos) e cobrança ativa sendo recusada (15 casos).

⚠️ `alunos.forma_pagamento_id` **não é confiável** como indicador: diz 113 em "Crédito Recorrente" em CG, enquanto a API diz 212 com cobrança automática de algum tipo. O campo correto é `cobranca_automatica` de `GET /matriculas`, que não persistimos.

Assunto para o Arthur, não para código: elevar a cobertura de cobrança automática em CG ao patamar da Barra endereça 21 dos 36 casos sozinho.
