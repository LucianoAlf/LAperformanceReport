
- o ltv tambem conta dados historicos e nao filtra corretamente pelos dados.

- Professor Gabriel Antony Trabalha em duas unidades mas so esta configurada para uma.


Do adm:
1. ~~A Grade horária está com horários diferentes do real(emusys), aulas em horários diferentes.~~ **RESOLVIDO 2026-05-20**: bug de timezone em `processar-matricula-emusys` (horário sem TZ vinha +3h errado). Fix via RPC `sincronizar_grade_horaria_alunos()` + cron diário 22h30 BRT (jobid 17). Plano: `docs/superpowers/plans/2026-05-20-fix-grade-horaria.md`.
2. ⁠Distribuição dos alunos por professor irregular.
3. ⁠Quantidade de novas matriculas irregular, ferindo o saldo liquido no report. 

Do comercial: 
1. Irregularidades na  sinalização das matriculas dos professores, experimental computada, mas matricula não direcionada aos professores. 
2. ⁠Quantidade de Matriculas no mês diferente entre os dois sistemas, ora para mais ora pra menos.

"Gap" dos 16 leads (INVESTIGADO 2026-05-21 — NÃO É BUG):
- Os 16 leads que apareciam como "gap" (data_experimental preenchido mas sem linha em lead_experimentais) são na verdade **VISITAS ESCOLARES** corretamente registradas pela Mila SDR.
- Todos têm `tipo_agendamento='visita'`, `status='visita_escola'`, `etapa_pipeline_id=6` e linha correspondente em `visitas` (`criado_por='mila'`).
- O campo `leads.data_experimental` é reaproveitado como "data de qualquer agendamento" (visita OU experimental). Pra distinguir, filtrar por `tipo_agendamento`.
- **Implicação pra queries de experimentais**: SEMPRE filtrar `tipo_agendamento='experimental'` (ou ler direto de `lead_experimentais` que só contém experimentais reais).
- **Dívida de design** (não urgente): renomear `leads.data_experimental` → `leads.data_agendamento` pra deixar a semântica explícita.

Pendências de fix (2026-05-21):
- **Edge `processar-matricula-emusys` v19 — 4 fixes aplicados (RESOLVIDO 2026-05-22 17h57 UTC, Supabase version 20)**: 4 bugs encadeados que apareceram no caso Thallyson Victor de Aguiar CG (Cavaquinho mat 2415 + Canto mat 2416 evadiram simultâneo, só 1 evasão gravada, Canto ficou ativo, vínculo Emusys sobrescrito). Fix manual do estado anterior aplicado 22/05 22h30 BRT (UPDATEs + INSERT id 2988). **Fixes no código (v19)**:
  1. **`registrarMovimentacao` dedup por curso_id**: filtro inclui `curso_id` agora (era `nome+unidade+tipo+mês`). Resolve subcontagem em evasão/renovação/trancamento quando pessoa tem 2+ cursos no mesmo mês.
  2. **`backfillMatriculaId` 2 guards**: (a) nunca sobrescrever `emusys_matricula_id` já existente; (b) recusar se outro aluno na mesma unidade já tem aquele ID. Previne corrupção silenciosa quando matching cai em fallback ruim.
  3. **`buscarAluno` Camada 3 endurecida**: aceita `eventoTipo`. Em `matricula_finalizacao`, filtra alunos já evadidos hoje + prefere match por `curso_id` quando disponível. `ALUNO_SELECT` ganhou `data_saida` pra suportar.
  4. **`resolverCursoId` valida nome**: match por `cursos.emusys_ids` valida que o nome do curso bate com `nomeCurso` do payload. Necessário porque `emusys_ids[]` mistura IDs de unidades diferentes (IDs Emusys são por escola) — Canto Emusys=2 resolvia Bateria (cursos[27].emusys_ids=[2,28]). Se nome não bate, descarta e usa fallback por nome puro.
  - **Não cobre**: casos passados (continuam com dados corrompidos — auditoria + backfill manual caso a caso). O conceito de "pessoa com 2 cursos = 2 linhas em `alunos`" NÃO é duplicata, é design intencional (1 linha por matrícula, marcada com `is_segundo_curso=true`). Ver `regras-negocio.md` seção "Modelo de dados de `alunos`".
- **Migrar TODAS as aulas experimentais para usar `lead_experimentais` como fonte única (proposto 2026-05-22)**: hoje o sistema mantém estado duplicado — flags em `leads` (`experimental_agendada`, `experimental_realizada`, `faltou_experimental`, `data_experimental`, `horario_experimental`, `professor_experimental_id`) E linhas em `lead_experimentais`. Essa duplicação é a raiz de quase todos os bugs recentes (paradoxos de flags, gap de 32% entre leads e canônico, cancelamento que não propaga, manual sync no ComercialPage, Alice/Giovana). **Plano de migração**:
  1. **Auditoria de uso**: mapear todos os hooks/componentes/queries que leem ou escrevem nas flags legadas (`useLeadsCRM`, `ComercialPage`, `AgendaTab`, `DashboardPage`, `useKPIsComercial`, edge functions Emusys, registrar_experimental, sync-presenca-emusys, automacao_log, funnel queries).
  2. **Refatorar leitura**: tudo que hoje lê flags do lead passa a fazer JOIN/EXISTS em `lead_experimentais` (filtrando por status + janela de data conforme contexto).
  3. **Refatorar escrita**: webhook Emusys (n8n `j41tPbyjGXUQUxrN`) e edge functions param de UPDATE em leads.experimental_*; só INSERT/UPSERT em `lead_experimentais`. Função `registrar_experimental` simplifica drasticamente (some o Bloco 4).
  4. **Backfill final**: garantir que toda linha de leads com flag legada tem linha equivalente em `lead_experimentais` (já cobrimos a maioria via v26/v27 do sync — falta confirmar 0%).
  5. **Drop colunas legadas**: depois de N semanas com tudo lendo da canônica e zero erro, DROP COLUMN em leads.
  6. **Status no lead vira derivado**: `leads.status='experimental_agendada'` passa a ser apenas um cache do estado mais recente da experimental ATIVA daquele lead (computed via trigger ou view materializada).
  - **Risco**: alto. Mexe em quase todas as telas e na automação principal. Precisa fazer em fases pequenas com rollback fácil.
  - **Resolve em cascata**: bug das flags (paradoxos), gap leads→canônico, divergências de contagem entre sistemas, simplifica registrar_experimental, elimina necessidade de sync manual em ComercialPage.
- **Fix flags `registrar_experimental`** (medido 2026-05-21): a função Postgres `registrar_experimental` (9 e 10 params) não zera `experimental_realizada`/`faltou_experimental` ao agendar uma nova experimental. Bloco 4 das duas funções tem o mesmo bug. **Estado atual no banco**: 456 leads em estado paradoxal (`experimental_agendada=true` + realizada/faltou=true), sendo 400 com realizada=true, 66 com faltou=true e 10 com ambos=true. Desses, apenas **2 são paradoxos reais ATIVOS** (`data_experimental >= hoje` E último status canônico = `experimental_agendada`) — os 454 restantes são históricos legítimos (data passada). Mas o bug é **preventivo**: toda nova experimental agendada para lead que já realizou/faltou cria paradoxo novo. **Solução**: adicionar CASE WHEN no UPDATE do Bloco 4 zerando flags quando `p_status='experimental_agendada'`:
  ```sql
  experimental_realizada = CASE WHEN p_status = 'experimental_agendada' THEN false ELSE experimental_realizada END,
  faltou_experimental  = CASE WHEN p_status = 'experimental_agendada' THEN false ELSE faltou_experimental  END,
  ```
  Resolve o paradoxo da Alice (lead 7815) e os 3 críticos do painel Saúde. Aplicar nas DUAS funções (9 e 10 params). Migration deletada (estava em `supabase/migrations/20260521_fix_registrar_experimental_zera_flags.sql`) — recriar quando for aplicar. Sem backfill por enquanto (decidido em 21/05).
- **Fix AgendaTab**: aba filtra leads por `data_contato` (mês) e busca apenas via `useLeadsCRM`, mas deveria buscar experimentais por `data_experimental` (mês) na tabela `lead_experimentais` (com fallback em `leads` enquanto o gap dos 16 não for resolvido na origem). Implementação anterior em `useExperimentaisDoMes.ts` (hook deletado, recriar). Só frontend, sem mudança de banco. Reversível sem risco.
- **Cancelamento de experimental não propaga ao banco (descoberto 2026-05-21, ampliado 2026-05-21)**: webhook `Fucq0bQwF4oeuWnv` recebeu evento `aula_experimental_cancelada` 2× para Maria Luiza (lead 9041, tel 5521980039421, Barra) e a execução retornou success, mas no banco: linha em `lead_experimentais` continua `status='experimental_agendada'` (não virou `cancelada`) E flags em `leads` continuam `experimental_agendada=true`. Resultado: o aluno cancelado continua aparecendo no Comercial → Barra → Hoje como agendado.
  - **Investigação realizada 2026-05-21**: Mapeamos o fluxo completo. O webhook pai dispara 3 sub-workflows em paralelo, e o sub-workflow `j41tPbyjGXUQUxrN` ("atualizar lead para experimental") é quem persiste no Supabase. Esse sub tem um Switch "Tipo Evento" → branch "Cancelar Experimental" → node Postgres → "Log Cancelamento".
  - **Próximos passos pra debug**:
    1. Pegar uma execução recente de `aula_experimental_cancelada` no `j41tPbyjGXUQUxrN` (filtrar por payload `evento=aula_experimental_cancelada`)
    2. Checar se a branch "Cancelar Experimental" foi de fato executada
    3. Ver qual SQL roda: deve chamar `registrar_experimental(p_status='cancelada')` ou UPDATE direto
    4. Validar se o matching por `(emusys_lead_id, unidade_id)` está correto (lembrando: IDs Emusys são por unidade, ver memory `emusys-api.md`)
    5. Verificar se a função `registrar_experimental` no bloco 2 (`IF p_status IN ('cancelada', 'novo')`) está sendo acionada
  - **Mitigação parcial**: a Opção B do sync-presenca-emusys v27 (a aplicar) tem fallback de cancelamento via `if (exp.cancelada)` no `reconciliarExperimentaisOrfas`. Não corrige o bug raiz, mas pega cancelamentos como rede de segurança.
- **sync-presenca-emusys: bug Falta→Realizada (RESOLVIDO 2026-05-21 v27, Opção B)**: a função `confirmarExperimentais` marcava como `experimental_realizada` SEM checar presença individual do aluno — só porque a aula existia no `aulas_emusys` e não estava cancelada. Causava ~100 falsos positivos/mês: aluno faltava no Emusys mas o cron flipava pra realizada. **Fix v27 aplicado**: (1) `confirmarExperimentais` não promove mais — só auto-faltou em >7d + detecção de cancelamento + log `pendente_presenca`. (2) `reconciliarExperimentaisOrfas` ganha fallback contextual: quando matching por nome falha, tenta via (data + professor + unidade) — se há 1 exp agendada nesse trio E aula tem 1 aluno, considera match (resolve casos como Davi/Diogo). **Fidelidade medida**: Barra Maio 2026 vai de 50% → 77% vs CSV Emusys. **Sem backfill** (decisão do usuário). Validação no agente `fiscal-dados` (queries #10-12).

- **sync-presenca-emusys: flags experimental_* não sincronizadas com canônica (RESOLVIDO 2026-05-21 v26)**: a função `reconciliarExperimentaisOrfas` inseria linha em `lead_experimentais` mas não atualizava as flags `experimental_agendada/realizada/faltou` em `leads`. Resultado: leads cuja experimental foi reconciliada pelo cron diário (em vez do webhook em tempo real) ficavam com flags `false` mesmo tendo experimental na canônica. Casos detectados em maio/2026: Manuela Dias (CG 8958), Roberta Ferreira (REC 8986). Fix aplicado em v26: depois do INSERT bem-sucedido, faz UPDATE em leads setando `experimental_agendada=true, experimental_realizada=presente, faltou_experimental=!presente` (sem mexer em status/etapa, pra preservar leads já convertidos). Limitação: só corrige casos NOVOS — leads que já tinham linha em `lead_experimentais` permanecem com flags antigas (sem backfill por decisão do usuário).

- **UI de marcação manual de presença adicionada na AgendaTab (RESOLVIDO 2026-05-21, ampliado pra visitas)**: agora a aba Agenda do Pré-Atendimento mostra botões "✅ Realizada" / "❌ Faltou" em cada evento com status `agendada` (nas vistas Semana, Lista e Dia), cobrindo experimentais E visitas. Handler `handleMarcarPresenca` roteia por tipo: Experimental atualiza `leads` (flags+status+etapa) com proteção pra não regredir convertidos + UPSERT em `lead_experimentais`; Visita atualiza só `visitas.status` (mapeando `faltou`→`nao_compareceu` do CHECK constraint). Cores dos cards diferenciadas por TIPO (Experimental=violeta, Visita=amber) com badge de status sobreposto.

- **Matrícula manual no ComercialPage com "Teve experimental?" agora sincroniza com canônico (RESOLVIDO 2026-05-22)**: o `handleSave` do modal de matrícula agora cria automaticamente uma linha em `lead_experimentais` com status `experimental_realizada` quando o checkbox "Teve experimental?" estiver marcado. Usa `formData.data` (data da matrícula) como `data_experimental` (aproximação aceita). Marcador implícito: `emusys_lead_id=NULL` na canônica distingue esses registros dos vindos do webhook Emusys. **Limitação:** não cobre o caso "Lançamento diário" agregado (cards Lead/Experimental/Visita com `quantidade>1`) — esses são contagens sem nome de aluno individual e não fazem sentido em `lead_experimentais` (que espera uma linha por experimental nominal).

- **Marcação manual de experimental no Comercial não sincroniza com `lead_experimentais` (DESCOBERTO 2026-05-21, RESOLVIDO 2026-05-22 parcialmente)**: o formulário "Lançamento diário" no `ComercialPage.tsx` (rota `/comercial`) permite criar registros com status `experimental_agendada/realizada/faltou/visita_escola/convertido` direto na tabela `leads`. Esse INSERT **não atualiza `lead_experimentais`** (canônico) na maioria dos casos. **Solução parcial aplicada**: matrícula com `teve_experimental=true` agora sincroniza (ver entrada acima). **Ainda pendente**: lançamentos agregados via cards `Lead`/`Experimental`/`Visita` (com `quantidade>1`, sem nome individual) — esses são contagens, não experimentais nominais. Decisão: deixar como está (não fazem sentido no canônico). **Investigar antes de mexer mais**: existe uma `PlanilhaComercial.tsx` exportada mas não usada (código morto) — verificar se deveria estar renderizada em algum lugar.

- **Redefinição semântica dos KPIs do Comercial — funil correto (proposto 2026-05-21)**: o dashboard Comercial deveria seguir um funil clássico, todos os marcos dentro do MESMO período selecionado:
  - **Leads novos no período** (já correto): `leads.data_contato BETWEEN startDate AND endDate`
  - **Experimentais agendadas no período**: desses leads novos, quantas experimentais foram AGENDADAS durante o período. Query proposta: `JOIN lead_experimentais le ON le.lead_id = l.id WHERE le.created_at BETWEEN startDate AND endDate AND le.status != 'cancelada'`. **Hoje filtra errado por `data_experimental` (data DA AULA) em vez de `created_at` (data DO AGENDAMENTO)** — por isso Alice Duarte (aula 22/05) e Alice e Giovana (aula 26/05), ambas agendadas hoje na Barra, somem do filtro "Hoje" mesmo sendo o caso clássico de "agendado hoje".
  - **Matrículas no período**: leads novos do período que matricularam no período. Verificar se a query atual filtra apenas por status ou se também filtra por `leads.data_conversao BETWEEN startDate AND endDate`. Se for só por status, está errado em períodos longos.
  - Versão **estrita** (recomendada pra esse dashboard "Lançamento diário"): tudo dentro do mesmo range — lead + experimental + matrícula no período.
  - Lacuna conceitual: também não existe view "Atividade do dia" mostrando o que o comercial agendou/matriculou hoje independente de quando o lead chegou.
  - Só frontend (ajuste de query no ComercialPage.tsx), sem mudança de banco. Reversível sem risco.